import {useEffect,useMemo,useState} from 'react';import './lessonPlayer.css';import './theoryExperience.css';import {lessonOneHundredThirtyThreePractice,lessonOneHundredThirtyThreeResponseCount,type LessonOneHundredThirtyThreeField,type LessonOneHundredThirtyThreeTask} from './data/lessonOneHundredThirtyThreePractice';
type Stage={id:string;eyebrow:string;title:string;body:string;note?:string;practice?:LessonOneHundredThirtyThreeTask;summary?:boolean};type Saved={version:1;stageIndex:number;responses:Record<string,string>;checked:Record<string,boolean>;attempts:Record<string,number>};type Jump={lessonNumber?:number;stageIndex?:number};const KEY='mathnikita-lesson-133-progress-v1';const norm=(v:string)=>v.normalize('NFKC').trim().toLocaleLowerCase('ru-RU').replace(/\s+/g,'').replace(/,/g,'.');const canonicalDecimal=(v:string)=>{let s=norm(v);if(!/^[+-]?(?:\d+(?:\.\d*)?|\.\d+)$/.test(s))return null;let sign='';if(s.startsWith('+')||s.startsWith('-')){sign=s.startsWith('-')?'-':'';s=s.slice(1)}let[whole,frac='']=s.split('.');whole=(whole||'0').replace(/^0+(?=\d)/,'');frac=frac.replace(/0+$/,'');if(whole==='0'&&!frac)sign='';return `${sign}${whole}${frac?`.${frac}`:''}`};const matchField=(value:string,field:LessonOneHundredThirtyThreeField)=>{const v=norm(value);if(!v)return false;const cv=canonicalDecimal(v);return field.answers.some(answer=>{const a=norm(answer);if(v===a)return true;const ca=canonicalDecimal(a);return cv!==null&&ca!==null&&cv===ca})};
const concepts:Stage[]=[
{id:'l133-meaning',eyebrow:'Урок 133 · § 35 · 1 из 9',title:'Деление отменяет умножение',body:'Если a:b=c, то c×b=a. Поэтому любое частное можно проверить обратным умножением.',note:'Например, 13,8:4=3,45, потому что 3,45×4=13,8.'},
{id:'l133-column',eyebrow:'Алгоритм',title:'Делим поразрядно, как натуральные числа',body:'Начинаем с целой части и последовательно сносим цифры делимого. Как только переходим через запятую делимого, ставим запятую в частном.',note:'Запятая появляется по разряду, а не по количеству цифр после запятой.'},
{id:'l133-leading-zero',eyebrow:'Частное меньше единицы',title:'Если целая часть меньше делителя — начинаем с 0,',body:'Когда делимое меньше делителя, целая часть частного равна нулю. Затем продолжаем деление десятых, сотых и тысячных.',note:'11,34:42=0,27; ноль перед запятой обязателен.'},
{id:'l133-append-zero',eyebrow:'Продолжаем деление',title:'Нули справа не меняют десятичную дробь',body:'Если цифры делимого закончились, а остаток ещё есть, можно дописать справа нули и продолжить деление.',note:'7=7,00, поэтому 7:4=1,75.'},
{id:'l133-powers',eyebrow:'10 · 100 · 1000',title:'При делении на 10ⁿ запятая движется влево',body:'Деление на 10, 100 и 1000 уменьшает число соответственно в 10, 100 и 1000 раз. Запятая переносится влево на 1, 2 или 3 разряда.',note:'3:100=0,03; 0,96:1000=0,00096.'},
{id:'l133-estimate',eyebrow:'Самопроверка',title:'Сначала оцени масштаб результата',body:'Если делитель больше 1, положительное частное меньше делимого. Сравнение делимого и делителя помогает понять, будет ответ больше или меньше единицы.',note:'6,3:7 меньше 1, поэтому 9 или 0,09 сразу подозрительны; правильный ответ 0,9.'},
{id:'l133-route',eyebrow:'Маршрут урока',title:'Правило → уголок → нули → разрядные единицы → задачи',body:'Разбираем точные №964, №967(1–6), №997, затем закрепляем деление на натуральное число в вычислениях, уравнениях, величинах и геометрии.',note:'20 задач · 50 проверяемых ответов. Делитель во всём уроке остаётся натуральным числом.'}
];
const practiceStages:Stage[]=lessonOneHundredThirtyThreePractice.map((practice,i)=>({id:`l133-practice-${String(i+1).padStart(2,'0')}`,eyebrow:practice.sourceExact?`Учебник · ${practice.source}`:`Обязательная практика · ${i+1} из 20`,title:practice.title,body:practice.prompt,practice}));const stages:Stage[]=[...concepts,...practiceStages,{id:'l133-summary',eyebrow:'Итог урока 133',title:'Основа деления десятичных дробей готова',body:'Ты умеешь делить десятичную дробь на натуральное число, получать десятичное частное из натуральных чисел, делить на 10, 100 и 1000 и проверять результат умножением.',note:'Урок 134 продолжит §35 и закрепит деление на натуральное число.',summary:true}];
export const lessonOneHundredThirtyThreeStageCount=stages.length;export const lessonOneHundredThirtyThreePracticeTaskCount=lessonOneHundredThirtyThreePractice.length;export const lessonOneHundredThirtyThreePracticeResponseCount=lessonOneHundredThirtyThreeResponseCount;
function load():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}};const p=JSON.parse(raw) as Saved;return p?.version===1?p:{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}catch{return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}}
export function DecimalDivisionFoundationsPlayer(){const initial=useMemo(load,[]);const[stageIndex,setStageIndex]=useState(()=>Math.max(0,Math.min(initial.stageIndex,stages.length-1)));const[responses,setResponses]=useState(initial.responses);const[checked,setChecked]=useState(initial.checked);const[attempts,setAttempts]=useState(initial.attempts);const stage=stages[stageIndex];const task=stage.practice;const key=stage.id;const complete=task?task.fields.every(field=>matchField(responses[`${key}:${field.id}`]??'',field)):true;useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,checked,attempts} satisfies Saved))},[stageIndex,responses,checked,attempts]);useEffect(()=>{const handler=(event:Event)=>{const d=(event as CustomEvent<Jump>).detail;if(d?.lessonNumber!==133||typeof d.stageIndex!=='number')return;setStageIndex(Math.max(0,Math.min(Math.trunc(d.stageIndex),stages.length-1)))};window.addEventListener('mathnikita-go-to-stage',handler);return()=>window.removeEventListener('mathnikita-go-to-stage',handler)},[]);const check=()=>{if(task){setChecked(p=>({...p,[key]:true}));setAttempts(p=>({...p,[key]:(p[key]??0)+1}))}};const move=(delta:number)=>setStageIndex(i=>Math.max(0,Math.min(i+delta,stages.length-1)));return <section className="lesson-player theory-experience" aria-label="Урок 133: деление десятичной дроби на натуральное число"><article className={`interactive-stage ${stage.summary?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p>{stage.note?<p className="theory-note">{stage.note}</p>:null}</div>{task?<div className="activity-area"><h3>{task.prompt}</h3><div className="answer-grid">{task.fields.map(field=>{const responseKey=`${key}:${field.id}`;return <label className="inline-answer" key={field.id}><span>{field.label}</span><input value={responses[responseKey]??''} placeholder={field.placeholder??'Введите ответ'} onChange={event=>{setResponses(p=>({...p,[responseKey]:event.target.value}));setChecked(p=>({...p,[key]:false}))}}/></label>})}</div><button className="check-button" type="button" onClick={check}>Проверить</button>{checked[key]?complete?<div className="instant-feedback good" data-explanation={task.explanation}><b>Верно.</b><span>{task.explanation}</span></div>:<div className="instant-feedback bad" data-explanation={task.explanation}><b>Пока есть ошибка.</b><span>{attempts[key]>1?task.hint:'Проверь разряд частного, положение запятой и обратное умножение.'}</span></div>:null}</div>:null}{stage.summary?<div className="summary-card"><b>Урок 133 завершён</b><span>20 задач · 50 ответов · § 35 · 1 из 9</span></div>:null}</article><nav className="lesson-controls" aria-label="Навигация по этапам урока 133"><button type="button" onClick={()=>move(-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {stages.length}</span><button type="button" onClick={()=>move(1)} disabled={stageIndex===stages.length-1}>Дальше →</button></nav></section>}
