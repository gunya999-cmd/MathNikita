import {useEffect,useMemo,useState} from 'react';import './lessonPlayer.css';import './theoryExperience.css';import {lessonOneHundredFortySevenPractice,lessonOneHundredFortySevenResponseCount,type LessonOneHundredFortySevenField,type LessonOneHundredFortySevenTask} from './data/lessonOneHundredFortySevenPractice';
type Stage={id:string;eyebrow:string;title:string;body:string;note?:string;practice?:LessonOneHundredFortySevenTask;summary?:boolean};type Saved={version:1;stageIndex:number;responses:Record<string,string>;checked:Record<string,boolean>;attempts:Record<string,number>};type Jump={lessonNumber?:number;stageIndex?:number};const KEY='mathnikita-lesson-147-progress-v1';const norm=(v:string)=>v.normalize('NFKC').trim().toLocaleLowerCase('ru-RU').replace(/\s+/g,'').replace(/,/g,'.');const canonicalDecimal=(v:string)=>{let s=norm(v);if(!/^[+-]?(?:\d+(?:\.\d*)?|\.\d+)$/.test(s))return null;let sign='';if(s.startsWith('+')||s.startsWith('-')){sign=s.startsWith('-')?'-':'';s=s.slice(1)}let[whole,frac='']=s.split('.');whole=(whole||'0').replace(/^0+(?=\d)/,'');frac=frac.replace(/0+$/,'');if(whole==='0'&&!frac)sign='';return `${sign}${whole}${frac?`.${frac}`:''}`};const matchField=(value:string,field:LessonOneHundredFortySevenField)=>{const v=norm(value);if(!v)return false;const cv=canonicalDecimal(v);return field.answers.some(answer=>{const a=norm(answer);if(v===a)return true;const ca=canonicalDecimal(a);return cv!==null&&ca!==null&&cv===ca})};
const concepts:Stage[]=[
{id:'l147-to-decimal',eyebrow:'Урок 147 · § 37 · 2 из 4',title:'Проценты → десятичная дробь',body:'Чтобы записать проценты в виде десятичной дроби, раздели число процентов на 100. Например, 6%=0,06, 84%=0,84, 160%=1,6.',note:'Вопрос 4 §37: число процентов делим на 100.'},
{id:'l147-from-decimal',eyebrow:'Обратное преобразование',title:'Десятичная дробь → проценты',body:'Чтобы записать десятичную дробь в процентах, умножь её на 100. Например, 0,58=58%, 0,008=0,8%, 2,5=250%.',note:'Умножение на 100 — обратное действие к делению на 100.'},
{id:'l147-find-percent',eyebrow:'Как найти p% от числа',title:'Преврати процент в множитель',body:'Сначала запиши p% десятичной дробью p/100, затем умножь исходное число на этот множитель: N·p/100.',note:'Вопрос 5 §37: несколько процентов от числа находят умножением числа на соответствующую десятичную дробь.'},
{id:'l147-estimate',eyebrow:'Проверка смысла',title:'До вычисления оцени ответ',body:'Если процент меньше 100%, найденная часть должна быть меньше исходного числа. Если процент больше 100%, результат будет больше исходного.',note:'Такая оценка помогает быстро заметить ошибку с запятой.'},
{id:'l147-textbook-conversions',eyebrow:'Учебник · №1063 и №1065',title:'Два направления должны стать автоматическими',body:'№1063 тренирует перевод процентов в десятичные дроби, а №1065 — обратный перевод. Важно различать 0,8=80% и 0,008=0,8%.',note:'Два разряда при умножении или делении на 100 меняют масштаб числа.'},
{id:'l147-rally',eyebrow:'Учебник · №1068',title:'72% от 565 км',body:'72%=0,72. Поэтому путь первого дня равен 565·0,72=406,8 км.',note:'Это прямое применение правила нахождения процентов от числа.'},
{id:'l147-route',eyebrow:'Маршрут урока',title:'Вопросы 4–5, №1063, №1065, №1068',body:'Сначала закрепляем преобразования, затем используем процент как десятичный множитель в задачах на путь, деньги, массу, время и количество.',note:'20 задач · 50 проверяемых ответов. Урок 148 продолжит практику §37.'}
];
const practiceStages:Stage[]=lessonOneHundredFortySevenPractice.map((practice,i)=>({id:`l147-practice-${String(i+1).padStart(2,'0')}`,eyebrow:practice.sourceExact?`Учебник · ${practice.source}`:`Обязательная практика · ${i+1} из 20`,title:practice.title,body:practice.prompt,practice}));const stages:Stage[]=[...concepts,...practiceStages,{id:'l147-summary',eyebrow:'Итог урока 147',title:'Процент стал удобным множителем',body:'Ты умеешь переводить проценты в десятичные дроби и обратно, находить p% от числа одним умножением и проверять разумность результата до точного вычисления.',note:'Далее — урок 148: закрепление §37 и более составные процентные задачи.',summary:true}];
export const lessonOneHundredFortySevenStageCount=stages.length;export const lessonOneHundredFortySevenPracticeTaskCount=lessonOneHundredFortySevenPractice.length;export const lessonOneHundredFortySevenPracticeResponseCount=lessonOneHundredFortySevenResponseCount;
function load():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}};const p=JSON.parse(raw) as Saved;return p?.version===1?p:{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}catch{return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}}
export function PercentConversionApplicationsPlayer(){const initial=useMemo(load,[]);const[stageIndex,setStageIndex]=useState(()=>Math.max(0,Math.min(initial.stageIndex,stages.length-1)));const[responses,setResponses]=useState(initial.responses);const[checked,setChecked]=useState(initial.checked);const[attempts,setAttempts]=useState(initial.attempts);const stage=stages[stageIndex];const task=stage.practice;const key=stage.id;const complete=task?task.fields.every(field=>matchField(responses[`${key}:${field.id}`]??'',field)):true;useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,checked,attempts} satisfies Saved))},[stageIndex,responses,checked,attempts]);useEffect(()=>{const handler=(event:Event)=>{const d=(event as CustomEvent<Jump>).detail;if(d?.lessonNumber!==147||typeof d.stageIndex!=='number')return;setStageIndex(Math.max(0,Math.min(Math.trunc(d.stageIndex),stages.length-1)))};window.addEventListener('mathnikita-go-to-stage',handler);return()=>window.removeEventListener('mathnikita-go-to-stage',handler)},[]);const check=()=>{if(task){setChecked(p=>({...p,[key]:true}));setAttempts(p=>({...p,[key]:(p[key]??0)+1}))}};const move=(delta:number)=>setStageIndex(i=>Math.max(0,Math.min(i+delta,stages.length-1)));return <section className="lesson-player theory-experience" aria-label="Урок 147: проценты, десятичные дроби и проценты от числа"><article className={`interactive-stage ${stage.summary?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p>{stage.note?<p className="theory-note">{stage.note}</p>:null}</div>{task?<div className="activity-area"><h3>{task.prompt}</h3><div className="answer-grid">{task.fields.map(field=>{const responseKey=`${key}:${field.id}`;return <label className="inline-answer" key={field.id}><span>{field.label}</span><input value={responses[responseKey]??''} placeholder={field.placeholder??'Введите ответ'} onChange={event=>{setResponses(p=>({...p,[responseKey]:event.target.value}));setChecked(p=>({...p,[key]:false}))}}/></label>})}</div><button className="check-button" type="button" onClick={check}>Проверить</button>{checked[key]?complete?<div className="instant-feedback good" data-explanation={task.explanation}><b>Верно.</b><span>{task.explanation}</span></div>:<div className="instant-feedback bad" data-explanation={task.explanation}><b>Пока есть ошибка.</b><span>{attempts[key]>1?task.hint:'Проверь перевод процента в десятичную дробь и положение запятой.'}</span></div>:null}</div>:null}{stage.summary?<div className="summary-card"><b>Урок 147 завершён</b><span>20 задач · 50 ответов · § 37 · 2 из 4</span></div>:null}</article><nav className="lesson-controls" aria-label="Навигация по этапам урока 147"><button type="button" onClick={()=>move(-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {stages.length}</span><button type="button" onClick={()=>move(1)} disabled={stageIndex===stages.length-1}>Дальше →</button></nav></section>}
