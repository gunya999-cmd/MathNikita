import {useEffect,useMemo,useState} from 'react';import './lessonPlayer.css';import './theoryExperience.css';import {lessonOneHundredFortyNinePractice,lessonOneHundredFortyNineResponseCount,type LessonOneHundredFortyNineField,type LessonOneHundredFortyNineTask} from './data/lessonOneHundredFortyNinePractice';
type Stage={id:string;eyebrow:string;title:string;body:string;note?:string;practice?:LessonOneHundredFortyNineTask;summary?:boolean};type Saved={version:1;stageIndex:number;responses:Record<string,string>;checked:Record<string,boolean>;attempts:Record<string,number>};type Jump={lessonNumber?:number;stageIndex?:number};const KEY='mathnikita-lesson-149-progress-v1';const norm=(v:string)=>v.normalize('NFKC').trim().toLocaleLowerCase('ru-RU').replace(/\s+/g,'').replace(/,/g,'.');const canonicalDecimal=(v:string)=>{let s=norm(v);if(!/^[+-]?(?:\d+(?:\.\d*)?|\.\d+)$/.test(s))return null;let sign='';if(s.startsWith('+')||s.startsWith('-')){sign=s.startsWith('-')?'-':'';s=s.slice(1)}let[whole,frac='']=s.split('.');whole=(whole||'0').replace(/^0+(?=\d)/,'');frac=frac.replace(/0+$/,'');if(whole==='0'&&!frac)sign='';return `${sign}${whole}${frac?`.${frac}`:''}`};const matchField=(value:string,field:LessonOneHundredFortyNineField)=>{const v=norm(value);if(!v)return false;const cv=canonicalDecimal(v);return field.answers.some(answer=>{const a=norm(answer);if(v===a)return true;const ca=canonicalDecimal(a);return cv!==null&&ca!==null&&cv===ca})};
const concepts:Stage[]=[
{id:'l149-base',eyebrow:'Урок 149 · § 37 · 4 из 4',title:'Главный вопрос: от какого числа берётся процент?',body:'В цепочке действий 100% может меняться. После первого шага новая величина часто становится базой для следующего процента.',note:'Нельзя автоматически считать все проценты от исходного числа.'},
{id:'l149-chain',eyebrow:'Процентная цепочка',title:'Решай строго по шагам',body:'Сначала вычисли результат первого процентного действия. Затем внимательно прочитай, относится ли второй процент к исходной величине или к уже полученному результату.',note:'№1079: 110% берутся от июньских потерь, а не от общей суммы за лето.'},
{id:'l149-remainder-base',eyebrow:'Процент от остатка',title:'Остаток становится новыми 100%',body:'Если сказано «15% запаса на начало этой недели» или «25% оставшегося пути», то перед вторым вычислением нужно найти новый остаток.',note:'В №1084 после первой недели 100% — уже 2040 л.'},
{id:'l149-geometry',eyebrow:'Проценты в геометрии',title:'Сначала восстанови нужную величину, затем используй формулы',body:'Если длина составляет 135% ширины, сначала найди длину через множитель 1,35. Только после этого вычисляй периметр и площадь.',note:'№1082: 40·1,35=54 см; P=188 см; S=2160 см².'},
{id:'l149-repeated-change',eyebrow:'Последовательные изменения',title:'Два изменения не всегда можно сложить',body:'Рост на 10%, а затем на 25% означает умножение сначала на 1,10, затем на 1,25. Аналогично две скидки применяются последовательно к разным базам.',note:'Процент второго шага относится к уже изменённой величине.'},
{id:'l149-source',eyebrow:'Учебник · №1079, №1082, №1084',title:'Три модели итогового урока §37',body:'№1079 — процент от результата предыдущего шага; №1082 — процентное отношение в геометрии; №1084 — повторяющийся процент от текущего остатка.',note:'Ответы: 94710 руб.; P=188 см и S=2160 см²; 2040 л и 1734 л.'},
{id:'l149-boundary',eyebrow:'Граница темы',title:'Сегодня известное целое остаётся известным',body:'Во всех задачах §37 исходное целое или текущая база известны, и мы находим их процентную часть или изменённое значение. Нахождение неизвестного целого по известной процентной части начнётся в §38.',note:'20 задач · 50 проверяемых ответов. После этого §37 завершён.'}
];
const practiceStages:Stage[]=lessonOneHundredFortyNinePractice.map((practice,i)=>({id:`l149-practice-${String(i+1).padStart(2,'0')}`,eyebrow:practice.sourceExact?`Учебник · ${practice.source}`:`Обязательная практика · ${i+1} из 20`,title:practice.title,body:practice.prompt,practice}));const stages:Stage[]=[...concepts,...practiceStages,{id:'l149-summary',eyebrow:'Итог урока 149',title:'§37 завершён',body:'Ты умеешь находить проценты от числа, переводить проценты и десятичные дроби, решать составные процентные задачи и отслеживать смену базы в последовательных шагах.',note:'Далее — урок 150 и §38: нахождение числа по его процентам.',summary:true}];
export const lessonOneHundredFortyNineStageCount=stages.length;export const lessonOneHundredFortyNinePracticeTaskCount=lessonOneHundredFortyNinePractice.length;export const lessonOneHundredFortyNinePracticeResponseCount=lessonOneHundredFortyNineResponseCount;
function load():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}};const p=JSON.parse(raw) as Saved;return p?.version===1?p:{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}catch{return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}}
export function PercentSectionFinalePlayer(){const initial=useMemo(load,[]);const[stageIndex,setStageIndex]=useState(()=>Math.max(0,Math.min(initial.stageIndex,stages.length-1)));const[responses,setResponses]=useState(initial.responses);const[checked,setChecked]=useState(initial.checked);const[attempts,setAttempts]=useState(initial.attempts);const stage=stages[stageIndex];const task=stage.practice;const key=stage.id;const complete=task?task.fields.every(field=>matchField(responses[`${key}:${field.id}`]??'',field)):true;useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,checked,attempts} satisfies Saved))},[stageIndex,responses,checked,attempts]);useEffect(()=>{const handler=(event:Event)=>{const d=(event as CustomEvent<Jump>).detail;if(d?.lessonNumber!==149||typeof d.stageIndex!=='number')return;setStageIndex(Math.max(0,Math.min(Math.trunc(d.stageIndex),stages.length-1)))};window.addEventListener('mathnikita-go-to-stage',handler);return()=>window.removeEventListener('mathnikita-go-to-stage',handler)},[]);const check=()=>{if(task){setChecked(p=>({...p,[key]:true}));setAttempts(p=>({...p,[key]:(p[key]??0)+1}))}};const move=(delta:number)=>setStageIndex(i=>Math.max(0,Math.min(i+delta,stages.length-1)));return <section className="lesson-player theory-experience" aria-label="Урок 149: итоговые задачи на проценты"><article className={`interactive-stage ${stage.summary?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p>{stage.note?<p className="theory-note">{stage.note}</p>:null}</div>{task?<div className="activity-area"><h3>{task.prompt}</h3><div className="answer-grid">{task.fields.map(field=>{const responseKey=`${key}:${field.id}`;return <label className="inline-answer" key={field.id}><span>{field.label}</span><input value={responses[responseKey]??''} placeholder={field.placeholder??'Введите ответ'} onChange={event=>{setResponses(p=>({...p,[responseKey]:event.target.value}));setChecked(p=>({...p,[key]:false}))}}/></label>})}</div><button className="check-button" type="button" onClick={check}>Проверить</button>{checked[key]?complete?<div className="instant-feedback good" data-explanation={task.explanation}><b>Верно.</b><span>{task.explanation}</span></div>:<div className="instant-feedback bad" data-explanation={task.explanation}><b>Пока есть ошибка.</b><span>{attempts[key]>1?task.hint:'Проверь, какое число является 100% именно на этом шаге.'}</span></div>:null}</div>:null}{stage.summary?<div className="summary-card"><b>Урок 149 завершён</b><span>20 задач · 50 ответов · § 37 · 4 из 4</span></div>:null}</article><nav className="lesson-controls" aria-label="Навигация по этапам урока 149"><button type="button" onClick={()=>move(-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {stages.length}</span><button type="button" onClick={()=>move(1)} disabled={stageIndex===stages.length-1}>Дальше →</button></nav></section>}
