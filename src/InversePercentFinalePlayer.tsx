import {useEffect,useMemo,useState} from 'react';import './lessonPlayer.css';import './theoryExperience.css';import {lessonOneHundredFiftyThreePractice,lessonOneHundredFiftyThreeResponseCount,type LessonOneHundredFiftyThreeField,type LessonOneHundredFiftyThreeTask} from './data/lessonOneHundredFiftyThreePractice';
type Stage={id:string;eyebrow:string;title:string;body:string;note?:string;practice?:LessonOneHundredFiftyThreeTask;summary?:boolean};type Saved={version:1;stageIndex:number;responses:Record<string,string>;checked:Record<string,boolean>;attempts:Record<string,number>};type Jump={lessonNumber?:number;stageIndex?:number};const KEY='mathnikita-lesson-153-progress-v1';const norm=(v:string)=>v.normalize('NFKC').trim().toLocaleLowerCase('ru-RU').replace(/\s+/g,'').replace(/,/g,'.');const canonicalDecimal=(v:string)=>{let s=norm(v);if(!/^[+-]?(?:\d+(?:\.\d*)?|\.\d+)$/.test(s))return null;let sign='';if(s.startsWith('+')||s.startsWith('-')){sign=s.startsWith('-')?'-':'';s=s.slice(1)}let[whole,frac='']=s.split('.');whole=(whole||'0').replace(/^0+(?=\d)/,'');frac=frac.replace(/0+$/,'');if(whole==='0'&&!frac)sign='';return `${sign}${whole}${frac?`.${frac}`:''}`};const matchField=(value:string,field:LessonOneHundredFiftyThreeField)=>{const v=norm(value);if(!v)return false;const cv=canonicalDecimal(v);return field.answers.some(answer=>{const a=norm(answer);if(v===a)return true;const ca=canonicalDecimal(a);return cv!==null&&ca!==null&&cv===ca})};
const concepts:Stage[]=[
{id:'l153-base',eyebrow:'Урок 153 · § 38 · 4 из 4',title:'Процент всегда имеет базу',body:'В составной задаче 45% может относиться не к исходному количеству, а к тому, что осталось после первого шага. Перед каждым вычислением назови величину, которая сейчас равна 100%.',note:'Главный вопрос финала §38: «100% чего?»'},
{id:'l153-remainder',eyebrow:'Процент от остатка',title:'Последовательные проценты перемножаются',body:'Если после первого шага осталось 68% исходного, а затем осталось 55% от этого остатка, итоговый остаток равен 0,68·0,55=0,374, то есть 37,4% исходного.',note:'Так устроено №1113.'},
{id:'l153-share-of-share',eyebrow:'Доля от доли',title:'Процент группы можно перевести в процент от всего',body:'Если розы — 60% всех цветов, а гладиолусы — 40% от числа роз, то гладиолусы составляют 0,6·0,4=0,24=24% всех цветов.',note:'Так мы приводим разные базы к одной базе 100%.'},
{id:'l153-inverse',eyebrow:'Восстановление 100%',title:'Сначала найди известный процент, затем дели',body:'Когда известная величина соответствует уже вычисленным 37,4%, 16% или 56%, исходное целое находится делением на 0,374; 0,16; 0,56 соответственно.',note:'Не угадывай делитель по первому проценту в условии.'},
{id:'l153-units',eyebrow:'Единицы измерения',title:'Процентная логика не отменяет обычную математику',body:'В №1120 сначала надо корректно перевести 2 ч 6 мин в часы: 6 минут — это 0,1 часа, значит время 2,1 часа. Только затем можно использовать формулу движения.',note:'Ошибочная единица измерения разрушит даже правильную процентную схему.'},
{id:'l153-check',eyebrow:'Обратная проверка',title:'После восстановления целого вернись к условию',body:'Найденные 100% полезно проверить прямым действием: вычислить требуемый процент, остаток или сумму скоростей и убедиться, что получается исходное известное значение.',note:'Обратная проверка ловит неверно выбранную базу.'},
{id:'l153-source',eyebrow:'Учебник · №1113, №1115, №1120',title:'Финал §38',body:'Три опорные задачи соединяют процент от остатка, долю от доли, восстановление целого и перевод единиц. После этого §38 завершён.',note:'20 задач · 50 проверяемых ответов. Далее — урок 154: повторение §36–§38.'}
];
const practiceStages:Stage[]=lessonOneHundredFiftyThreePractice.map((practice,i)=>({id:`l153-practice-${String(i+1).padStart(2,'0')}`,eyebrow:practice.sourceExact?`Учебник · ${practice.source}`:`Обязательная практика · ${i+1} из 20`,title:practice.title,body:practice.prompt,practice}));const stages:Stage[]=[...concepts,...practiceStages,{id:'l153-summary',eyebrow:'Итог урока 153',title:'§38 завершён',body:'Ты умеешь определять базу каждого процента, работать с процентом от остатка и долей от доли, восстанавливать 100%, переводить единицы и проверять ответ обратным ходом.',note:'Следующий этап — повторение §36–§38 перед контрольной работой №9.',summary:true}];
export const lessonOneHundredFiftyThreeStageCount=stages.length;export const lessonOneHundredFiftyThreePracticeTaskCount=lessonOneHundredFiftyThreePractice.length;export const lessonOneHundredFiftyThreePracticeResponseCount=lessonOneHundredFiftyThreeResponseCount;
function load():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}};const p=JSON.parse(raw) as Saved;return p?.version===1?p:{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}catch{return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}}
export function InversePercentFinalePlayer(){const initial=useMemo(load,[]);const[stageIndex,setStageIndex]=useState(()=>Math.max(0,Math.min(initial.stageIndex,stages.length-1)));const[responses,setResponses]=useState(initial.responses);const[checked,setChecked]=useState(initial.checked);const[attempts,setAttempts]=useState(initial.attempts);const stage=stages[stageIndex];const task=stage.practice;const key=stage.id;const complete=task?task.fields.every(field=>matchField(responses[`${key}:${field.id}`]??'',field)):true;useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,checked,attempts} satisfies Saved))},[stageIndex,responses,checked,attempts]);useEffect(()=>{const handler=(event:Event)=>{const d=(event as CustomEvent<Jump>).detail;if(d?.lessonNumber!==153||typeof d.stageIndex!=='number')return;setStageIndex(Math.max(0,Math.min(Math.trunc(d.stageIndex),stages.length-1)))};window.addEventListener('mathnikita-go-to-stage',handler);return()=>window.removeEventListener('mathnikita-go-to-stage',handler)},[]);const check=()=>{if(task){setChecked(p=>({...p,[key]:true}));setAttempts(p=>({...p,[key]:(p[key]??0)+1}))}};const move=(delta:number)=>setStageIndex(i=>Math.max(0,Math.min(i+delta,stages.length-1)));return <section className="lesson-player theory-experience" aria-label="Урок 153: итог параграфа 38"><article className={`interactive-stage ${stage.summary?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p>{stage.note?<p className="theory-note">{stage.note}</p>:null}</div>{task?<div className="activity-area"><h3>{task.prompt}</h3><div className="answer-grid">{task.fields.map(field=>{const responseKey=`${key}:${field.id}`;return <label className="inline-answer" key={field.id}><span>{field.label}</span><input value={responses[responseKey]??''} placeholder={field.placeholder??'Введите ответ'} onChange={event=>{setResponses(p=>({...p,[responseKey]:event.target.value}));setChecked(p=>({...p,[key]:false}))}}/></label>})}</div><button className="check-button" type="button" onClick={check}>Проверить</button>{checked[key]?complete?<div className="instant-feedback good" data-explanation={task.explanation}><b>Верно.</b><span>{task.explanation}</span></div>:<div className="instant-feedback bad" data-explanation={task.explanation}><b>Пока есть ошибка.</b><span>{attempts[key]>1?task.hint:'Назови текущие 100%, приведи проценты к одной базе и только потом восстанавливай целое.'}</span></div>:null}</div>:null}{stage.summary?<div className="summary-card"><b>Урок 153 завершён</b><span>20 задач · 50 ответов · §38 завершён</span></div>:null}</article><nav className="lesson-controls" aria-label="Навигация по этапам урока 153"><button type="button" onClick={()=>move(-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {stages.length}</span><button type="button" onClick={()=>move(1)} disabled={stageIndex===stages.length-1}>Дальше →</button></nav></section>}
