import {useEffect,useMemo,useState} from 'react';import './lessonPlayer.css';import './theoryExperience.css';import {lessonOneHundredThirtyOnePractice,lessonOneHundredThirtyOneResponseCount,type LessonOneHundredThirtyOneField,type LessonOneHundredThirtyOneTask} from './data/lessonOneHundredThirtyOnePractice';
type Stage={id:string;eyebrow:string;title:string;body:string;note?:string;practice?:LessonOneHundredThirtyOneTask;summary?:boolean};type Saved={version:1;stageIndex:number;responses:Record<string,string>;checked:Record<string,boolean>;attempts:Record<string,number>};type Jump={lessonNumber?:number;stageIndex?:number};const KEY='mathnikita-lesson-131-progress-v1';const norm=(v:string)=>v.normalize('NFKC').trim().toLocaleLowerCase('ru-RU').replace(/\s+/g,'').replace(/,/g,'.');const matchField=(value:string,field:LessonOneHundredThirtyOneField)=>field.answers.some(answer=>norm(value)===norm(answer));
const concepts:Stage[]=[
{id:'l131-structure',eyebrow:'Урок 131 · § 34 · 6 из 7',title:'Сначала структура, потом вычисление',body:'Длинное выражение часто становится коротким, если заметить общий множитель или одинаковую буквенную часть.',note:'Цель — уменьшить число действий и риск ошибки.'},
{id:'l131-distribute',eyebrow:'Распределительное свойство',title:'Общий множитель выносим за скобки',body:'ab+ac=a(b+c), а ab−ac=a(b−c). Это особенно полезно, когда внутри скобок получается 10, 1 или 0.',note:'№946: вместо трёх отдельных умножений можно сделать одно.'},
{id:'l131-like',eyebrow:'Буквенные выражения',title:'Складываем коэффициенты при одинаковой букве',body:'0,13p+0,47p=(0,13+0,47)p=0,6p. После упрощения подставляем значение переменной.',note:'Сначала упрощение, затем подстановка.'},
{id:'l131-order',eyebrow:'Порядок действий',title:'Составное выражение решаем по шагам',body:'Сначала действия в скобках и отдельные произведения, затем объединяем результаты.',note:'№942(2): 15,3 → 14,6 → 86,14; отдельно 1,58; итог 87,72.'},
{id:'l131-geometry',eyebrow:'Геометрия',title:'Площадь и периметр требуют разных формул',body:'Прямоугольник: S=ab, P=2(a+b). Квадрат: S=a², P=4a. Если одна сторона задана через разность, сначала восстанавливаем её.',note:'№952 объединяет десятичное умножение с геометрией.'},
{id:'l131-meeting',eyebrow:'Встречное движение',title:'Скорости навстречу складываются',body:'При движении навстречу скорость сближения равна сумме скоростей. Расстояние до встречи: s=(v₁+v₂)t.',note:'Домашнее №941 использует именно эту модель.'},
{id:'l131-route',eyebrow:'Маршрут урока',title:'Общий множитель → буквы → порядок действий → геометрия → движение',body:'Разбираем №942(2), 946, 948(1–2), 952 и затем закрепляем те же навыки в дополнительных задачах.',note:'20 задач · 50 проверяемых ответов.'}
];
const practiceStages:Stage[]=lessonOneHundredThirtyOnePractice.map((practice,i)=>({id:`l131-practice-${String(i+1).padStart(2,'0')}`,eyebrow:practice.sourceExact?`Учебник · ${practice.source}`:`Обязательная практика · ${i+1} из 20`,title:practice.title,body:practice.prompt,practice}));const stages:Stage[]=[...concepts,...practiceStages,{id:'l131-summary',eyebrow:'Итог урока 131',title:'Шестой урок § 34 завершён',body:'Ты умеешь использовать общий множитель, упрощать буквенные выражения, считать площади и периметры и решать встречное движение.',note:'Домашнее задание: § 34, № 941, 947, 949 (1–2). Урок 132 завершит § 34.',summary:true}];
export const lessonOneHundredThirtyOneStageCount=stages.length;export const lessonOneHundredThirtyOnePracticeTaskCount=lessonOneHundredThirtyOnePractice.length;export const lessonOneHundredThirtyOnePracticeResponseCount=lessonOneHundredThirtyOneResponseCount;
function load():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}};const p=JSON.parse(raw) as Saved;return p?.version===1?p:{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}catch{return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}}
export function DecimalMultiplicationFactorGeometryPlayer(){const initial=useMemo(load,[]);const[stageIndex,setStageIndex]=useState(()=>Math.max(0,Math.min(initial.stageIndex,stages.length-1)));const[responses,setResponses]=useState(initial.responses);const[checked,setChecked]=useState(initial.checked);const[attempts,setAttempts]=useState(initial.attempts);const stage=stages[stageIndex];const task=stage.practice;const key=stage.id;const complete=task?task.fields.every(field=>matchField(responses[`${key}:${field.id}`]??'',field)):true;useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,checked,attempts} satisfies Saved))},[stageIndex,responses,checked,attempts]);useEffect(()=>{const handler=(event:Event)=>{const d=(event as CustomEvent<Jump>).detail;if(d?.lessonNumber!==131||typeof d.stageIndex!=='number')return;setStageIndex(Math.max(0,Math.min(Math.trunc(d.stageIndex),stages.length-1)))};window.addEventListener('mathnikita-go-to-stage',handler);return()=>window.removeEventListener('mathnikita-go-to-stage',handler)},[]);const check=()=>{if(task){setChecked(p=>({...p,[key]:true}));setAttempts(p=>({...p,[key]:(p[key]??0)+1}))}};const move=(delta:number)=>setStageIndex(i=>Math.max(0,Math.min(i+delta,stages.length-1)));return <section className="lesson-player theory-experience" aria-label="Урок 131: общий множитель, площадь и встречное движение"><article className={`interactive-stage ${stage.summary?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p>{stage.note?<p className="theory-note">{stage.note}</p>:null}</div>{task?<div className="activity-area"><h3>{task.prompt}</h3><div className="answer-grid">{task.fields.map(field=>{const responseKey=`${key}:${field.id}`;return <label className="inline-answer" key={field.id}><span>{field.label}</span><input value={responses[responseKey]??''} placeholder={field.placeholder??'Введите ответ'} onChange={event=>{setResponses(p=>({...p,[responseKey]:event.target.value}));setChecked(p=>({...p,[key]:false}))}}/></label>})}</div><button className="check-button" type="button" onClick={check}>Проверить</button>{checked[key]?complete?<div className="instant-feedback good" data-explanation={task.explanation}><b>Верно.</b><span>{task.explanation}</span></div>:<div className="instant-feedback bad" data-explanation={task.explanation}><b>Пока есть ошибка.</b><span>{attempts[key]>1?task.hint:'Проверь структуру выражения, порядок действий и положение запятой.'}</span></div>:null}</div>:null}{stage.summary?<div className="summary-card"><b>Урок 131 завершён</b><span>20 задач · 50 ответов · § 34 · 6 из 7</span></div>:null}</article><nav className="lesson-controls" aria-label="Навигация по этапам урока 131"><button type="button" onClick={()=>move(-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {stages.length}</span><button type="button" onClick={()=>move(1)} disabled={stageIndex===stages.length-1}>Дальше →</button></nav></section>}
