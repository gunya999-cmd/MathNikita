import {useEffect,useMemo,useState} from 'react';import './lessonPlayer.css';import './theoryExperience.css';import {lessonOneHundredFiftyOnePractice,lessonOneHundredFiftyOneResponseCount,type LessonOneHundredFiftyOneField,type LessonOneHundredFiftyOneTask} from './data/lessonOneHundredFiftyOnePractice';
type Stage={id:string;eyebrow:string;title:string;body:string;note?:string;practice?:LessonOneHundredFiftyOneTask;summary?:boolean};type Saved={version:1;stageIndex:number;responses:Record<string,string>;checked:Record<string,boolean>;attempts:Record<string,number>};type Jump={lessonNumber?:number;stageIndex?:number};const KEY='mathnikita-lesson-151-progress-v1';const norm=(v:string)=>v.normalize('NFKC').trim().toLocaleLowerCase('ru-RU').replace(/\s+/g,'').replace(/,/g,'.');const canonicalDecimal=(v:string)=>{let s=norm(v);if(!/^[+-]?(?:\d+(?:\.\d*)?|\.\d+)$/.test(s))return null;let sign='';if(s.startsWith('+')||s.startsWith('-')){sign=s.startsWith('-')?'-':'';s=s.slice(1)}let[whole,frac='']=s.split('.');whole=(whole||'0').replace(/^0+(?=\d)/,'');frac=frac.replace(/0+$/,'');if(whole==='0'&&!frac)sign='';return `${sign}${whole}${frac?`.${frac}`:''}`};const matchField=(value:string,field:LessonOneHundredFiftyOneField)=>{const v=norm(value);if(!v)return false;const cv=canonicalDecimal(v);return field.answers.some(answer=>{const a=norm(answer);if(v===a)return true;const ca=canonicalDecimal(a);return cv!==null&&ca!==null&&cv===ca})};
const concepts:Stage[]=[
{id:'l151-application',eyebrow:'Урок 151 · § 38 · 2 из 4',title:'Правило известно — теперь распознавай модель в тексте',body:'В прикладной задаче редко написано «p% числа равны b». Нужно самому определить, какая величина является процентной частью, а какая соответствует 100%.',note:'Сначала подпиши словами: часть = p%, целое = 100%.'},
{id:'l151-solution',eyebrow:'Растворы и смеси',title:'Масса вещества — процент от массы всей смеси',body:'Если раствор содержит 14% соли и соли 49 кг, то 49 кг соответствуют 14%, а масса всего раствора — 100%. Поэтому 49:0,14=350 кг.',note:'После нахождения целого можно найти массу остальных компонентов вычитанием.'},
{id:'l151-yield',eyebrow:'Сушка и выход продукта',title:'Готовый продукт может быть лишь частью исходной массы',body:'Если масса сушеных слив составляет 15% массы свежих, то известные 36 кг — это 15% исходной массы. Деление на 0,15 возвращает свежую массу.',note:'Полезная проверка: исходная масса должна быть больше массы после сушки.'},
{id:'l151-over100',eyebrow:'Больше 100%',title:'При 120% восстановленные 100% меньше известного результата',body:'Если фактическая величина равна 120% плана, то она в 1,2 раза больше плана. Чтобы вернуть 100%, дели фактическую величину на 1,2.',note:'№1102: 28,8:1,2=24 кг.'},
{id:'l151-complement',eyebrow:'После восстановления целого',title:'Остаток удобно находить через дополнение до 100%',body:'Если известная часть — 35%, то после нахождения целого оставшаяся часть равна 65%. Можно найти её процентом от целого или вычитанием известной части.',note:'Два способа должны дать одинаковый результат.'},
{id:'l151-check',eyebrow:'Обратная проверка',title:'Верни найденные 100% обратно к исходной части',body:'Умножь найденное целое на p/100. Результат обязан совпасть с величиной из условия. Это особенно важно для дробных процентов и процентов больше 100%.',note:'350·0,14=49; 240·0,15=36; 24·1,2=28,8.'},
{id:'l151-source',eyebrow:'Учебник · №1098, №1100, №1102',title:'Три прикладные модели §38',body:'№1098 — концентрация раствора; №1100 — выход продукта после сушки; №1102 — восстановление плана из результата 120%.',note:'20 задач · 50 проверяемых ответов. Урок 152 продолжит §38 более составными задачами.'}
];
const practiceStages:Stage[]=lessonOneHundredFiftyOnePractice.map((practice,i)=>({id:`l151-practice-${String(i+1).padStart(2,'0')}`,eyebrow:practice.sourceExact?`Учебник · ${practice.source}`:`Обязательная практика · ${i+1} из 20`,title:practice.title,body:practice.prompt,practice}));const stages:Stage[]=[...concepts,...practiceStages,{id:'l151-summary',eyebrow:'Итог урока 151',title:'Обратные процентные задачи применяются в разных сюжетах',body:'Ты умеешь распознавать неизвестные 100% в задачах на смеси, сушку, скидки, планы и перевыполнение, работать с дробными процентами и процентами больше 100%, а затем проверять ответ прямым вычислением.',note:'Далее — урок 152: составные задачи §38.',summary:true}];
export const lessonOneHundredFiftyOneStageCount=stages.length;export const lessonOneHundredFiftyOnePracticeTaskCount=lessonOneHundredFiftyOnePractice.length;export const lessonOneHundredFiftyOnePracticeResponseCount=lessonOneHundredFiftyOneResponseCount;
function load():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}};const p=JSON.parse(raw) as Saved;return p?.version===1?p:{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}catch{return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}}
export function InversePercentApplicationsPlayer(){const initial=useMemo(load,[]);const[stageIndex,setStageIndex]=useState(()=>Math.max(0,Math.min(initial.stageIndex,stages.length-1)));const[responses,setResponses]=useState(initial.responses);const[checked,setChecked]=useState(initial.checked);const[attempts,setAttempts]=useState(initial.attempts);const stage=stages[stageIndex];const task=stage.practice;const key=stage.id;const complete=task?task.fields.every(field=>matchField(responses[`${key}:${field.id}`]??'',field)):true;useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,checked,attempts} satisfies Saved))},[stageIndex,responses,checked,attempts]);useEffect(()=>{const handler=(event:Event)=>{const d=(event as CustomEvent<Jump>).detail;if(d?.lessonNumber!==151||typeof d.stageIndex!=='number')return;setStageIndex(Math.max(0,Math.min(Math.trunc(d.stageIndex),stages.length-1)))};window.addEventListener('mathnikita-go-to-stage',handler);return()=>window.removeEventListener('mathnikita-go-to-stage',handler)},[]);const check=()=>{if(task){setChecked(p=>({...p,[key]:true}));setAttempts(p=>({...p,[key]:(p[key]??0)+1}))}};const move=(delta:number)=>setStageIndex(i=>Math.max(0,Math.min(i+delta,stages.length-1)));return <section className="lesson-player theory-experience" aria-label="Урок 151: прикладные задачи на нахождение числа по процентам"><article className={`interactive-stage ${stage.summary?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p>{stage.note?<p className="theory-note">{stage.note}</p>:null}</div>{task?<div className="activity-area"><h3>{task.prompt}</h3><div className="answer-grid">{task.fields.map(field=>{const responseKey=`${key}:${field.id}`;return <label className="inline-answer" key={field.id}><span>{field.label}</span><input value={responses[responseKey]??''} placeholder={field.placeholder??'Введите ответ'} onChange={event=>{setResponses(p=>({...p,[responseKey]:event.target.value}));setChecked(p=>({...p,[key]:false}))}}/></label>})}</div><button className="check-button" type="button" onClick={check}>Проверить</button>{checked[key]?complete?<div className="instant-feedback good" data-explanation={task.explanation}><b>Верно.</b><span>{task.explanation}</span></div>:<div className="instant-feedback bad" data-explanation={task.explanation}><b>Пока есть ошибка.</b><span>{attempts[key]>1?task.hint:'Определи, какая величина соответствует p%, а какая должна быть восстановлена как 100%.'}</span></div>:null}</div>:null}{stage.summary?<div className="summary-card"><b>Урок 151 завершён</b><span>20 задач · 50 ответов · § 38 · 2 из 4</span></div>:null}</article><nav className="lesson-controls" aria-label="Навигация по этапам урока 151"><button type="button" onClick={()=>move(-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {stages.length}</span><button type="button" onClick={()=>move(1)} disabled={stageIndex===stages.length-1}>Дальше →</button></nav></section>}
