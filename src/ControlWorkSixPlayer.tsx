import {useEffect,useMemo,useState} from 'react';
import {loadLessonTiming} from './lessonTiming';
import './lessonPlayer.css';
import './controlWork.css';
import {lessonOneHundredEightFields,lessonOneHundredEightProgressKey,lessonOneHundredEightStages,type ControlField108} from './data/lessonOneHundredEightControl';

type Saved={version:1;stageIndex:number;responses:Record<string,string>;submitted:boolean;completedAt?:string;submittedResponses?:Record<string,string>;correctionFieldIds?:string[];correctionCompletedAt?:string};
type StageJumpDetail={lessonNumber?:number;stageIndex?:number};
const COMPLETION_KEY='mathnikita:lesson-complete:108';
const fieldById=new Map(lessonOneHundredEightFields.map(field=>[field.id,field]));
function normalize(value:string){return value.trim().toLowerCase().replace(/\s+/g,'').replace(/[−–—]/g,'-').replace(/,/g,'.')}
function normalizeSet(value:string){return value.match(/\d+/g)?.map(Number).sort((a,b)=>a-b).join(',')??''}
function correct(field:ControlField108,response:string){if(field.mode==='number-set')return normalizeSet(response)===normalizeSet(field.answer);return[field.answer,...(field.accepted??[])].some(answer=>normalize(response)===normalize(answer))}
function loadSaved():Saved{try{const raw=localStorage.getItem(lessonOneHundredEightProgressKey);if(!raw)return{version:1,stageIndex:0,responses:{},submitted:false};const parsed=JSON.parse(raw) as Saved;if(parsed?.version===1)return{version:1,stageIndex:Math.max(0,Math.min(parsed.stageIndex,lessonOneHundredEightStages.length-1)),responses:parsed.responses??{},submitted:Boolean(parsed.submitted),completedAt:parsed.completedAt,submittedResponses:parsed.submittedResponses,correctionFieldIds:parsed.correctionFieldIds??[],correctionCompletedAt:parsed.correctionCompletedAt}}catch{}return{version:1,stageIndex:0,responses:{},submitted:false}}

export function ControlWorkSixPlayer(){
  const initial=useMemo(loadSaved,[]);const[stageIndex,setStageIndex]=useState(initial.stageIndex);const[responses,setResponses]=useState<Record<string,string>>(initial.responses);const[submitted,setSubmitted]=useState(initial.submitted);const[completedAt,setCompletedAt]=useState<string|undefined>(initial.completedAt);const[submittedResponses,setSubmittedResponses]=useState<Record<string,string>|undefined>(initial.submittedResponses);const[correctionFieldIds,setCorrectionFieldIds]=useState<string[]>(initial.correctionFieldIds??[]);const[correctionCompletedAt,setCorrectionCompletedAt]=useState<string|undefined>(initial.correctionCompletedAt);const stage=lessonOneHundredEightStages[stageIndex];const answeredCount=lessonOneHundredEightFields.filter(field=>Boolean(responses[field.id]?.trim())).length;const baselineResponses=submittedResponses??(submitted?responses:{});const score=lessonOneHundredEightFields.filter(field=>correct(field,baselineResponses[field.id]??'')).length;const grade=score>=14?'5':score>=11?'4':score>=8?'3':'нужно повторить';const wrongFieldIds=submitted?lessonOneHundredEightFields.filter(field=>!correct(field,baselineResponses[field.id]??'')).map(field=>field.id):[];const correctionMode=submitted&&correctionFieldIds.length>0&&!correctionCompletedAt;const correctionCorrectCount=correctionFieldIds.filter(id=>{const field=fieldById.get(id);return Boolean(field&&correct(field,responses[id]??''))}).length;const correctionStageIndexes=lessonOneHundredEightStages.map((item,index)=>({item,index})).filter(({item})=>item.kind==='task'&&item.fieldIds?.some(id=>correctionFieldIds.includes(id))).map(({index})=>index);const correctionPosition=correctionStageIndexes.indexOf(stageIndex);const stageFields=(stage.fieldIds??[]).map(id=>fieldById.get(id)).filter(Boolean) as ControlField108[];const stageComplete=correctionMode?stageFields.filter(field=>correctionFieldIds.includes(field.id)).every(field=>correct(field,responses[field.id]??'')):stageFields.every(field=>Boolean(responses[field.id]?.trim()));
  useEffect(()=>{localStorage.setItem(lessonOneHundredEightProgressKey,JSON.stringify({version:1,stageIndex,responses,submitted,completedAt,submittedResponses,correctionFieldIds,correctionCompletedAt} satisfies Saved))},[stageIndex,responses,submitted,completedAt,submittedResponses,correctionFieldIds,correctionCompletedAt]);
  useEffect(()=>{const go=(event:Event)=>{const detail=(event as CustomEvent<StageJumpDetail>).detail;if(detail?.lessonNumber!==108||typeof detail.stageIndex!=='number')return;moveTo(detail.stageIndex)};window.addEventListener('mathnikita-go-to-stage',go);return()=>window.removeEventListener('mathnikita-go-to-stage',go)},[]);
  function stopNarration(){window.dispatchEvent(new CustomEvent('mathnikita-stop-narration'));if('speechSynthesis'in window)window.speechSynthesis.cancel()}
  function moveTo(index:number){stopNarration();setStageIndex(Math.max(0,Math.min(index,lessonOneHundredEightStages.length-1)));window.scrollTo({top:0,behavior:'smooth'})}
  function canEdit(id:string){return!submitted||(correctionMode&&correctionFieldIds.includes(id))}
  function setResponse(id:string,value:string){if(canEdit(id))setResponses(previous=>({...previous,[id]:value}))}
  function submit(){if(answeredCount!==lessonOneHundredEightFields.length||submitted)return;const now=new Date().toISOString();const snapshot={...responses};const activeSeconds=Math.round(loadLessonTiming(108).activeSeconds);setSubmittedResponses(snapshot);setSubmitted(true);setCompletedAt(now);setCorrectionFieldIds([]);setCorrectionCompletedAt(undefined);moveTo(lessonOneHundredEightStages.length-1);localStorage.setItem(COMPLETION_KEY,JSON.stringify({completedAt:now,activeSeconds}));window.dispatchEvent(new CustomEvent('mathnikita-lesson-completed',{detail:{lessonNumber:108,completedAt:now,activeSeconds}}))}
  function startCorrection(){if(!wrongFieldIds.length)return;const indexes=lessonOneHundredEightStages.map((item,index)=>({item,index})).filter(({item})=>item.kind==='task'&&item.fieldIds?.some(id=>wrongFieldIds.includes(id))).map(({index})=>index);setCorrectionFieldIds(wrongFieldIds);setCorrectionCompletedAt(undefined);moveTo(indexes[0]??lessonOneHundredEightStages.length-1)}
  function finishCorrection(){if(!correctionFieldIds.length||correctionCorrectCount!==correctionFieldIds.length)return;setCorrectionCompletedAt(new Date().toISOString());moveTo(lessonOneHundredEightStages.length-1)}
  function reset(){stopNarration();localStorage.removeItem(lessonOneHundredEightProgressKey);localStorage.removeItem(COMPLETION_KEY);setResponses({});setSubmittedResponses(undefined);setSubmitted(false);setCompletedAt(undefined);setCorrectionFieldIds([]);setCorrectionCompletedAt(undefined);setStageIndex(0)}
  const visibleFields=correctionMode?stageFields.filter(field=>correctionFieldIds.includes(field.id)):stageFields;
  return <main className="control-work-page control-six-page" data-control-work="6" data-lesson-number="108" data-source-reference="Мерзляк · дидактические материалы · контрольная работа № 6 · вариант 1" data-control-field-count={lessonOneHundredEightFields.length}>
    <section className="lesson-player-shell"><div className="control-work-status"><div><span>Контрольная работа № 6</span><b>{submitted?`Первичный балл ${score}/${lessonOneHundredEightFields.length}`:`Заполнено ${answeredCount}/${lessonOneHundredEightFields.length}`}</b></div><div className="control-progress"><i style={{width:`${((stageIndex+1)/lessonOneHundredEightStages.length)*100}%`}}/></div></div>
      <article className={`interactive-stage control-stage ${stage.kind==='summary'?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p></div>
        {stage.kind==='intro'?<><div className="control-source-badge"><b>Точный источник</b><span>А. Г. Мерзляк, В. Б. Полонский, Е. М. Рабинович, М. С. Якир. Дидактические материалы, 5 класс: контрольная работа № 6 «Обыкновенные дроби», вариант 1.</span></div><div className="control-rules"><b>Во время первичной работы</b><ul><li>ответы и объяснения скрыты до сдачи;</li><li>можно возвращаться к заданиям и менять ответы;</li><li>после сдачи первичный балл фиксируется.</li></ul></div></>:null}
        {stage.kind==='task'?<div className="activity-area"><div className="answer-grid">{visibleFields.map(field=><label className="inline-answer" key={field.id}><span>{field.number}. {field.label}</span><input value={responses[field.id]??''} disabled={!canEdit(field.id)} placeholder={field.placeholder??'Введите ответ'} onChange={event=>setResponse(field.id,event.target.value)}/>{submitted&&!correctionMode?<small className={correct(field,baselineResponses[field.id]??'')?'control-correct':'control-wrong'}>{correct(field,baselineResponses[field.id]??'')?'Верно':`Ответ: ${field.answer}`}</small>:null}{submitted&&!correctionMode?<p className="control-explanation">{field.explanation}</p>:null}</label>)}</div>{correctionMode?<div className="control-correction-status">Исправлено {correctionCorrectCount}/{correctionFieldIds.length}</div>:null}</div>:null}
        {stage.kind==='submit'?<div className="control-submit-card"><b>Заполнено {answeredCount} из {lessonOneHundredEightFields.length}</b><button type="button" disabled={answeredCount!==lessonOneHundredEightFields.length||submitted} onClick={submit}>Сдать работу</button></div>:null}
        {stage.kind==='summary'&&submitted?<div className="summary-card"><b>Первичный результат: {score}/{lessonOneHundredEightFields.length} · оценка {grade}</b><span>{wrongFieldIds.length?`Ошибок: ${wrongFieldIds.length}. Первичный балл уже сохранён.`:'Все ответы верны.'}</span>{wrongFieldIds.length&&!correctionMode&&!correctionCompletedAt?<button type="button" onClick={startCorrection}>Исправить ошибки</button>:null}{correctionCompletedAt?<span>Коррекция завершена.</span>:null}<button type="button" onClick={reset}>Пройти заново</button></div>:null}
      </article>
      <nav className="lesson-controls" aria-label="Навигация по контрольной работе № 6"><button type="button" onClick={()=>moveTo(stageIndex-1)} disabled={stageIndex===0||correctionMode&&correctionPosition===0}>← Назад</button><span>Этап {stageIndex+1} из {lessonOneHundredEightStages.length}</span>{correctionMode?<button type="button" onClick={()=>{if(correctionPosition===correctionStageIndexes.length-1)finishCorrection();else moveTo(correctionStageIndexes[correctionPosition+1])}} disabled={!stageComplete}>{correctionPosition===correctionStageIndexes.length-1?'Завершить коррекцию':'Следующая ошибка →'}</button>:<button type="button" onClick={()=>moveTo(stageIndex+1)} disabled={stageIndex===lessonOneHundredEightStages.length-1||stage.kind==='task'&&!stageComplete}>Дальше →</button>}</nav>
    </section>
  </main>
}
