import {useEffect,useMemo,useState} from 'react';import './lessonPlayer.css';import './theoryExperience.css';import {lessonOneHundredThirtyPractice,lessonOneHundredThirtyResponseCount,type LessonOneHundredThirtyField,type LessonOneHundredThirtyTask} from './data/lessonOneHundredThirtyPractice';
type Stage={id:string;eyebrow:string;title:string;body:string;note?:string;practice?:LessonOneHundredThirtyTask;summary?:boolean};type Saved={version:1;stageIndex:number;responses:Record<string,string>;checked:Record<string,boolean>;attempts:Record<string,number>};type Jump={lessonNumber?:number;stageIndex?:number};const KEY='mathnikita-lesson-130-progress-v1';const norm=(v:string)=>v.normalize('NFKC').trim().toLocaleLowerCase('ru-RU').replace(/\s+/g,'').replace(/,/g,'.');const matchField=(value:string,field:LessonOneHundredThirtyField)=>field.answers.some(answer=>norm(value)===norm(answer));
const concepts:Stage[]=[
{id:'l130-model',eyebrow:'Урок 130 · § 34 · 5 из 7',title:'Одна операция — разные модели',body:'Сегодня умножение встречается в выражениях, масштабе и движении. Перед вычислением определяем смысл каждого множителя.',note:'Полезный вопрос: результат должен увеличиться или уменьшиться?'},
{id:'l130-coeff',eyebrow:'Буквенные выражения',title:'Собираем числовой коэффициент',body:'В произведении с буквами числовые множители можно переставлять и группировать, а буквенную часть сохранять.',note:'0,27m×0,3n = 0,081mn.'},
{id:'l130-scale',eyebrow:'Масштаб',title:'10, 100, 1000 меняют положение запятой',body:'Умножение на 10, 100, 1000 сдвигает запятую вправо; умножение на 0,1, 0,01, 0,001 — влево.',note:'№944 проверяет именно понимание масштаба, а не механическое правило.'},
{id:'l130-order',eyebrow:'Порядок действий',title:'Длинное выражение разбиваем на короткие шаги',body:'Сначала скобки, внутри них — умножение до сложения, затем внешнее умножение и вычитание.',note:'№942(1): 3,69 → 18,4 → 69,92 → 20,484.'},
{id:'l130-river',eyebrow:'Движение по реке',title:'Течение прибавляет или вычитает скорость',body:'По течению скорость лодки равна собственной скорости плюс скорость течения. Против течения — минус скорость течения.',note:'№950: 21,3 км/ч по течению и 16,5 км/ч против.'},
{id:'l130-catch',eyebrow:'Догонка',title:'Скорость сближения — разность скоростей',body:'Когда оба движутся в одном направлении, расстояние между ними сокращается на разность скоростей за единицу времени.',note:'№961: 40−25=15 м/мин; 540÷15=36 мин.'},
{id:'l130-route',eyebrow:'Маршрут урока',title:'Коэффициенты → масштаб → выражение → река → догонка',body:'Разбираем №928 (7–9), 942(1), 944, 950, 961, затем закрепляем каждую модель отдельно.',note:'20 задач · 50 проверяемых ответов.'}
];
const practiceStages:Stage[]=lessonOneHundredThirtyPractice.map((practice,i)=>({id:`l130-practice-${String(i+1).padStart(2,'0')}`,eyebrow:practice.sourceExact?`Учебник · ${practice.source}`:`Обязательная практика · ${i+1} из 20`,title:practice.title,body:practice.prompt,practice}));const stages:Stage[]=[...concepts,...practiceStages,{id:'l130-summary',eyebrow:'Итог урока 130',title:'Пятый урок § 34 завершён',body:'Ты умеешь контролировать масштаб произведения, вычислять составные выражения и строить модели движения по реке и догонки.',note:'Домашнее задание: § 34, № 929 (3–4), 943 (1–2), 945. Урок 131 продолжит § 34.',summary:true}];
export const lessonOneHundredThirtyStageCount=stages.length;export const lessonOneHundredThirtyPracticeTaskCount=lessonOneHundredThirtyPractice.length;export const lessonOneHundredThirtyPracticeResponseCount=lessonOneHundredThirtyResponseCount;
function load():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}};const p=JSON.parse(raw) as Saved;return p?.version===1?p:{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}catch{return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}}
export function DecimalMultiplicationScaleMotionPlayer(){const initial=useMemo(load,[]);const[stageIndex,setStageIndex]=useState(()=>Math.max(0,Math.min(initial.stageIndex,stages.length-1)));const[responses,setResponses]=useState(initial.responses);const[checked,setChecked]=useState(initial.checked);const[attempts,setAttempts]=useState(initial.attempts);const stage=stages[stageIndex];const task=stage.practice;const key=stage.id;const complete=task?task.fields.every(field=>matchField(responses[`${key}:${field.id}`]??'',field)):true;useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,checked,attempts} satisfies Saved))},[stageIndex,responses,checked,attempts]);useEffect(()=>{const handler=(event:Event)=>{const d=(event as CustomEvent<Jump>).detail;if(d?.lessonNumber!==130||typeof d.stageIndex!=='number')return;setStageIndex(Math.max(0,Math.min(Math.trunc(d.stageIndex),stages.length-1)))};window.addEventListener('mathnikita-go-to-stage',handler);return()=>window.removeEventListener('mathnikita-go-to-stage',handler)},[]);const check=()=>{if(task){setChecked(p=>({...p,[key]:true}));setAttempts(p=>({...p,[key]:(p[key]??0)+1}))}};const move=(delta:number)=>setStageIndex(i=>Math.max(0,Math.min(i+delta,stages.length-1)));return <section className="lesson-player theory-experience" aria-label="Урок 130: масштаб, течение и догонка"><article className={`interactive-stage ${stage.summary?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p>{stage.note?<p className="theory-note">{stage.note}</p>:null}</div>{task?<div className="activity-area"><h3>{task.prompt}</h3><div className="answer-grid">{task.fields.map(field=>{const responseKey=`${key}:${field.id}`;return <label className="inline-answer" key={field.id}><span>{field.label}</span><input value={responses[responseKey]??''} placeholder={field.placeholder??'Введите ответ'} onChange={event=>{setResponses(p=>({...p,[responseKey]:event.target.value}));setChecked(p=>({...p,[key]:false}))}}/></label>})}</div><button className="check-button" type="button" onClick={check}>Проверить</button>{checked[key]?complete?<div className="instant-feedback good" data-explanation={task.explanation}><b>Верно.</b><span>{task.explanation}</span></div>:<div className="instant-feedback bad" data-explanation={task.explanation}><b>Пока есть ошибка.</b><span>{attempts[key]>1?task.hint:'Проверь модель задачи, порядок действий и положение запятой.'}</span></div>:null}</div>:null}{stage.summary?<div className="summary-card"><b>Урок 130 завершён</b><span>20 задач · 50 ответов · § 34 · 5 из 7</span></div>:null}</article><nav className="lesson-controls" aria-label="Навигация по этапам урока 130"><button type="button" onClick={()=>move(-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {stages.length}</span><button type="button" onClick={()=>move(1)} disabled={stageIndex===stages.length-1}>Дальше →</button></nav></section>}
