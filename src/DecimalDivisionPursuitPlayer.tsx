import {useEffect,useMemo,useState} from 'react';import './lessonPlayer.css';import './theoryExperience.css';import {lessonOneHundredThirtyNinePractice,lessonOneHundredThirtyNineResponseCount,type LessonOneHundredThirtyNineField,type LessonOneHundredThirtyNineTask} from './data/lessonOneHundredThirtyNinePractice';
type Stage={id:string;eyebrow:string;title:string;body:string;note?:string;practice?:LessonOneHundredThirtyNineTask;summary?:boolean};type Saved={version:1;stageIndex:number;responses:Record<string,string>;checked:Record<string,boolean>;attempts:Record<string,number>};type Jump={lessonNumber?:number;stageIndex?:number};const KEY='mathnikita-lesson-139-progress-v1';const norm=(v:string)=>v.normalize('NFKC').trim().toLocaleLowerCase('ru-RU').replace(/\s+/g,'').replace(/,/g,'.');const canonicalDecimal=(v:string)=>{let s=norm(v);if(!/^[+-]?(?:\d+(?:\.\d*)?|\.\d+)$/.test(s))return null;let sign='';if(s.startsWith('+')||s.startsWith('-')){sign=s.startsWith('-')?'-':'';s=s.slice(1)}let[whole,frac='']=s.split('.');whole=(whole||'0').replace(/^0+(?=\d)/,'');frac=frac.replace(/0+$/,'');if(whole==='0'&&!frac)sign='';return `${sign}${whole}${frac?`.${frac}`:''}`};const matchField=(value:string,field:LessonOneHundredThirtyNineField)=>{const v=norm(value);if(!v)return false;const cv=canonicalDecimal(v);return field.answers.some(answer=>{const a=norm(answer);if(v===a)return true;const ca=canonicalDecimal(a);return cv!==null&&ca!==null&&cv===ca})};
const concepts:Stage[]=[
{id:'l139-expression',eyebrow:'Урок 139 · § 35 · 7 из 9',title:'Сложное выражение разбирай на независимые блоки',body:'Когда в числителе и знаменателе есть свои действия, сначала полностью вычисли каждый блок. Только после этого выполняй внешнее деление.',note:'№999(1): 35,94:6=5,99.'},
{id:'l139-equation-product',eyebrow:'Уравнения',title:'Скобка в произведении — один неизвестный множитель',body:'В уравнении (51,32+x)·0,12=72 сначала найди значение всей скобки делением 72:0,12. После этого останется простое уравнение на неизвестное слагаемое.',note:'51,32+x=600 → x=548,68.'},
{id:'l139-equation-divisor',eyebrow:'Неизвестный делитель',title:'Скобка в делителе ищется через делимое и частное',body:'В записи 17,28:(56−x)=36 выражение 56−x является неизвестным делителем. Он равен 17,28:36, а уже затем находится x.',note:'56−x=0,48 → x=55,52.'},
{id:'l139-pursuit',eyebrow:'Догонка',title:'При движении в одном направлении скорости вычитаются',body:'Более быстрый участник сокращает начальный отрыв со скоростью, равной разности скоростей. Время догонки равно начальный отрыв, делённый на скорость сближения.',note:'№1009: 302−298,8=3,2; 30,4:3,2=9,5 мин.'},
{id:'l139-direction',eyebrow:'Модель движения',title:'Встреча и догонка отличаются одним ключевым действием',body:'При встречном движении расстояние уменьшается суммой скоростей. При догонке в одном направлении — их разностью. Перед вычислением всегда подпиши направление.',note:'Навстречу — сумма; догонка — разность.'},
{id:'l139-check',eyebrow:'Самопроверка',title:'Проверяй обратным действием и смыслом задачи',body:'В уравнении подставь найденное x. В догонке умножь разность скоростей на найденное время — должен получиться исходный отрыв. В выражении сверяй промежуточные скобки.',note:'Для №1009: 3,2·9,5=30,4.'},
{id:'l139-route',eyebrow:'Маршрут урока',title:'Выражения → сложные уравнения → догонка → смешанные задачи',body:'Разбираем точные №999(1), №1001(3–4), №1009, затем переносим эти схемы в стоимость, геометрию, доли, уравнения и задачи на движение.',note:'20 задач · 50 проверяемых ответов. Урок 140 завершит тренировочную часть §35 перед контрольной.'}
];
const practiceStages:Stage[]=lessonOneHundredThirtyNinePractice.map((practice,i)=>({id:`l139-practice-${String(i+1).padStart(2,'0')}`,eyebrow:practice.sourceExact?`Учебник · ${practice.source}`:`Обязательная практика · ${i+1} из 20`,title:practice.title,body:practice.prompt,practice}));const stages:Stage[]=[...concepts,...practiceStages,{id:'l139-summary',eyebrow:'Итог урока 139',title:'Сложные уравнения и догонка сведены к понятным шагам',body:'Ты умеешь разбирать выражения на блоки, находить скобку как неизвестный компонент действия и решать задачи на догонку через разность скоростей.',note:'Следующий урок — заключительное закрепление §35 перед контрольной работой.',summary:true}];
export const lessonOneHundredThirtyNineStageCount=stages.length;export const lessonOneHundredThirtyNinePracticeTaskCount=lessonOneHundredThirtyNinePractice.length;export const lessonOneHundredThirtyNinePracticeResponseCount=lessonOneHundredThirtyNineResponseCount;
function load():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}};const p=JSON.parse(raw) as Saved;return p?.version===1?p:{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}catch{return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}}
export function DecimalDivisionPursuitPlayer(){const initial=useMemo(load,[]);const[stageIndex,setStageIndex]=useState(()=>Math.max(0,Math.min(initial.stageIndex,stages.length-1)));const[responses,setResponses]=useState(initial.responses);const[checked,setChecked]=useState(initial.checked);const[attempts,setAttempts]=useState(initial.attempts);const stage=stages[stageIndex];const task=stage.practice;const key=stage.id;const complete=task?task.fields.every(field=>matchField(responses[`${key}:${field.id}`]??'',field)):true;useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,checked,attempts} satisfies Saved))},[stageIndex,responses,checked,attempts]);useEffect(()=>{const handler=(event:Event)=>{const d=(event as CustomEvent<Jump>).detail;if(d?.lessonNumber!==139||typeof d.stageIndex!=='number')return;setStageIndex(Math.max(0,Math.min(Math.trunc(d.stageIndex),stages.length-1)))};window.addEventListener('mathnikita-go-to-stage',handler);return()=>window.removeEventListener('mathnikita-go-to-stage',handler)},[]);const check=()=>{if(task){setChecked(p=>({...p,[key]:true}));setAttempts(p=>({...p,[key]:(p[key]??0)+1}))}};const move=(delta:number)=>setStageIndex(i=>Math.max(0,Math.min(i+delta,stages.length-1)));return <section className="lesson-player theory-experience" aria-label="Урок 139: сложные уравнения и догонка"><article className={`interactive-stage ${stage.summary?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p>{stage.note?<p className="theory-note">{stage.note}</p>:null}</div>{task?<div className="activity-area"><h3>{task.prompt}</h3><div className="answer-grid">{task.fields.map(field=>{const responseKey=`${key}:${field.id}`;return <label className="inline-answer" key={field.id}><span>{field.label}</span><input value={responses[responseKey]??''} placeholder={field.placeholder??'Введите ответ'} onChange={event=>{setResponses(p=>({...p,[responseKey]:event.target.value}));setChecked(p=>({...p,[key]:false}))}}/></label>})}</div><button className="check-button" type="button" onClick={check}>Проверить</button>{checked[key]?complete?<div className="instant-feedback good" data-explanation={task.explanation}><b>Верно.</b><span>{task.explanation}</span></div>:<div className="instant-feedback bad" data-explanation={task.explanation}><b>Пока есть ошибка.</b><span>{attempts[key]>1?task.hint:'Проверь скобки, неизвестный компонент и направление движения.'}</span></div>:null}</div>:null}{stage.summary?<div className="summary-card"><b>Урок 139 завершён</b><span>20 задач · 50 ответов · § 35 · 7 из 9</span></div>:null}</article><nav className="lesson-controls" aria-label="Навигация по этапам урока 139"><button type="button" onClick={()=>move(-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {stages.length}</span><button type="button" onClick={()=>move(1)} disabled={stageIndex===stages.length-1}>Дальше →</button></nav></section>}
