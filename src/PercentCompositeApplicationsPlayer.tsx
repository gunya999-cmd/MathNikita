import {useEffect,useMemo,useState} from 'react';import './lessonPlayer.css';import './theoryExperience.css';import {lessonOneHundredFortyEightPractice,lessonOneHundredFortyEightResponseCount,type LessonOneHundredFortyEightField,type LessonOneHundredFortyEightTask} from './data/lessonOneHundredFortyEightPractice';
type Stage={id:string;eyebrow:string;title:string;body:string;note?:string;practice?:LessonOneHundredFortyEightTask;summary?:boolean};type Saved={version:1;stageIndex:number;responses:Record<string,string>;checked:Record<string,boolean>;attempts:Record<string,number>};type Jump={lessonNumber?:number;stageIndex?:number};const KEY='mathnikita-lesson-148-progress-v1';const norm=(v:string)=>v.normalize('NFKC').trim().toLocaleLowerCase('ru-RU').replace(/\s+/g,'').replace(/,/g,'.');const canonicalDecimal=(v:string)=>{let s=norm(v);if(!/^[+-]?(?:\d+(?:\.\d*)?|\.\d+)$/.test(s))return null;let sign='';if(s.startsWith('+')||s.startsWith('-')){sign=s.startsWith('-')?'-':'';s=s.slice(1)}let[whole,frac='']=s.split('.');whole=(whole||'0').replace(/^0+(?=\d)/,'');frac=frac.replace(/0+$/,'');if(whole==='0'&&!frac)sign='';return `${sign}${whole}${frac?`.${frac}`:''}`};const matchField=(value:string,field:LessonOneHundredFortyEightField)=>{const v=norm(value);if(!v)return false;const cv=canonicalDecimal(v);return field.answers.some(answer=>{const a=norm(answer);if(v===a)return true;const ca=canonicalDecimal(a);return cv!==null&&ca!==null&&cv===ca})};
const concepts:Stage[]=[
{id:'l148-complement',eyebrow:'Урок 148 · § 37 · 3 из 4',title:'Оставшаяся часть дополняет известные проценты до 100%',body:'Если известная часть равна p%, то остаток равен 100−p%. Если частей несколько, сначала сложи их проценты, а затем вычти сумму из 100%.',note:'Например, 24% + 32% = 56%, значит остаётся 44%.'},
{id:'l148-two-step',eyebrow:'Два шага',title:'Сначала найди нужный процент, потом переводи его в число',body:'Во многих задачах число для остатка не дано напрямую. Сначала вычисли долю остатка в процентах, затем умножь целое на соответствующую десятичную дробь.',note:'Для №1072: серебро 55%, затем 80·0,55=44.'},
{id:'l148-multiple-parts',eyebrow:'Несколько категорий',title:'Следи, чтобы все части вместе давали 100%',body:'Если целое разделено на несколько категорий, удобно одновременно контролировать проценты и реальные количества. Сумма всех процентных частей должна быть 100%.',note:'Так легко проверить задачу про клубничное, малиновое и вишнёвое варенье.'},
{id:'l148-over-hundred',eyebrow:'Процент больше 100%',title:'101,6% означает увеличение, а не ошибку',body:'100% — исходная величина. 101,6% — это исходная величина плюс 1,6%. Для вычисления используется множитель 1,016.',note:'Если процент больше 100%, результат должен быть больше исходного значения.'},
{id:'l148-change',eyebrow:'Увеличение и уменьшение',title:'Сравнивай новую величину с исходной',body:'При увеличении на p% новое значение составляет 100+p процентов исходного. При уменьшении на p% остаётся 100−p процентов.',note:'Например, рост на 12% даёт 112%, а скидка 18% оставляет 82%.'},
{id:'l148-source',eyebrow:'Учебник · №1072, №1074, №1076',title:'Три типовых модели §37',body:'№1072 — остаток после известной процентной части; №1074 — несколько частей и остаток; №1076 — величина больше 100% исходной.',note:'Ответы: 44 пуда; 88 банок; 254 штуки.'},
{id:'l148-route',eyebrow:'Маршрут урока',title:'Закрепляем прямые задачи на проценты',body:'На практике будут остатки, несколько категорий, скидки, увеличение, дробные проценты и проценты больше 100%. Мы не ищем неизвестное целое по его проценту — это тема следующего §38.',note:'20 задач · 50 проверяемых ответов. Урок 149 завершит §37.'}
];
const practiceStages:Stage[]=lessonOneHundredFortyEightPractice.map((practice,i)=>({id:`l148-practice-${String(i+1).padStart(2,'0')}`,eyebrow:practice.sourceExact?`Учебник · ${practice.source}`:`Обязательная практика · ${i+1} из 20`,title:practice.title,body:practice.prompt,practice}));const stages:Stage[]=[...concepts,...practiceStages,{id:'l148-summary',eyebrow:'Итог урока 148',title:'Составные процентные задачи стали управляемыми',body:'Ты умеешь находить остаток до 100%, работать с несколькими процентными частями, рассчитывать увеличение и уменьшение и понимать проценты больше 100%.',note:'Далее — урок 149: итоговое закрепление §37 перед переходом к нахождению числа по его процентам.',summary:true}];
export const lessonOneHundredFortyEightStageCount=stages.length;export const lessonOneHundredFortyEightPracticeTaskCount=lessonOneHundredFortyEightPractice.length;export const lessonOneHundredFortyEightPracticeResponseCount=lessonOneHundredFortyEightResponseCount;
function load():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}};const p=JSON.parse(raw) as Saved;return p?.version===1?p:{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}catch{return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}}
export function PercentCompositeApplicationsPlayer(){const initial=useMemo(load,[]);const[stageIndex,setStageIndex]=useState(()=>Math.max(0,Math.min(initial.stageIndex,stages.length-1)));const[responses,setResponses]=useState(initial.responses);const[checked,setChecked]=useState(initial.checked);const[attempts,setAttempts]=useState(initial.attempts);const stage=stages[stageIndex];const task=stage.practice;const key=stage.id;const complete=task?task.fields.every(field=>matchField(responses[`${key}:${field.id}`]??'',field)):true;useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,checked,attempts} satisfies Saved))},[stageIndex,responses,checked,attempts]);useEffect(()=>{const handler=(event:Event)=>{const d=(event as CustomEvent<Jump>).detail;if(d?.lessonNumber!==148||typeof d.stageIndex!=='number')return;setStageIndex(Math.max(0,Math.min(Math.trunc(d.stageIndex),stages.length-1)))};window.addEventListener('mathnikita-go-to-stage',handler);return()=>window.removeEventListener('mathnikita-go-to-stage',handler)},[]);const check=()=>{if(task){setChecked(p=>({...p,[key]:true}));setAttempts(p=>({...p,[key]:(p[key]??0)+1}))}};const move=(delta:number)=>setStageIndex(i=>Math.max(0,Math.min(i+delta,stages.length-1)));return <section className="lesson-player theory-experience" aria-label="Урок 148: составные задачи на проценты"><article className={`interactive-stage ${stage.summary?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p>{stage.note?<p className="theory-note">{stage.note}</p>:null}</div>{task?<div className="activity-area"><h3>{task.prompt}</h3><div className="answer-grid">{task.fields.map(field=>{const responseKey=`${key}:${field.id}`;return <label className="inline-answer" key={field.id}><span>{field.label}</span><input value={responses[responseKey]??''} placeholder={field.placeholder??'Введите ответ'} onChange={event=>{setResponses(p=>({...p,[responseKey]:event.target.value}));setChecked(p=>({...p,[key]:false}))}}/></label>})}</div><button className="check-button" type="button" onClick={check}>Проверить</button>{checked[key]?complete?<div className="instant-feedback good" data-explanation={task.explanation}><b>Верно.</b><span>{task.explanation}</span></div>:<div className="instant-feedback bad" data-explanation={task.explanation}><b>Пока есть ошибка.</b><span>{attempts[key]>1?task.hint:'Проверь, какую часть составляет остаток, и только потом переводи процент в число.'}</span></div>:null}</div>:null}{stage.summary?<div className="summary-card"><b>Урок 148 завершён</b><span>20 задач · 50 ответов · § 37 · 3 из 4</span></div>:null}</article><nav className="lesson-controls" aria-label="Навигация по этапам урока 148"><button type="button" onClick={()=>move(-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {stages.length}</span><button type="button" onClick={()=>move(1)} disabled={stageIndex===stages.length-1}>Дальше →</button></nav></section>}
