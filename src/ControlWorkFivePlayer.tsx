import {useEffect,useMemo,useState} from 'react';
import {loadLessonTiming} from './lessonTiming';
import './lessonPlayer.css';
import './controlWork.css';

type ControlField={id:string;number:string;label:string;answer:string;accepted?:string[];placeholder?:string;explanation:string;mode?:'number-set'};
export type ControlWorkFiveStage={id:string;title:string;eyebrow:string;kind:'intro'|'task'|'submit'|'summary';body:string;fieldIds?:string[]};
type Saved={version:1;stageIndex:number;responses:Record<string,string>;submitted:boolean;completedAt?:string;submittedResponses?:Record<string,string>;correctionFieldIds?:string[];correctionCompletedAt?:string};
type StageJumpDetail={lessonNumber?:number;stageIndex?:number};

const KEY='mathnikita-lesson-90-control-v1';
const COMPLETION_KEY='mathnikita:lesson-complete:90';

const fields:ControlField[]=[
  {id:'l90-1q',number:'1.1',label:'478 : 15 — неполное частное',answer:'31',placeholder:'Частное',explanation:'15 · 31 = 465, а 478 − 465 = 13.'},
  {id:'l90-1r',number:'1.2',label:'478 : 15 — остаток',answer:'13',placeholder:'Остаток',explanation:'478 = 15 · 31 + 13, причём 13 < 15.'},
  {id:'l90-2',number:'2',label:'Площадь прямоугольника, см²',answer:'588',accepted:['588 см²','588 см2'],placeholder:'Площадь',explanation:'Вторая сторона 14 · 3 = 42 см. S = 14 · 42 = 588 см².'},
  {id:'l90-3v',number:'3.1',label:'Объём куба с ребром 3 см, см³',answer:'27',accepted:['27 см³','27 см3'],placeholder:'Объём',explanation:'V = 3³ = 27 см³.'},
  {id:'l90-3s',number:'3.2',label:'Площадь поверхности куба, см²',answer:'54',accepted:['54 см²','54 см2'],placeholder:'Площадь поверхности',explanation:'S = 6 · 3² = 54 см².'},
  {id:'l90-4',number:'4',label:'Объём прямоугольного параллелепипеда, см³',answer:'3240',accepted:['3 240','3240 см³','3240 см3'],placeholder:'Объём',explanation:'Ширина 18 : 2 = 9 см, высота 9 + 11 = 20 см. V = 18 · 9 · 20 = 3240 см³.'},
  {id:'l90-5',number:'5',label:'Делимое',answer:'83',placeholder:'Делимое',explanation:'Делимое = делитель · неполное частное + остаток = 11 · 7 + 6 = 83.'},
  {id:'l90-6',number:'6',label:'Периметр поля, м',answer:'1100',accepted:['1 100','1100 м'],placeholder:'Периметр',explanation:'6 га = 60 000 м². Вторая сторона 60 000 : 150 = 400 м. P = 2 · (150 + 400) = 1100 м.'},
  {id:'l90-7',number:'7',label:'Все трёхзначные числа из цифр 5, 6 и 0 без повторений',answer:'506, 560, 605, 650',placeholder:'Например: 123, 132, ...',explanation:'Ноль не может стоять первым. Допустимые числа: 506, 560, 605, 650.',mode:'number-set'},
  {id:'l90-8',number:'8',label:'Третье измерение параллелепипеда, см',answer:'6',accepted:['6 см'],placeholder:'Длина',explanation:'4 · (a+b+c)=116, значит a+b+c=29. Третье измерение: 29 − 12 − 11 = 6 см.'},
];

const fieldById=new Map(fields.map(field=>[field.id,field]));

export const lessonNinetyStages:ControlWorkFiveStage[]=[
  {id:'l90-rules',kind:'intro',eyebrow:'Контрольная работа № 5 · вариант 1',title:'Самостоятельная контрольная работа',body:'8 заданий · 10 оцениваемых ответов. Правильные ответы и разбор появятся только после сдачи всей работы.'},
  {id:'l90-task1',kind:'task',eyebrow:'Задание 1 из 8',title:'Деление с остатком',body:'Выполните деление с остатком: 478 : 15.',fieldIds:['l90-1q','l90-1r']},
  {id:'l90-task2',kind:'task',eyebrow:'Задание 2 из 8',title:'Площадь прямоугольника',body:'Найдите площадь прямоугольника, одна сторона которого равна 14 см, а вторая сторона в 3 раза больше первой.',fieldIds:['l90-2']},
  {id:'l90-task3',kind:'task',eyebrow:'Задание 3 из 8',title:'Куб',body:'Вычислите объём и площадь поверхности куба с ребром 3 см.',fieldIds:['l90-3v','l90-3s']},
  {id:'l90-task4',kind:'task',eyebrow:'Задание 4 из 8',title:'Прямоугольный параллелепипед',body:'Длина прямоугольного параллелепипеда равна 18 см, ширина — в 2 раза меньше длины, а высота — на 11 см больше ширины. Вычислите объём параллелепипеда.',fieldIds:['l90-4']},
  {id:'l90-task5',kind:'task',eyebrow:'Задание 5 из 8',title:'Восстановите делимое',body:'Чему равно делимое, если делитель равен 11, неполное частное — 7, а остаток — 6?',fieldIds:['l90-5']},
  {id:'l90-task6',kind:'task',eyebrow:'Задание 6 из 8',title:'Поле',body:'Поле прямоугольной формы имеет площадь 6 га. Ширина поля 150 м. Вычислите периметр поля.',fieldIds:['l90-6']},
  {id:'l90-task7',kind:'task',eyebrow:'Задание 7 из 8',title:'Комбинаторная задача',body:'Запишите все трёхзначные числа, для записи которых используются только цифры 5, 6 и 0 (цифры не могут повторяться).',fieldIds:['l90-7']},
  {id:'l90-task8',kind:'task',eyebrow:'Задание 8 из 8',title:'Сумма длин всех рёбер',body:'Сумма длин всех рёбер прямоугольного параллелепипеда равна 116 см, а два его измерения — 12 см и 11 см. Найдите третье измерение параллелепипеда.',fieldIds:['l90-8']},
  {id:'l90-submit',kind:'submit',eyebrow:'Перед сдачей',title:'Финальная самопроверка',body:'Проверь все 10 ответов, единицы измерения и полноту перебора. До сдачи можно вернуться к любому заданию.'},
  {id:'l90-summary',kind:'summary',eyebrow:'Результат',title:'Контрольная работа № 5',body:'Первичный результат сохраняется неизменным. Если есть ошибки, откроется коррекция только по неверным ответам.'},
];

function normalize(value:string){return value.trim().toLowerCase().replace(/\s+/g,'').replace(/[−–—]/g,'-').replace(/см(?:²|2|³|3)?|м(?:²|2)?/g,'')}
function normalizeSet(value:string){return value.match(/\d+/g)?.map(Number).sort((a,b)=>a-b).join(',')??''}
function correct(field:ControlField,response:string){if(field.mode==='number-set')return normalizeSet(response)===normalizeSet(field.answer);return[field.answer,...(field.accepted??[])].some(answer=>normalize(response)===normalize(answer))}
function loadSaved():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},submitted:false};const parsed=JSON.parse(raw) as Saved;if(parsed?.version===1)return{version:1,stageIndex:Math.max(0,Math.min(parsed.stageIndex,lessonNinetyStages.length-1)),responses:parsed.responses??{},submitted:Boolean(parsed.submitted),completedAt:parsed.completedAt,submittedResponses:parsed.submittedResponses,correctionFieldIds:parsed.correctionFieldIds??[],correctionCompletedAt:parsed.correctionCompletedAt}}catch{}return{version:1,stageIndex:0,responses:{},submitted:false}}

export function ControlWorkFivePlayer(){
  const initial=useMemo(loadSaved,[]);const[stageIndex,setStageIndex]=useState(initial.stageIndex);const[responses,setResponses]=useState<Record<string,string>>(initial.responses);const[submitted,setSubmitted]=useState(initial.submitted);const[completedAt,setCompletedAt]=useState<string|undefined>(initial.completedAt);const[submittedResponses,setSubmittedResponses]=useState<Record<string,string>|undefined>(initial.submittedResponses);const[correctionFieldIds,setCorrectionFieldIds]=useState<string[]>(initial.correctionFieldIds??[]);const[correctionCompletedAt,setCorrectionCompletedAt]=useState<string|undefined>(initial.correctionCompletedAt);const stage=lessonNinetyStages[stageIndex];const answeredCount=fields.filter(field=>Boolean(responses[field.id]?.trim())).length;const baselineResponses=submittedResponses??(submitted?responses:{});const score=fields.filter(field=>correct(field,baselineResponses[field.id]??'')).length;const grade=score>=9?'5':score>=7?'4':score>=5?'3':'нужно повторить';const wrongFieldIds=submitted?fields.filter(field=>!correct(field,baselineResponses[field.id]??'')).map(field=>field.id):[];const correctionMode=submitted&&correctionFieldIds.length>0&&!correctionCompletedAt;const correctionCorrectCount=correctionFieldIds.filter(id=>{const field=fieldById.get(id);return Boolean(field&&correct(field,responses[id]??''))}).length;const correctionStageIndexes=lessonNinetyStages.map((item,index)=>({item,index})).filter(({item})=>item.kind==='task'&&item.fieldIds?.some(id=>correctionFieldIds.includes(id))).map(({index})=>index);const correctionPosition=correctionStageIndexes.indexOf(stageIndex);const stageFields=(stage.fieldIds??[]).map(id=>fieldById.get(id)).filter(Boolean) as ControlField[];const stageComplete=correctionMode?stageFields.filter(field=>correctionFieldIds.includes(field.id)).every(field=>correct(field,responses[field.id]??'')):stageFields.every(field=>Boolean(responses[field.id]?.trim()));
  useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,submitted,completedAt,submittedResponses,correctionFieldIds,correctionCompletedAt} satisfies Saved))},[stageIndex,responses,submitted,completedAt,submittedResponses,correctionFieldIds,correctionCompletedAt]);
  useEffect(()=>{const go=(event:Event)=>{const detail=(event as CustomEvent<StageJumpDetail>).detail;if(detail?.lessonNumber!==90||typeof detail.stageIndex!=='number')return;moveTo(detail.stageIndex)};window.addEventListener('mathnikita-go-to-stage',go);return()=>window.removeEventListener('mathnikita-go-to-stage',go)},[]);
  function stopNarration(){window.dispatchEvent(new CustomEvent('mathnikita-stop-narration'));if('speechSynthesis'in window)window.speechSynthesis.cancel()}
  function moveTo(index:number){stopNarration();setStageIndex(Math.max(0,Math.min(index,lessonNinetyStages.length-1)));window.scrollTo({top:0,behavior:'smooth'})}
  function canEdit(id:string){return!submitted||(correctionMode&&correctionFieldIds.includes(id))}
  function setResponse(id:string,value:string){if(canEdit(id))setResponses(previous=>({...previous,[id]:value}))}
  function submit(){if(answeredCount!==fields.length||submitted)return;const now=new Date().toISOString();const snapshot={...responses};const activeSeconds=Math.round(loadLessonTiming(90).activeSeconds);setSubmittedResponses(snapshot);setSubmitted(true);setCompletedAt(now);setCorrectionFieldIds([]);setCorrectionCompletedAt(undefined);moveTo(lessonNinetyStages.length-1);localStorage.setItem(COMPLETION_KEY,JSON.stringify({completedAt:now,activeSeconds}));window.dispatchEvent(new CustomEvent('mathnikita-lesson-completed',{detail:{lessonNumber:90,completedAt:now,activeSeconds}}))}
  function startCorrection(){if(!wrongFieldIds.length)return;const indexes=lessonNinetyStages.map((item,index)=>({item,index})).filter(({item})=>item.kind==='task'&&item.fieldIds?.some(id=>wrongFieldIds.includes(id))).map(({index})=>index);setCorrectionFieldIds(wrongFieldIds);setCorrectionCompletedAt(undefined);moveTo(indexes[0]??lessonNinetyStages.length-1)}
  function finishCorrection(){if(!correctionFieldIds.length||correctionCorrectCount!==correctionFieldIds.length)return;setCorrectionCompletedAt(new Date().toISOString());moveTo(lessonNinetyStages.length-1)}
  function reset(){stopNarration();localStorage.removeItem(KEY);localStorage.removeItem(COMPLETION_KEY);setResponses({});setSubmittedResponses(undefined);setSubmitted(false);setCompletedAt(undefined);setCorrectionFieldIds([]);setCorrectionCompletedAt(undefined);setStageIndex(0)}
  return <main className="control-work-page control-five-page" data-control-work="5" data-lesson-number="90" data-source-reference="Мерзляк · методическое пособие · контрольная работа № 5 · вариант 1" data-control-field-count={fields.length}>
    <section className="lesson-player-shell"><div className="control-work-status"><div><span>Контрольная работа № 5</span><b>{submitted?`Первичный балл ${score}/${fields.length}`:`Заполнено ${answeredCount}/${fields.length}`}</b></div><div className="control-progress"><i style={{width:`${((stageIndex+1)/lessonNinetyStages.length)*100}%`}}/></div></div>
      <article className={`interactive-stage control-stage ${stage.kind==='summary'?'stage-summary':''}`} data-stage-id={stage.id}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p></div>
        {stage.kind==='intro'?<><div className="control-source-badge"><b>Точный источник</b><span>А. Г. Мерзляк и др., методическое пособие: контрольная работа № 5 «Деление с остатком. Площадь прямоугольника. Прямоугольный параллелепипед и его объём. Комбинаторные задачи», вариант 1.</span></div><div className="control-rules"><b>Во время работы</b><ul><li>решай самостоятельно;</li><li>подсказки, наставник и мгновенная проверка отключены;</li><li>ответы и разбор появятся только после сдачи;</li><li>первичный результат после сдачи не переписывается.</li></ul></div></>:null}
        {stage.kind==='task'?<div className="control-fields">{stageFields.map(field=>{const editable=canEdit(field.id);const correctionTarget=correctionMode&&correctionFieldIds.includes(field.id);return <section className={`control-field ${correctionTarget?'is-correction-target':''}`} data-control-field={field.id} key={field.id}><label><b>{field.number}. {field.label}</b><input value={responses[field.id]??''} onChange={event=>setResponse(field.id,event.target.value)} placeholder={field.placeholder} disabled={!editable}/></label>{submitted&&!correctionMode?<div className={correct(field,baselineResponses[field.id]??'')?'control-answer correct':'control-answer wrong'}><b>{correct(field,baselineResponses[field.id]??'')?'Верно':'Ошибка'}</b>{!correct(field,baselineResponses[field.id]??'')?<><span>Правильный ответ: {field.answer}</span><em>{field.explanation}</em></>:null}</div>:null}{correctionMode&&correctionTarget&&correct(field,responses[field.id]??'')?<div className="control-answer correct"><b>Исправлено ✓</b></div>:null}</section>})}</div>:null}
        {stage.kind==='submit'?<div className="control-submit-card"><strong>{answeredCount}/{fields.length}</strong><p>{answeredCount===fields.length?'Все 10 ответов заполнены. Можно сдавать работу.':'Не все ответы заполнены. Вернись к пропущенным заданиям.'}</p><button type="button" onClick={submit} disabled={answeredCount!==fields.length||submitted}>Сдать контрольную работу</button></div>:null}
        {stage.kind==='summary'?submitted?<div className="summary-card control-summary stage-summary" data-stage-id="l90-summary-result"><div className="control-score"><span>Первичный результат</span><strong>{score}/{fields.length}</strong><b>Оценка: {grade}</b><small>Сдана{completedAt?` · ${new Date(completedAt).toLocaleDateString('ru-RU')}`:''}</small></div>{wrongFieldIds.length?<div className={`control-correction-card ${correctionCompletedAt?'is-complete':''}`}><b>{correctionCompletedAt?'Коррекция завершена ✓':`Нужно закрепить: ${wrongFieldIds.length}`}</b><p>{correctionCompletedAt?'Все ошибки первой попытки исправлены. Первичный балл сохранён.':'Откроются только ответы, где была ошибка. Первичный результат останется неизменным.'}</p>{!correctionCompletedAt?<button type="button" onClick={startCorrection}>Исправить только ошибки</button>:null}</div>:<div className="control-correction-card is-complete"><b>{fields.length} из {fields.length} — все ответы верны ✓</b><p>Коррекция не требуется.</p></div>}<div className="control-review-list">{fields.map(field=>{const original=baselineResponses[field.id]??'';const ok=correct(field,original);const corrected=!ok&&Boolean(correctionCompletedAt)&&correct(field,responses[field.id]??'');return <section key={field.id} className={ok||corrected?'correct':'wrong'}><header><span>{field.number}</span><b>{ok?'Верно':corrected?'Исправлено':'Нужно исправить'}</b></header><p>{field.label}</p><small>Ответ при сдаче: {original||'—'}</small>{corrected?<strong>После коррекции: {responses[field.id]}</strong>:null}{!ok?<><strong>Правильный ответ: {field.answer}</strong><em>{field.explanation}</em></>:null}</section>})}</div><button className="control-reset" type="button" onClick={reset}>Пройти контрольную заново</button></div>:<div className="control-submit-card"><strong>Результат закрыт</strong><p>Сначала заполни задания и сдай работу.</p></div>:null}
      </article>
      {correctionMode&&stage.kind==='task'?<nav className="lesson-controls control-correction-controls" aria-label="Навигация коррекции"><button type="button" onClick={()=>moveTo(correctionStageIndexes[Math.max(0,correctionPosition-1)]??stageIndex)} disabled={correctionPosition<=0}>← Предыдущая ошибка</button><span>Коррекция {correctionPosition+1} / {correctionStageIndexes.length}</span>{correctionPosition<correctionStageIndexes.length-1?<button className="primary" type="button" onClick={()=>moveTo(correctionStageIndexes[correctionPosition+1])} disabled={!stageComplete}>Следующая ошибка →</button>:<button className="primary" type="button" onClick={finishCorrection} disabled={correctionCorrectCount!==correctionFieldIds.length}>Завершить коррекцию ✓</button>}</nav>:<nav className="lesson-controls" aria-label="Навигация контрольной работы"><button type="button" onClick={()=>moveTo(stageIndex-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {lessonNinetyStages.length}</span>{stageIndex<lessonNinetyStages.length-1?<button className="primary" type="button" onClick={()=>moveTo(stageIndex+1)} disabled={stage.kind==='task'&&!stageComplete}>Далее →</button>:<button className="primary" type="button" onClick={()=>moveTo(9)}>К сдаче</button>}</nav>}
    </section>
  </main>
}
