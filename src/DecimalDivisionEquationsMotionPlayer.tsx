import {useEffect,useMemo,useState} from 'react';import './lessonPlayer.css';import './theoryExperience.css';import {lessonOneHundredThirtyEightPractice,lessonOneHundredThirtyEightResponseCount,type LessonOneHundredThirtyEightField,type LessonOneHundredThirtyEightTask} from './data/lessonOneHundredThirtyEightPractice';
type Stage={id:string;eyebrow:string;title:string;body:string;note?:string;practice?:LessonOneHundredThirtyEightTask;summary?:boolean};type Saved={version:1;stageIndex:number;responses:Record<string,string>;checked:Record<string,boolean>;attempts:Record<string,number>};type Jump={lessonNumber?:number;stageIndex?:number};const KEY='mathnikita-lesson-138-progress-v1';const norm=(v:string)=>v.normalize('NFKC').trim().toLocaleLowerCase('ru-RU').replace(/\s+/g,'').replace(/,/g,'.');const canonicalDecimal=(v:string)=>{let s=norm(v);if(!/^[+-]?(?:\d+(?:\.\d*)?|\.\d+)$/.test(s))return null;let sign='';if(s.startsWith('+')||s.startsWith('-')){sign=s.startsWith('-')?'-':'';s=s.slice(1)}let[whole,frac='']=s.split('.');whole=(whole||'0').replace(/^0+(?=\d)/,'');frac=frac.replace(/0+$/,'');if(whole==='0'&&!frac)sign='';return `${sign}${whole}${frac?`.${frac}`:''}`};const matchField=(value:string,field:LessonOneHundredThirtyEightField)=>{const v=norm(value);if(!v)return false;const cv=canonicalDecimal(v);return field.answers.some(answer=>{const a=norm(answer);if(v===a)return true;const ca=canonicalDecimal(a);return cv!==null&&ca!==null&&cv===ca})};
const concepts:Stage[]=[
{id:'l138-order',eyebrow:'Урок 138 · § 35 · 6 из 9',title:'Скобки задают маршрут вычисления',body:'В длинном выражении сначала завершаем действия внутри скобок. Умножение и деление выполняем раньше сложения и вычитания, а сложные частные удобно считать отдельно.',note:'№995(3): 34−30,9248=3,0752; затем 7,8−4,96=2,84.'},
{id:'l138-equations',eyebrow:'Уравнения',title:'Считай выражение в скобках одним неизвестным компонентом',body:'В уравнении вида 8(x−1,4)=0,56 сначала находим значение всей скобки как неизвестного множителя. Только после этого раскрываем простое уравнение x−1,4=0,07.',note:'8(x−1,4)=0,56 → x−1,4=0,07 → x=1,47.'},
{id:'l138-division-equation',eyebrow:'Неизвестное в делителе',title:'Если скобка стоит в делителе, порядок поиска меняется',body:'В записи a:(b−x)=c выражение b−x является неизвестным делителем. Его находим как a:c, а затем решаем полученное простое уравнение.',note:'0,36:(4,5−z)=1,2 → 4,5−z=0,3 → z=4,2.'},
{id:'l138-motion',eyebrow:'Встречное движение',title:'Расстояние делим на время встречи — получаем скорость сближения',body:'При движении навстречу общая скорость сближения равна сумме скоростей. Поэтому сначала находим S:t, а затем вычитаем известную скорость.',note:'№1005: 136,8:12=11,4; 11,4−9,6=1,8 м/мин.'},
{id:'l138-direction',eyebrow:'Направление движения',title:'Навстречу — складываем скорости, догонка — вычитаем',body:'Одинаковая формула S:t даёт скорость изменения расстояния между участниками. Но затем нужно учесть направление: при встрече скорости складываются, при догонке работает их разность.',note:'Перед вычислением подпиши: встреча или догонка.'},
{id:'l138-check',eyebrow:'Самопроверка',title:'Проверяй промежуточные результаты, а не только финальный ответ',body:'В длинном выражении ошибка одного частного портит весь итог. В уравнении подставь найденное значение, а в задаче на движение проверь, что пройденные расстояния складываются в исходное.',note:'Для №1005: (1,8+9,6)×12=136,8.'},
{id:'l138-route',eyebrow:'Маршрут урока',title:'Скобки → уравнения → движение → смешанные задачи',body:'Разбираем точные №995(3), №1001(1–2), №1005, затем переносим правила в цену, геометрию, доли, догонку и длинные вычислительные цепочки.',note:'20 задач · 50 проверяемых ответов. Урок 139 продолжит §35 более сложными уравнениями и прикладными задачами.'}
];
const practiceStages:Stage[]=lessonOneHundredThirtyEightPractice.map((practice,i)=>({id:`l138-practice-${String(i+1).padStart(2,'0')}`,eyebrow:practice.sourceExact?`Учебник · ${practice.source}`:`Обязательная практика · ${i+1} из 20`,title:practice.title,body:practice.prompt,practice}));const stages:Stage[]=[...concepts,...practiceStages,{id:'l138-summary',eyebrow:'Итог урока 138',title:'Скобки, уравнения и движение собраны в один алгоритм',body:'Ты умеешь сохранять порядок действий в длинных выражениях, рассматривать скобку как неизвестный компонент уравнения и находить неизвестную скорость через скорость сближения.',note:'Следующий урок продолжит §35 уравнениями и задачами повышенной сложности.',summary:true}];
export const lessonOneHundredThirtyEightStageCount=stages.length;export const lessonOneHundredThirtyEightPracticeTaskCount=lessonOneHundredThirtyEightPractice.length;export const lessonOneHundredThirtyEightPracticeResponseCount=lessonOneHundredThirtyEightResponseCount;
function load():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}};const p=JSON.parse(raw) as Saved;return p?.version===1?p:{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}catch{return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}}
export function DecimalDivisionEquationsMotionPlayer(){const initial=useMemo(load,[]);const[stageIndex,setStageIndex]=useState(()=>Math.max(0,Math.min(initial.stageIndex,stages.length-1)));const[responses,setResponses]=useState(initial.responses);const[checked,setChecked]=useState(initial.checked);const[attempts,setAttempts]=useState(initial.attempts);const stage=stages[stageIndex];const task=stage.practice;const key=stage.id;const complete=task?task.fields.every(field=>matchField(responses[`${key}:${field.id}`]??'',field)):true;useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,checked,attempts} satisfies Saved))},[stageIndex,responses,checked,attempts]);useEffect(()=>{const handler=(event:Event)=>{const d=(event as CustomEvent<Jump>).detail;if(d?.lessonNumber!==138||typeof d.stageIndex!=='number')return;setStageIndex(Math.max(0,Math.min(Math.trunc(d.stageIndex),stages.length-1)))};window.addEventListener('mathnikita-go-to-stage',handler);return()=>window.removeEventListener('mathnikita-go-to-stage',handler)},[]);const check=()=>{if(task){setChecked(p=>({...p,[key]:true}));setAttempts(p=>({...p,[key]:(p[key]??0)+1}))}};const move=(delta:number)=>setStageIndex(i=>Math.max(0,Math.min(i+delta,stages.length-1)));return <section className="lesson-player theory-experience" aria-label="Урок 138: скобки, уравнения и встречное движение"><article className={`interactive-stage ${stage.summary?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p>{stage.note?<p className="theory-note">{stage.note}</p>:null}</div>{task?<div className="activity-area"><h3>{task.prompt}</h3><div className="answer-grid">{task.fields.map(field=>{const responseKey=`${key}:${field.id}`;return <label className="inline-answer" key={field.id}><span>{field.label}</span><input value={responses[responseKey]??''} placeholder={field.placeholder??'Введите ответ'} onChange={event=>{setResponses(p=>({...p,[responseKey]:event.target.value}));setChecked(p=>({...p,[key]:false}))}}/></label>})}</div><button className="check-button" type="button" onClick={check}>Проверить</button>{checked[key]?complete?<div className="instant-feedback good" data-explanation={task.explanation}><b>Верно.</b><span>{task.explanation}</span></div>:<div className="instant-feedback bad" data-explanation={task.explanation}><b>Пока есть ошибка.</b><span>{attempts[key]>1?task.hint:'Проверь скобки, порядок действий, неизвестный компонент и направление движения.'}</span></div>:null}</div>:null}{stage.summary?<div className="summary-card"><b>Урок 138 завершён</b><span>20 задач · 50 ответов · § 35 · 6 из 9</span></div>:null}</article><nav className="lesson-controls" aria-label="Навигация по этапам урока 138"><button type="button" onClick={()=>move(-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {stages.length}</span><button type="button" onClick={()=>move(1)} disabled={stageIndex===stages.length-1}>Дальше →</button></nav></section>}
