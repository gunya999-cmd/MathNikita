import {useEffect,useMemo,useState} from 'react';import './lessonPlayer.css';import './theoryExperience.css';import {lessonOneHundredFortySixPractice,lessonOneHundredFortySixResponseCount,type LessonOneHundredFortySixField,type LessonOneHundredFortySixTask} from './data/lessonOneHundredFortySixPractice';
type Stage={id:string;eyebrow:string;title:string;body:string;note?:string;practice?:LessonOneHundredFortySixTask;summary?:boolean};type Saved={version:1;stageIndex:number;responses:Record<string,string>;checked:Record<string,boolean>;attempts:Record<string,number>};type Jump={lessonNumber?:number;stageIndex?:number};const KEY='mathnikita-lesson-146-progress-v1';const norm=(v:string)=>v.normalize('NFKC').trim().toLocaleLowerCase('ru-RU').replace(/\s+/g,'').replace(/,/g,'.');const canonicalDecimal=(v:string)=>{let s=norm(v);if(!/^[+-]?(?:\d+(?:\.\d*)?|\.\d+)$/.test(s))return null;let sign='';if(s.startsWith('+')||s.startsWith('-')){sign=s.startsWith('-')?'-':'';s=s.slice(1)}let[whole,frac='']=s.split('.');whole=(whole||'0').replace(/^0+(?=\d)/,'');frac=frac.replace(/0+$/,'');if(whole==='0'&&!frac)sign='';return `${sign}${whole}${frac?`.${frac}`:''}`};const matchField=(value:string,field:LessonOneHundredFortySixField)=>{const v=norm(value);if(!v)return false;const cv=canonicalDecimal(v);return field.answers.some(answer=>{const a=norm(answer);if(v===a)return true;const ca=canonicalDecimal(a);return cv!==null&&ca!==null&&cv===ca})};
const concepts:Stage[]=[
{id:'l146-percent',eyebrow:'Урок 146 · § 37 · 1 из 4',title:'Один процент — одна сотая',body:'Процентом называют сотую часть величины или числа. Поэтому 1%=1/100=0,01.',note:'Вопрос 1 §37: процент — это сотая часть величины или числа.'},
{id:'l146-one-percent',eyebrow:'Как найти 1%',title:'Раздели всю величину на 100',body:'Если вся величина известна, её один процент находят делением на 100. Например, 1% от 760 равен 760:100=7,6.',note:'Вопрос 2 §37: чтобы найти 1% величины, раздели её на 100.'},
{id:'l146-find-percent',eyebrow:'Как найти p%',title:'Сначала 1%, затем умножь на число процентов',body:'Чтобы найти p% от числа x, можно вычислить x:100·p. То же самое: x·(p/100).',note:'№1057 тренирует проценты меньше, равные и больше 100.'},
{id:'l146-whole',eyebrow:'Что такое 100%',title:'Вся величина — это 100%',body:'Если ничего не убрали и не добавили, вся исходная величина составляет 100%. Проценты больше 100% тоже возможны: 156% — это 1,56 исходной величины.',note:'Вопрос 3 §37: вся величина составляет 100%.'},
{id:'l146-to-decimal',eyebrow:'Проценты → десятичная дробь',title:'Раздели число процентов на 100',body:'7%=0,07; 45%=0,45; 125%=1,25. Такая запись удобно превращает задачу «найти p%» в умножение.',note:'Например, 94% от 16,5 = 16,5·0,94.'},
{id:'l146-from-decimal',eyebrow:'Десятичная дробь → проценты',title:'Умножь число на 100',body:'Чтобы выразить десятичную дробь в процентах, умножь её на 100: 0,08=8%, 0,35=35%, 1,4=140%.',note:'Это обратное действие к делению числа процентов на 100.'},
{id:'l146-route',eyebrow:'Маршрут урока',title:'Вопросы 1–3, №1057, №1059 и повторение №1087',body:'Основная тема — смысл процента и нахождение процентов от числа. №1059 закрепляет дополнение до 100%, а №1087 в маршруте урока повторяет нахождение целого по известной дроби.',note:'20 задач · 50 проверяемых ответов. Следующий урок 147 продолжает §37.'}
];
const practiceStages:Stage[]=lessonOneHundredFortySixPractice.map((practice,i)=>({id:`l146-practice-${String(i+1).padStart(2,'0')}`,eyebrow:practice.sourceExact?`Учебник · ${practice.source}`:`Обязательная практика · ${i+1} из 20`,title:practice.title,body:practice.prompt,practice}));const stages:Stage[]=[...concepts,...practiceStages,{id:'l146-summary',eyebrow:'Итог урока 146',title:'Первый шаг в процентах сделан',body:'Ты понимаешь смысл 1% и 100%, умеешь находить проценты от числа, переводить проценты в десятичные дроби и обратно, а также решать задачи на остаток, скидку и увеличение.',note:'Далее — урок 147: продолжаем §37 и решаем обратные задачи с процентами.',summary:true}];
export const lessonOneHundredFortySixStageCount=stages.length;export const lessonOneHundredFortySixPracticeTaskCount=lessonOneHundredFortySixPractice.length;export const lessonOneHundredFortySixPracticeResponseCount=lessonOneHundredFortySixResponseCount;
function load():Saved{try{const raw=localStorage.getItem(KEY);if(!raw)return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}};const p=JSON.parse(raw) as Saved;return p?.version===1?p:{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}catch{return{version:1,stageIndex:0,responses:{},checked:{},attempts:{}}}}
export function PercentFoundationsPlayer(){const initial=useMemo(load,[]);const[stageIndex,setStageIndex]=useState(()=>Math.max(0,Math.min(initial.stageIndex,stages.length-1)));const[responses,setResponses]=useState(initial.responses);const[checked,setChecked]=useState(initial.checked);const[attempts,setAttempts]=useState(initial.attempts);const stage=stages[stageIndex];const task=stage.practice;const key=stage.id;const complete=task?task.fields.every(field=>matchField(responses[`${key}:${field.id}`]??'',field)):true;useEffect(()=>{localStorage.setItem(KEY,JSON.stringify({version:1,stageIndex,responses,checked,attempts} satisfies Saved))},[stageIndex,responses,checked,attempts]);useEffect(()=>{const handler=(event:Event)=>{const d=(event as CustomEvent<Jump>).detail;if(d?.lessonNumber!==146||typeof d.stageIndex!=='number')return;setStageIndex(Math.max(0,Math.min(Math.trunc(d.stageIndex),stages.length-1)))};window.addEventListener('mathnikita-go-to-stage',handler);return()=>window.removeEventListener('mathnikita-go-to-stage',handler)},[]);const check=()=>{if(task){setChecked(p=>({...p,[key]:true}));setAttempts(p=>({...p,[key]:(p[key]??0)+1}))}};const move=(delta:number)=>setStageIndex(i=>Math.max(0,Math.min(i+delta,stages.length-1)));return <section className="lesson-player theory-experience" aria-label="Урок 146: проценты и нахождение процентов от числа"><article className={`interactive-stage ${stage.summary?'stage-summary':''}`} data-stage-id={stage.id} data-stage-index={stageIndex}><div className="stage-copy"><span className="stage-eyebrow">{stage.eyebrow}</span><h2>{stage.title}</h2><p>{stage.body}</p>{stage.note?<p className="theory-note">{stage.note}</p>:null}</div>{task?<div className="activity-area"><h3>{task.prompt}</h3><div className="answer-grid">{task.fields.map(field=>{const responseKey=`${key}:${field.id}`;return <label className="inline-answer" key={field.id}><span>{field.label}</span><input value={responses[responseKey]??''} placeholder={field.placeholder??'Введите ответ'} onChange={event=>{setResponses(p=>({...p,[responseKey]:event.target.value}));setChecked(p=>({...p,[key]:false}))}}/></label>})}</div><button className="check-button" type="button" onClick={check}>Проверить</button>{checked[key]?complete?<div className="instant-feedback good" data-explanation={task.explanation}><b>Верно.</b><span>{task.explanation}</span></div>:<div className="instant-feedback bad" data-explanation={task.explanation}><b>Пока есть ошибка.</b><span>{attempts[key]>1?task.hint:'Проверь: вся величина — 100%, а один процент — одна сотая.'}</span></div>:null}</div>:null}{stage.summary?<div className="summary-card"><b>Урок 146 завершён</b><span>20 задач · 50 ответов · § 37 · 1 из 4</span></div>:null}</article><nav className="lesson-controls" aria-label="Навигация по этапам урока 146"><button type="button" onClick={()=>move(-1)} disabled={stageIndex===0}>← Назад</button><span>Этап {stageIndex+1} из {stages.length}</span><button type="button" onClick={()=>move(1)} disabled={stageIndex===stages.length-1}>Дальше →</button></nav></section>}
