import {expect,test} from '@playwright/test';
async function openLesson114(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 114:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 114 из 175')).toBeVisible();await expect(page.getByText('Урок 114 · глава 5 · § 31 · 2 из 3')).toBeVisible();await page.locator('.lesson-opening-start').click()}
async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:114,stageIndex}})),index)}
async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 114 opens, solves exact §31 sources and reaches summary',async({page})=>{await openLesson114(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Двойное неравенство задаёт две границы');await jump(page,8);await expect(stage).toContainText('№ 828');await solve(stage,['8','13','14','15','16','17','18','19']);await jump(page,9);await expect(stage).toContainText('№ 830');await solve(stage,['5','6','24','25']);await jump(page,10);await expect(stage).toContainText('№ 832');await solve(stage,['0','012345','789']);await jump(page,28);await expect(stage).toContainText('Урок 114 завершён')});
