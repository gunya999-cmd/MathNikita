import {expect,test,type Page} from '@playwright/test';
async function open146(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 146:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 146 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('[aria-label="Урок 146: проценты и нахождение процентов от числа"]')).toBeVisible()}
async function jump(page:Page,stageIndex:number){await page.evaluate(index=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:146,stageIndex:index}})),stageIndex)}
test('lesson 146 opens exact textbook percent task and persists progress',async({page})=>{await open146(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l146-practice-01"]');await expect(stage).toContainText('1% от числа 76');await expect(stage).toContainText('156% от числа 62');const inputs=stage.locator('input');await expect(inputs).toHaveCount(6);for(const[value,index]of[['000,7600',0],['21',1],['2,6',2],['36',3],['15,51',4],['96,72',5]] as const){await inputs.nth(index).fill(value)}await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-146-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(saved?.responses?.['l146-practice-01:a']).toBe('000,7600');await page.reload();await page.locator('button[aria-label^="Открыть урок 146:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l146-practice-01"]')).toBeVisible()});
test('lesson 146 rejects close but incorrect percent result',async({page})=>{await open146(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l146-practice-01"]');const inputs=stage.locator('input');for(const[value,index]of[['0,7600000001',0],['21',1],['2,6',2],['36',3],['15,51',4],['96,72',5]] as const){await inputs.nth(index).fill(value)}await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()});
test('lesson 146 teaches one percent, whole percent and conversions',async({page})=>{await open146(page);await jump(page,0);await expect(page.locator('.interactive-stage[data-stage-id="l146-percent"]')).toContainText('сотую');await jump(page,3);await expect(page.locator('.interactive-stage[data-stage-id="l146-whole"]')).toContainText('100%');await jump(page,5);await expect(page.locator('.interactive-stage[data-stage-id="l146-from-decimal"]')).toContainText('умножь');await expect(page.locator('.interactive-stage[data-stage-id="l146-from-decimal"]')).toContainText('100');await jump(page,27);await expect(page.locator('.interactive-stage[data-stage-id="l146-summary"]')).toContainText('20 задач · 50 ответов · § 37 · 1 из 4')});
