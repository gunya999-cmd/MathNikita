import {expect,test,type Page} from '@playwright/test';
async function open144(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 144:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 144 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('[aria-label="Урок 144: закрепление среднего арифметического и средних величин"]')).toBeVisible()}
async function jump(page:Page,stageIndex:number){await page.evaluate(index=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:144,stageIndex:index}})),stageIndex)}
test('lesson 144 opens exact reverse-mean task and persists progress',async({page})=>{await open144(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l144-practice-01"]');await expect(stage).toContainText('7,8');await expect(stage).toContainText('7,2');const input=stage.locator('input');await expect(input).toHaveCount(1);await input.fill('006,600');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-144-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(saved?.responses?.['l144-practice-01:a']).toBe('006,600');await page.reload();await page.locator('button[aria-label^="Открыть урок 144:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l144-practice-01"]')).toBeVisible()});
test('lesson 144 rejects a close but incorrect reverse-mean answer',async({page})=>{await open144(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l144-practice-01"]');await stage.locator('input').fill('6,600000001');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()});
test('lesson 144 teaches midpoint, frozen sums and reverse average speed',async({page})=>{await open144(page);await jump(page,2);await expect(page.locator('.interactive-stage[data-stage-id="l144-midpoint"]')).toContainText('5,5');await jump(page,3);await expect(page.locator('.interactive-stage[data-stage-id="l144-total-first"]')).toContainText('56');await expect(page.locator('.interactive-stage[data-stage-id="l144-total-first"]')).toContainText('80');await jump(page,5);const speed=page.locator('.interactive-stage[data-stage-id="l144-reverse-speed"]');await expect(speed).toContainText('378 км');await expect(speed).toContainText('45 км/ч');await jump(page,27);await expect(page.locator('.interactive-stage[data-stage-id="l144-summary"]')).toContainText('20 задач · 50 ответов · § 36 · 2 из 3')});
