import {expect,test} from '@playwright/test';
async function openLesson120(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 120:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 120 из 175')).toBeVisible();await expect(page.getByText('Урок 120 · глава 5 · § 33 · 2 из 6')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click())}
async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:120,stageIndex}})),index)}
async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').evaluate((node:HTMLButtonElement)=>node.click());await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 120 opens, solves exact §33 sources and reaches summary',async({page})=>{await openLesson120(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Вычитаем только одинаковые разряды');await jump(page,8);await expect(stage).toContainText('Вопрос 2');await solve(stage,['дописать нули','запятая под запятой','вычесть как натуральные числа','поставить запятую']);await jump(page,9);await expect(stage).toContainText('№ 867');await solve(stage,['2,5','9,77','5,22','14,37','3,622','20,66']);await jump(page,10);await expect(stage).toContainText('№ 873');await solve(stage,['18,2','14,6']);await jump(page,11);await expect(stage).toContainText('№ 875');await solve(stage,['70','71,5']);await jump(page,28);await expect(stage).toContainText('Вычитание десятичных дробей освоено');await expect(stage).toContainText('Урок 120 завершён')});
