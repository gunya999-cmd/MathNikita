import {expect,test} from '@playwright/test';
async function openLesson117(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 117:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 117 из 175')).toBeVisible();await expect(page.getByText('Урок 117 · глава 5 · § 32 · 2 из 3')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click())}
async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:117,stageIndex}})),index)}
async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').evaluate((node:HTMLButtonElement)=>node.click());await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 117 opens, solves exact §32 sources and reaches summary',async({page})=>{await openLesson117(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('До единиц решает цифра десятых');await jump(page,8);await expect(stage).toContainText('№ 845(3)');await solve(stage,['26','8','56','62']);await jump(page,9);await expect(stage).toContainText('№ 845(4)');await solve(stage,['2,398','8,556','47,785']);await jump(page,10);await expect(stage).toContainText('№ 847(4)');await solve(stage,['5 000 000','10 000 000']);await jump(page,11);await expect(stage).toContainText('№ 847(5)');await solve(stage,['5000','600000','30000000']);await jump(page,12);await expect(stage).toContainText('№ 861');await solve(stage,['4 кг']);await jump(page,28);await expect(stage).toContainText('Крупные разряды § 32 освоены');await expect(stage).toContainText('Урок 117 завершён')});
