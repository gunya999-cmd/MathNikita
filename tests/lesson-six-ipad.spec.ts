import { expect,test,type Page } from '@playwright/test';
type Answer={type:'choice';value:string}|{type:'input';value:string}|{type:'order';values:string[]};
const answers:Record<string,Answer>={
'l6-unique':{type:'choice',value:'ровно один'},'l6-name':{type:'choice',value:'AB и BA'},'l6-unit':{type:'choice',value:'подсчитать, сколько единичных отрезков в нём помещается'},'l6-units':{type:'choice',value:'PK = 17 мм'},'l6-endpoints':{type:'choice',value:'M и N'},'l6-whole':{type:'input',value:'15'},'l6-part':{type:'input',value:'11'},'l6-convert':{type:'choice',value:'48 мм'},'l6-equal':{type:'choice',value:'они совпадают при наложении'},'l6-build-order':{type:'order',values:['Отметить точку A','Совместить нулевую отметку линейки с A','На отметке 6 см поставить точку B','Соединить A и B по линейке']},'l6-ruler-shift':{type:'input',value:'5'},'l6-quiz1':{type:'choice',value:'1'},'l6-quiz2':{type:'input',value:'13'},'l6-quiz3':{type:'input',value:'8'},'l6-quiz4':{type:'choice',value:'54 мм'},'l6-quiz5':{type:'choice',value:'да'},'l6-challenge':{type:'input',value:'7'}};
async function openLessonSix(page:Page){await page.goto('/');const lessons=page.locator('.course-lesson-grid > button.is-interactive');await expect(lessons).toHaveCount(13);await lessons.nth(5).click();await expect(page.getByRole('heading',{name:'Отрезок. Длина отрезка'}).first()).toBeVisible();await page.locator('.lesson-opening-start').click();await expect(page.locator('[data-stage-id="l6-story"]')).toBeVisible();await expect(page.locator('.cat-mentor-collapsed')).toBeVisible()}
async function answerStage(page:Page,answer:Answer){const stage=page.locator('.lesson-runtime:not([hidden]) .interactive-stage');if(answer.type==='input')await stage.locator('.inline-answer input').fill(answer.value);else if(answer.type==='choice')await stage.getByRole('button',{name:answer.value,exact:true}).click();else for(const value of answer.values)await stage.locator('.order-bank').getByRole('button',{name:value,exact:true}).click();await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 6 completes every stage and produces full scores on iPad WebKit',async({page})=>{test.setTimeout(180_000);await openLessonSix(page);const visited=new Set<string>();for(let step=0;step<23;step+=1){const stage=page.locator('.lesson-runtime:not([hidden]) .interactive-stage');const stageId=await stage.getAttribute('data-stage-id');expect(stageId,`Stage ${step+1} must have data-stage-id`).toBeTruthy();visited.add(stageId!);if(await stage.locator('.activity-area').count()){const answer=answers[stageId!];expect(answer,`Missing automated answer for ${stageId}`).toBeTruthy();await answerStage(page,answer)}if(stageId==='l6-summary')break;const next=page.locator('.lesson-controls .primary');await expect(next).toBeEnabled();await next.click();await expect(stage).not.toHaveAttribute('data-stage-id',stageId!)}await expect(page.locator('[data-stage-id="l6-summary"]')).toBeVisible();expect(visited.size).toBe(23);expect(Object.keys(answers).every(stageId=>visited.has(stageId))).toBe(true);const summary=page.locator('.summary-card');await expect(summary).toContainText('5/5');await expect(summary).toContainText('6/6');await expect(summary).toContainText('Завершён');await expect(page.locator('.lesson-page-navigator-toggle')).toContainText('Страница 23/23');const overflow=await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth);expect(overflow).toBeLessThanOrEqual(2)});
test('lesson 6 keeps an answer after direct page navigation',async({page})=>{await openLessonSix(page);await page.evaluate(()=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:6,stageIndex:9}})));const endpoints=page.locator('[data-stage-id="l6-endpoints"]');await expect(endpoints).toBeVisible();await endpoints.getByRole('button',{name:'M и N',exact:true}).click();await page.evaluate(()=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:6,stageIndex:10}})));await expect(page.locator('[data-stage-id="l6-whole"]')).toBeVisible();await page.evaluate(()=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:6,stageIndex:9}})));await expect(page.locator('[data-stage-id="l6-endpoints"] .choice-grid button.selected')).toHaveText('M и N')});
