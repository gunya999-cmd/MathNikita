import {expect,test,type Page} from '@playwright/test';

test.use({viewport:{width:1024,height:1366},userAgent:'Mozilla/5.0 (iPad; CPU OS 18_0 like Mac OS X) AppleWebKit/605.1.15 Version/18.0 Mobile/15E148 Safari/604.1'});

async function openLesson42(page:Page){await page.goto('/');const chapterTwo=page.locator('.course-chapter-group').nth(1);if(!(await chapterTwo.evaluate(element=>(element as HTMLDetailsElement).open)))await chapterTwo.locator('summary').click();const lesson42=page.getByRole('button',{name:/Открыть урок 42:/});const lesson43=page.locator('.course-lesson-grid > button').nth(42);await expect(lesson42).toBeVisible();await expect(lesson42).toBeEnabled();await expect(lesson43).toBeDisabled();await lesson42.click();await expect(page.locator('.lesson-opening')).toContainText('Геометрические задачи на градусную меру');await page.locator('.lesson-opening-start').click()}
async function nextStage(page:Page,currentId:string,nextId:string){const stage=page.locator(`.lesson-runtime:not([hidden]) .interactive-stage[data-stage-id="${currentId}"]`);await expect(stage).toBeVisible();await stage.locator('.lesson-controls button').last().click();await expect(page.locator(`.lesson-runtime:not([hidden]) .interactive-stage[data-stage-id="${nextId}"]`)).toBeVisible()}

test('lesson 42 keeps overlapping-angle reasoning, persistence, Pythagoras and mandatory practice stable on iPad',async({page})=>{await page.addInitScript(()=>localStorage.setItem('mathnikita-mentor-auto-guide','false'));await openLesson42(page);await expect(page.locator('[data-stage-id="l42-mission"]')).toBeVisible();await expect(page.locator('.lesson-runtime:not([hidden]) .lesson-controls')).toContainText('Этап 1 из 36');await expect(page.locator('.lesson-runtime:not([hidden]) .angle-diagram svg')).toBeVisible();await nextStage(page,'l42-mission','l42-bridge');await nextStage(page,'l42-bridge','l42-right-model');await nextStage(page,'l42-right-model','l42-practice1');const practice1=page.locator('[data-stage-id="l42-practice1"]');await practice1.locator('.inline-answer input').fill('46');await practice1.locator('.check-button').click();await expect(practice1.locator('.instant-feedback.good')).toBeVisible();await practice1.locator('.lesson-controls button').last().click();await expect(page.locator('[data-stage-id="l42-proof-model"]')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-42-progress-v1')??'null'));expect(saved?.stageIndex).toBe(4);expect(saved?.results?.['l42-p1']).toBe(true);await page.getByRole('button',{name:/Все уроки/}).click();await page.getByRole('button',{name:/Открыть урок 42:/}).click();await page.locator('.lesson-opening-start').click();await expect(page.locator('[data-stage-id="l42-proof-model"]')).toBeVisible();await page.evaluate(()=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:42,stageIndex:10}})));await expect(page.locator('[data-stage-id="l42-truth-model"]')).toBeVisible();const collapsedMentor=page.locator('.cat-mentor-collapsed');await expect(collapsedMentor).toBeVisible();await collapsedMentor.click();await expect(page.locator('.cat-mentor-panel')).toBeVisible();await expect(page.locator('.cat-mentor-actions button')).toHaveCount(4);await page.evaluate(()=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:42,stageIndex:35}})));await expect(page.locator('[data-stage-id="l42-summary"]')).toBeVisible();await expect(page.locator('.lesson-reflection')).toHaveCount(1);await expect(page.locator('.extended-practice')).toHaveCount(1);await expect(page.locator('.extended-practice[data-practice-task="l42-extra-01"]')).toBeVisible();const practice=page.locator('.extended-practice');await practice.locator('.extended-practice-input input').fill('46');await practice.locator('.extended-practice-check').click();await expect(practice.locator('.extended-practice-feedback.is-correct')).toBeVisible();await expect(practice.locator('.practice-pythagoras')).toBeVisible();await expect(practice.locator('.practice-pythagoras-actions button')).toHaveCount(4);const overflow=await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth);expect(overflow).toBeLessThanOrEqual(2)});
