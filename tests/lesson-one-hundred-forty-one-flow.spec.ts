import {expect,test,type Page} from '@playwright/test';async function open141(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 141:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 141 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByLabel('Урок 141: итог §35 и систематизация деления')).toBeVisible()}async function jump(page:Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:141,stageIndex}})),index)}test('lesson 141 opens exact equations and persists progress',async({page})=>{await open141(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l141-practice-01"]');await expect(stage).toBeVisible();await expect(stage.locator('input')).toHaveCount(2);await stage.locator('input').nth(0).fill('02,500');await stage.locator('input').nth(1).fill('0,30');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-141-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(Object.keys(saved?.responses??{})).toHaveLength(2);await page.reload();await page.locator('button[aria-label^="Открыть урок 141:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l141-practice-01"]')).toBeVisible();});test('lesson 141 rejects a close but incorrect textbook answer',async({page})=>{await open141(page);await jump(page,9);const stage=page.locator('.interactive-stage[data-stage-id="l141-practice-03"]');await stage.locator('input').fill('6,889999999');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible();});test('lesson 141 explains productivity and decimal shift models',async({page})=>{await open141(page);await jump(page,1);await expect(page.locator('.interactive-stage[data-stage-id="l141-productivity"]')).toContainText('65,34:5,4=12,1');await jump(page,2);const shift=page.locator('.interactive-stage[data-stage-id="l141-decimal-shift"]');await expect(shift).toContainText('10x−x=9x');await expect(shift).toContainText('x=6,89');});