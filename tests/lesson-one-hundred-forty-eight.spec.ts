import {expect,test} from '@playwright/test';import {lessonOneHundredFortyEightPractice,lessonOneHundredFortyEightResponseCount} from '../src/data/lessonOneHundredFortyEightPractice';import {extendedPracticeByLesson} from '../src/data/extendedPracticeData';import {extendedPracticeSetResponseCount} from '../src/data/extendedPracticeTypes';import {isExtendedPracticeAnswerCorrect,normalizeExactDecimalPracticeAnswer} from '../src/extendedPracticeEngine';
test('lesson 148 source and workload contract',()=>{expect(lessonOneHundredFortyEightPractice).toHaveLength(20);expect(lessonOneHundredFortyEightResponseCount).toBe(50);expect(lessonOneHundredFortyEightPractice.slice(0,3).map(task=>task.source)).toEqual(['Мерзляк · §37 · №1072','Мерзляк · §37 · №1074','Мерзляк · §37 · №1076']);expect(lessonOneHundredFortyEightPractice.slice(0,3).every(task=>task.sourceExact)).toBe(true);expect(lessonOneHundredFortyEightPractice[0].fields.map(field=>field.answers[0])).toEqual(['44']);expect(lessonOneHundredFortyEightPractice[1].fields.map(field=>field.answers[0])).toEqual(['88']);expect(lessonOneHundredFortyEightPractice[2].fields.map(field=>field.answers[0])).toEqual(['254']);});
test('lesson 148 is registered in mandatory completion practice',()=>{const practice=extendedPracticeByLesson[148];expect(practice).toBeDefined();expect(practice.tasks).toHaveLength(20);expect(extendedPracticeSetResponseCount(practice)).toBe(50);expect(practice.tasks.every(task=>task.type==='multi-input')).toBe(true);for(const task of practice.tasks){if(task.type!=='multi-input')continue;expect(task.fields.every(field=>field.validation==='exact-decimal')).toBe(true)}});
test('lesson 148 mandatory practice keeps exact decimal semantics',()=>{expect(normalizeExactDecimalPracticeAnswer('044,000')).toBe('44');const practice=extendedPracticeByLesson[148];const task=practice.tasks[0];expect(task.type).toBe('multi-input');if(task.type!=='multi-input')return;expect(isExtendedPracticeAnswerCorrect(task,{a:'044,000'})).toBe(true);expect(isExtendedPracticeAnswerCorrect(task,{a:'44,0000001'})).toBe(false);expect(isExtendedPracticeAnswerCorrect(task,{a:'43,9999999'})).toBe(false);});
