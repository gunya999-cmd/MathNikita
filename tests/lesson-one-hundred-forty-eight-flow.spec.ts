import {expect,test,type Page} from '@playwright/test';
async function open148(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 148:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 148 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('[aria-label="Урок 148: составные задачи на проценты"]')).toBeVisible()}
async function jump(page:Page,stageIndex:number){await page.evaluate(index=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:148,stageIndex:index}})),stageIndex)}
test('lesson 148 opens exact textbook task and persists progress',async({page})=>{await open148(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l148-practice-01"]');await expect(stage).toContainText('80 пудов');await expect(stage).toContainText('45%');const input=stage.locator('input');await expect(input).toHaveCount(1);await input.fill('044,000');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-148-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(saved?.responses?.['l148-practice-01:a']).toBe('044,000');await page.reload();await page.locator('button[aria-label^="Открыть урок 148:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l148-practice-01"]')).toBeVisible()});
test('lesson 148 rejects close but incorrect result',async({page})=>{await open148(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l148-practice-01"]');await stage.locator('input').fill('44,0000001');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()});
test('lesson 148 teaches complement, multiple parts and percentages above 100',async({page})=>{await open148(page);await jump(page,0);await expect(page.locator('.interactive-stage[data-stage-id="l148-complement"]')).toContainText('100%');await jump(page,2);await expect(page.locator('.interactive-stage[data-stage-id="l148-multiple-parts"]')).toContainText('100%');await jump(page,3);await expect(page.locator('.interactive-stage[data-stage-id="l148-over-hundred"]')).toContainText('101,6%');await jump(page,27);await expect(page.locator('.interactive-stage[data-stage-id="l148-summary"]')).toContainText('20 задач · 50 ответов · § 37 · 3 из 4')});
