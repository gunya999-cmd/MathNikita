import {expect,test} from '@playwright/test';
async function openLesson113(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 113:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 113 из 175')).toBeVisible();await expect(page.getByText('Урок 113 · глава 5 · § 31 · 1 из 3')).toBeVisible();await page.locator('.lesson-opening-start').click()}
async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:113,stageIndex}})),index)}
async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 113 opens, solves exact §31 sources and reaches summary',async({page})=>{await openLesson113(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Нули справа не меняют десятичную дробь');await jump(page,8);await expect(stage).toContainText('№ 824');await solve(stage,['<','>','>','<','<','>']);await jump(page,9);await expect(stage).toContainText('№ 826');await solve(stage,['9,02','9,2','9,53','9,6','9,613','9,8']);await jump(page,10);await expect(stage).toContainText('№ 839');await solve(stage,['10','да']);await jump(page,28);await expect(stage).toContainText('Урок 113 завершён')});
