import {expect,test,type Page} from '@playwright/test';
async function openLesson80(page:Page){await page.goto('/');const chapterThree=page.locator('.course-chapter-group').nth(2);if(!(await chapterThree.evaluate(element=>(element as HTMLDetailsElement).open)))await chapterThree.locator('summary').click();const lessons=page.locator('.course-lesson-grid > button');await expect(lessons.nth(79)).toContainText('Пирамида: основание, грани, рёбра и развёртка');await expect(lessons.nth(79)).toBeEnabled();await expect(lessons.nth(80)).toContainText('Объём фигуры: единичный куб и кубические единицы');await expect(lessons.nth(80)).toBeEnabled();await expect(lessons.nth(81)).toContainText('Объём прямоугольного параллелепипеда: формулы и задачи');await expect(lessons.nth(81)).toBeEnabled();await expect(lessons.nth(82)).toContainText('Объём: обратные и составные задачи');await expect(lessons.nth(82)).toBeEnabled();await expect(lessons.nth(83)).toContainText('Объём прямоугольного параллелепипеда');await expect(lessons.nth(83)).toBeDisabled();await page.getByRole('button',{name:/Открыть урок 80:/}).click();await page.locator('.lesson-opening-start').click()}
async function jump(page:Page,stageIndex:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:80,stageIndex}})),stageIndex)}
async function noHorizontalOverflow(page:Page){return page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth+2)}
test('lesson 80 remains usable after lesson 83 unlock and reaches 50-response practice',async({page})=>{test.setTimeout(100_000);await page.addInitScript(()=>localStorage.setItem('mathnikita-mentor-auto-guide','false'));await openLesson80(page);const stage=page.locator('.lesson-runtime:not([hidden]) .interactive-stage[data-stage-id]');await expect(stage).toHaveAttribute('data-stage-id','l80-mission');expect(await noHorizontalOverflow(page)).toBeTruthy();await jump(page,2);await expect(stage).toHaveAttribute('data-stage-id','l80-oral5-kg');const input=stage.locator('.inline-answer input');for(const wrong of ['500','700']){await input.fill(wrong);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()}await expect(stage.getByRole('button',{name:'Дальше →',exact:true})).toBeEnabled();await input.fill('600');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const beforeReload=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-80-progress-v1')??'null'));expect(beforeReload?.results?.['l80-p1']).toBe(true);await page.reload();await page.getByRole('button',{name:/Открыть урок 80:/}).click();await page.locator('.lesson-opening-start').click();await expect(page.locator('.lesson-runtime:not([hidden]) .interactive-stage')).toHaveAttribute('data-stage-id','l80-oral5-kg');await jump(page,35);await expect(page.locator('.lesson-runtime:not([hidden]) .interactive-stage')).toHaveAttribute('data-stage-id','l80-summary');await expect(page.locator('.extended-practice')).toHaveAttribute('data-practice-response-count','50');await expect(page.locator('.practice-pythagoras-actions button')).toHaveCount(4);expect(await noHorizontalOverflow(page)).toBeTruthy()});
