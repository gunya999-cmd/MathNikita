import {expect,test,type Page} from '@playwright/test';
async function open143(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 143:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 143 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('[aria-label="Урок 143: среднее арифметическое и среднее значение величины"]')).toBeVisible()}
async function jump(page:Page,stageIndex:number){await page.evaluate(index=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:143,stageIndex:index}})),stageIndex)}
test('lesson 143 opens exact textbook arithmetic means and persists progress',async({page})=>{await open143(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l143-practice-01"]');await expect(stage).toContainText('4,2 и 2,1');await expect(stage).toContainText('3,9; 6; 9,18 и 15,8');const inputs=stage.locator('input');await expect(inputs).toHaveCount(2);await inputs.nth(0).fill('003,1500');await inputs.nth(1).fill('8.72');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-143-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(Object.keys(saved?.responses??{})).toHaveLength(2);await page.reload();await page.locator('button[aria-label^="Открыть урок 143:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l143-practice-01"]')).toBeVisible()});
test('lesson 143 rejects a close but incorrect arithmetic mean',async({page})=>{await open143(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l143-practice-01"]');const inputs=stage.locator('input');await inputs.nth(0).fill('3,149999999');await inputs.nth(1).fill('8,72');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()});
test('lesson 143 teaches total path over total time and weighted mean values',async({page})=>{await open143(page);await jump(page,3);await expect(page.locator('.interactive-stage[data-stage-id="l143-mean-value"]')).toContainText('1552:50=31,04');await jump(page,4);const speed=page.locator('.interactive-stage[data-stage-id="l143-speed"]');await expect(speed).toContainText('Средняя скорость = весь путь : всё время');await expect(speed).toContainText('420 км за 7 ч → 60 км/ч');await jump(page,27);await expect(page.locator('.interactive-stage[data-stage-id="l143-summary"]')).toContainText('20 задач · 50 ответов · § 36 · 1 из 3')});