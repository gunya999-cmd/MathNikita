import {expect,test,type Page} from '@playwright/test';
async function open151(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 151:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 151 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('[aria-label="Урок 151: прикладные задачи на нахождение числа по процентам"]')).toBeVisible()}
async function jump(page:Page,stageIndex:number){await page.evaluate(index=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:151,stageIndex:index}})),stageIndex)}
test('lesson 151 opens exact textbook task and persists progress',async({page})=>{await open151(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l151-practice-01"]');await expect(stage).toContainText('14% соли');await expect(stage.locator('input')).toHaveCount(1);await stage.locator('input').fill('0350,000');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-151-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(saved?.responses?.['l151-practice-01:a']).toBe('0350,000');await page.reload();await page.locator('button[aria-label^="Открыть урок 151:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l151-practice-01"]')).toBeVisible()});
test('lesson 151 rejects close but incorrect result',async({page})=>{await open151(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l151-practice-01"]');await stage.locator('input').fill('350,0000001');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()});
test('lesson 151 teaches application models including over 100 percent',async({page})=>{await open151(page);await jump(page,0);await expect(page.locator('.interactive-stage[data-stage-id="l151-application"]')).toContainText('100%');await jump(page,1);await expect(page.locator('.interactive-stage[data-stage-id="l151-solution"]')).toContainText('49:0,14=350');await jump(page,3);await expect(page.locator('.interactive-stage[data-stage-id="l151-over100"]')).toContainText('28,8:1,2=24');await jump(page,27);await expect(page.locator('.interactive-stage[data-stage-id="l151-summary"]')).toContainText('20 задач · 50 ответов · § 38 · 2 из 4')});
