import {expect,test,type Page} from '@playwright/test';
async function openLesson88(page:Page){await page.goto('/');const chapter=page.locator('.course-chapter-group').nth(2);if(!(await chapter.evaluate(el=>(el as HTMLDetailsElement).open)))await chapter.locator('summary').click();const lessons=page.locator('.course-lesson-grid > button');await expect(lessons.nth(87)).toContainText('Повторение');await expect(lessons.nth(87)).toBeEnabled();await page.getByRole('button',{name:/Открыть урок 88:/}).click();await page.locator('.lesson-opening-start').click()}
async function jump(page:Page,stageIndex:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:88,stageIndex}})),stageIndex)}
test('lesson 88 full flow preserves chapter-three diagnostic, persistence and exact practice contract',async({page})=>{test.setTimeout(120_000);await page.addInitScript(()=>localStorage.setItem('mathnikita-mentor-auto-guide','false'));await openLesson88(page);const stage=page.locator('.lesson-runtime:not([hidden]) .interactive-stage[data-stage-id]');await expect(stage).toHaveAttribute('data-stage-id','l88-mission');await jump(page,3);await expect(stage).toHaveAttribute('data-stage-id','l88-multiply-check');const input=stage.locator('.inline-answer input');for(const wrong of ['700','170']){await input.fill(wrong);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()}await expect(stage.getByRole('button',{name:'Дальше →',exact:true})).toBeEnabled();await input.fill('1700');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-88-progress-v1')??'null'));expect(saved?.results?.['l88-p1']).toBe(true);await jump(page,27);await expect(stage).toHaveAttribute('data-stage-id','l88-test-08');await stage.getByRole('button',{name:'4',exact:true}).click();await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();await jump(page,31);await expect(stage).toHaveAttribute('data-stage-id','l88-test-12');await stage.getByRole('button',{name:'8',exact:true}).click();await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();await jump(page,35);await expect(stage).toHaveAttribute('data-stage-id','l88-summary');await expect(page.locator('.lesson-reflection .extended-practice')).toHaveAttribute('data-practice-response-count','50');await expect(page.locator('.lesson-reflection .extended-practice')).toHaveAttribute('data-practice-task','l88-extra-01')});
