import {expect,test,type Page} from '@playwright/test';
async function openLesson85(page:Page){await page.goto('/');const chapter=page.locator('.course-chapter-group').nth(2);if(!(await chapter.evaluate(el=>(el as HTMLDetailsElement).open)))await chapter.locator('summary').click();await page.getByRole('button',{name:/Открыть урок 85:/}).click();await page.locator('.lesson-opening-start').click()}
async function jump(page:Page,stageIndex:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:85,stageIndex}})),stageIndex)}
async function noHorizontalOverflow(page:Page){return page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth+2)}
test('lesson 85 is usable on iPad, preserves retry state and reaches 50-response practice',async({page})=>{test.setTimeout(120_000);await page.addInitScript(()=>localStorage.setItem('mathnikita-mentor-auto-guide','false'));await openLesson85(page);const stage=page.locator('.lesson-runtime:not([hidden]) .interactive-stage[data-stage-id]');await expect(stage).toHaveAttribute('data-stage-id','l85-mission');expect(await noHorizontalOverflow(page)).toBeTruthy();await jump(page,17);await expect(stage).toHaveAttribute('data-stage-id','l85-650-first');const input=stage.locator('.inline-answer input');for(const wrong of ['1','3']){await input.fill(wrong);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()}await expect(stage.getByRole('button',{name:'Дальше →',exact:true})).toBeEnabled();await input.fill('2');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const before=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-85-progress-v1')??'null'));expect(before?.results?.['l85-p11']).toBe(true);await page.reload();await page.getByRole('button',{name:/Открыть урок 85:/}).click();await page.locator('.lesson-opening-start').click();await expect(page.locator('.lesson-runtime:not([hidden]) .interactive-stage')).toHaveAttribute('data-stage-id','l85-650-first');await jump(page,35);await expect(page.locator('.lesson-runtime:not([hidden]) .interactive-stage')).toHaveAttribute('data-stage-id','l85-summary');await expect(page.locator('.lesson-reflection .extended-practice')).toHaveAttribute('data-practice-response-count','50');await expect(page.locator('.practice-pythagoras-actions button')).toHaveCount(4);expect(await noHorizontalOverflow(page)).toBeTruthy()});