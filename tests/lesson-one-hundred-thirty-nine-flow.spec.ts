import {expect,test,type Page} from '@playwright/test';async function open139(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 139:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 139 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByLabel('Урок 139: сложные уравнения и догонка')).toBeVisible()}async function jump(page:Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:139,stageIndex}})),index)}test('lesson 139 opens exact textbook expression and persists progress',async({page})=>{await open139(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l139-practice-01"]');await expect(stage).toBeVisible();await expect(stage.locator('input')).toHaveCount(1);await stage.locator('input').fill('005,990');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-139-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(Object.keys(saved?.responses??{})).toHaveLength(1);await page.reload();await page.locator('button[aria-label^="Открыть урок 139:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l139-practice-01"]')).toBeVisible();});test('lesson 139 rejects a close but incorrect equation answer',async({page})=>{await open139(page);await jump(page,8);const stage=page.locator('.interactive-stage[data-stage-id="l139-practice-02"]');await stage.locator('input').nth(0).fill('548,679999999');await stage.locator('input').nth(1).fill('55,52');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible();});test('lesson 139 explains pursuit through difference of speeds',async({page})=>{await open139(page);await jump(page,3);const stage=page.locator('.interactive-stage[data-stage-id="l139-pursuit"]');await expect(stage).toContainText('302−298,8=3,2');await expect(stage).toContainText('30,4:3,2=9,5');await jump(page,4);await expect(page.locator('.interactive-stage[data-stage-id="l139-direction"]')).toContainText('догонка — разность');});