import {expect,test} from '@playwright/test';import fs from 'node:fs';
test('lesson 144 player keeps strict decimal semantics and 28-stage contract',()=>{const source=fs.readFileSync('src/ArithmeticMeanReinforcementPlayer.tsx','utf8');expect(source).toContain("const KEY='mathnikita-lesson-144-progress-v1'");expect(source).toContain('canonicalDecimal');expect(source).not.toContain('Math.abs');expect(source).not.toContain('toFixed');expect(source).toContain('lessonNumber!==144');expect(source).toContain('20 задач · 50 ответов · § 36 · 2 из 3');expect(source).toContain('lessonOneHundredFortyFourPracticeResponseCount=lessonOneHundredFortyFourResponseCount');});
