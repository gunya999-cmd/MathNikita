import {expect,test} from '@playwright/test';import fs from 'node:fs';
test('lesson 154 player keeps strict decimal semantics and 28-stage contract',()=>{const source=fs.readFileSync('src/MeanPercentReviewPlayer.tsx','utf8');expect(source).toContain("const KEY='mathnikita-lesson-154-progress-v1'");expect(source).toContain('canonicalDecimal');expect(source).not.toContain('Math.abs');expect(source).not.toContain('toFixed');expect(source).toContain('lessonNumber!==154');expect(source).toContain('20 задач · 50 ответов · повторение § 36–38');expect(source).toContain('lessonOneHundredFiftyFourPracticeResponseCount=lessonOneHundredFiftyFourResponseCount');});
