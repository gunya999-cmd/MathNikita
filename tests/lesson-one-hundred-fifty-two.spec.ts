import {expect,test} from '@playwright/test';import {lessonOneHundredFiftyTwoPractice,lessonOneHundredFiftyTwoResponseCount} from '../src/data/lessonOneHundredFiftyTwoPractice';import {extendedPracticeByLesson} from '../src/data/extendedPracticeData';import {extendedPracticeSetResponseCount} from '../src/data/extendedPracticeTypes';import {isExtendedPracticeAnswerCorrect,normalizeExactDecimalPracticeAnswer} from '../src/extendedPracticeEngine';
test('lesson 152 source and workload contract',()=>{expect(lessonOneHundredFiftyTwoPractice).toHaveLength(20);expect(lessonOneHundredFiftyTwoResponseCount).toBe(50);expect(lessonOneHundredFiftyTwoPractice.slice(0,4).map(task=>task.source)).toEqual(['Мерзляк · §38 · №1104','Мерзляк · §38 · №1106','Мерзляк · §38 · №1108','Мерзляк · §38 · №1122']);expect(lessonOneHundredFiftyTwoPractice.slice(0,4).every(task=>task.sourceExact)).toBe(true);expect(lessonOneHundredFiftyTwoPractice[0].fields.map(field=>field.answers[0])).toEqual(['25']);expect(lessonOneHundredFiftyTwoPractice[1].fields.map(field=>field.answers[0])).toEqual(['400']);expect(lessonOneHundredFiftyTwoPractice[2].fields.map(field=>field.answers[0])).toEqual(['10,8','12','30']);expect(lessonOneHundredFiftyTwoPractice[3].fields.map(field=>field.answers[0])).toEqual(['200']);});
test('lesson 152 is registered in mandatory completion practice',()=>{const practice=extendedPracticeByLesson[152];expect(practice).toBeDefined();expect(practice.tasks).toHaveLength(20);expect(extendedPracticeSetResponseCount(practice)).toBe(50);expect(practice.tasks.every(task=>task.type==='multi-input')).toBe(true);for(const task of practice.tasks){if(task.type!=='multi-input')continue;expect(task.fields.every(field=>field.validation==='exact-decimal')).toBe(true)}});
test('lesson 152 mandatory practice keeps exact decimal semantics',()=>{expect(normalizeExactDecimalPracticeAnswer('025,000')).toBe('25');const practice=extendedPracticeByLesson[152];const task=practice.tasks[0];expect(task.type).toBe('multi-input');if(task.type!=='multi-input')return;expect(isExtendedPracticeAnswerCorrect(task,{a:'025,000'})).toBe(true);expect(isExtendedPracticeAnswerCorrect(task,{a:'25,0000001'})).toBe(false);});
