import {expect,test,type Page} from '@playwright/test';
async function open90(page:Page){await page.goto('/');const chapter=page.locator('.course-chapter-group').nth(2);if(!(await chapter.evaluate(el=>(el as HTMLDetailsElement).open)))await chapter.locator('summary').click();await page.getByRole('button',{name:/Открыть урок 90:/}).click();await page.locator('.lesson-opening-start').click()}
async function jump(page:Page,stageIndex:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:90,stageIndex}})),stageIndex)}
const values:Record<string,string>={'l90-1q':'31','l90-1r':'13','l90-2':'588','l90-3v':'27','l90-3s':'54','l90-4':'3240','l90-5':'82','l90-6':'1100','l90-7':'506,560,605,650','l90-8':'6'};
test('lesson 90 freezes primary attempt while correction stays separate',async({page})=>{test.setTimeout(120000);await page.addInitScript(()=>localStorage.setItem('mathnikita-mentor-auto-guide','false'));await open90(page);for(let stageIndex=1;stageIndex<=8;stageIndex++){await jump(page,stageIndex);for(const [id,value] of Object.entries(values)){const input=page.locator(`[data-control-field="${id}"] input`);if(await input.count())await input.fill(value)}}await jump(page,9);await page.getByRole('button',{name:'Сдать контрольную работу'}).click();await expect(page.getByText('9/10',{exact:true})).toBeVisible();await expect(page.getByText('Правильный ответ: 83')).toBeVisible();await page.getByRole('button',{name:'Исправить только ошибки'}).click();const target=page.locator('[data-control-field="l90-5"] input');await expect(target).toBeEnabled();await target.fill('83');await page.getByRole('button',{name:'Завершить коррекцию ✓'}).click();await expect(page.getByText('9/10',{exact:true})).toBeVisible();await expect(page.getByText('Коррекция завершена ✓')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-90-control-v1')??'null'));expect(saved.submittedResponses['l90-5']).toBe('82');expect(saved.responses['l90-5']).toBe('83');expect(saved.correctionCompletedAt).toBeTruthy()});
