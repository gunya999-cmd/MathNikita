import {expect,test,type Page} from '@playwright/test';
async function openLesson87(page:Page){await page.goto('/');const chapter=page.locator('.course-chapter-group').nth(2);if(!(await chapter.evaluate(el=>(el as HTMLDetailsElement).open)))await chapter.locator('summary').click();const lessons=page.locator('.course-lesson-grid > button');await expect(lessons.nth(86)).toContainText('Комбинаторные задачи');await expect(lessons.nth(86)).toBeEnabled();await page.getByRole('button',{name:/Открыть урок 87:/}).click();await page.locator('.lesson-opening-start').click()}
async function jump(page:Page,stageIndex:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:87,stageIndex}})),stageIndex)}
test('lesson 87 full flow preserves unordered-pair logic, persistence and exact practice contract',async({page})=>{test.setTimeout(120_000);await page.addInitScript(()=>localStorage.setItem('mathnikita-mentor-auto-guide','false'));await openLesson87(page);const stage=page.locator('.lesson-runtime:not([hidden]) .interactive-stage[data-stage-id]');await expect(stage).toHaveAttribute('data-stage-id','l87-mission');await jump(page,6);await expect(stage).toHaveAttribute('data-stage-id','l87-659-sum');const input=stage.locator('.inline-answer input');for(const wrong of ['10','14']){await input.fill(wrong);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()}await expect(stage.getByRole('button',{name:'Дальше →',exact:true})).toBeEnabled();await input.fill('12');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-87-progress-v1')??'null'));expect(saved?.results?.['l87-p4']).toBe(true);await jump(page,17);await expect(stage).toHaveAttribute('data-stage-id','l87-666-source');await stage.locator('.inline-answer input').fill('6');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();await jump(page,30);await expect(stage).toHaveAttribute('data-stage-id','l87-673-conclusion');await stage.getByRole('button',{name:'нет',exact:true}).click();await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();await jump(page,35);await expect(stage).toHaveAttribute('data-stage-id','l87-summary');await expect(page.locator('.lesson-reflection .extended-practice')).toHaveAttribute('data-practice-response-count','50');await expect(page.locator('.lesson-reflection .extended-practice')).toHaveAttribute('data-practice-task','l87-extra-01')});
