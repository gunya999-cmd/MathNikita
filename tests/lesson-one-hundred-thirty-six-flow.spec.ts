import {expect,test,type Page} from '@playwright/test';async function open136(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 136:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 136 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByLabel('Урок 136: закрепление деления на десятичную дробь')).toBeVisible()}async function jump(page:Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:136,stageIndex}})),index)}test('lesson 136 opens exact source practice and persists progress',async({page})=>{await open136(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l136-practice-01"]');await expect(stage).toBeVisible();await expect(stage.locator('input')).toHaveCount(3);for(const[index,value]of ['0240,00','30.800','0,5180'].entries())await stage.locator('input').nth(index).fill(value);await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-136-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(Object.keys(saved?.responses??{})).toHaveLength(3);await page.reload();await page.locator('button[aria-label^="Открыть урок 136:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l136-practice-01"]')).toBeVisible();});test('lesson 136 rejects a close but incorrect quotient',async({page})=>{await open136(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l136-practice-01"]');for(const[index,value]of ['240','30,8','0,517999999999'].entries())await stage.locator('input').nth(index).fill(value);await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible();});test('lesson 136 teaches scale control and like-term equations',async({page})=>{await open136(page);await jump(page,0);await expect(page.locator('.interactive-stage[data-stage-id="l136-scale"]')).toContainText('9,6:0,04=240');await jump(page,3);const equations=page.locator('.interactive-stage[data-stage-id="l136-like-terms"]');await expect(equations).toContainText('приведи подобные');await expect(equations).toContainText('8,4a=13,44');});