import {expect,test} from '@playwright/test';
async function openLesson110(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 110:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 110 из 175')).toBeVisible();await expect(page.getByText('Урок 110 · глава 5 · § 30 · 2 из 4')).toBeVisible();await page.locator('.lesson-opening-start').click()}
async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:110,stageIndex}})),index)}
async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 110 opens, solves exact §30 sources and reaches summary',async({page})=>{await openLesson110(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Знаменатель задаёт длину дробной части');await jump(page,8);await expect(stage).toContainText('№ 799(9–16)');await solve(stage,['2,003','74,00013','0,006','0,0012','0,00005','1,1','1,01','1,001']);await jump(page,9);await expect(stage).toContainText('№ 801(4–6)');await solve(stage,['92,66','8,448','29,48697']);await jump(page,10);await expect(stage).toContainText('№ 803(7–8)');await solve(stage,['5 6/100','12 18/1000']);await jump(page,11);await expect(stage).toContainText('№ 805');await solve(stage,['2,7','30,28','0,013']);await jump(page,28);await expect(stage).toContainText('Урок 110 завершён')});
