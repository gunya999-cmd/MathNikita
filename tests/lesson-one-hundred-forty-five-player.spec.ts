import {expect,test} from '@playwright/test';import fs from 'node:fs';
test('lesson 145 player keeps strict decimal semantics and 28-stage contract',()=>{const source=fs.readFileSync('src/ArithmeticMeanSynthesisPlayer.tsx','utf8');expect(source).toContain("const KEY='mathnikita-lesson-145-progress-v1'");expect(source).toContain('canonicalDecimal');expect(source).not.toContain('Math.abs');expect(source).not.toContain('toFixed');expect(source).toContain('lessonNumber!==145');expect(source).toContain('20 задач · 50 ответов · § 36 · 3 из 3');expect(source).toContain('lessonOneHundredFortyFivePracticeResponseCount=lessonOneHundredFortyFiveResponseCount');});
