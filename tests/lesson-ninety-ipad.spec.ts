import {expect,test} from '@playwright/test';
test('lesson 90 control work stays usable on iPad without horizontal overflow',async({page})=>{await page.addInitScript(()=>localStorage.setItem('mathnikita-mentor-auto-guide','false'));await page.goto('/');const chapter=page.locator('.course-chapter-group').nth(2);if(!(await chapter.evaluate(el=>(el as HTMLDetailsElement).open)))await chapter.locator('summary').click();const lesson=page.locator('.course-lesson-grid > button').nth(89);await expect(lesson).toBeEnabled();await expect(lesson).toHaveClass(/is-control-ready/);await lesson.click();await page.locator('.lesson-opening-start').click();await page.evaluate(()=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:90,stageIndex:1}})));const input=page.locator('[data-control-field="l90-1q"] input');await expect(input).toBeVisible();await input.tap();await input.fill('31');const overflow=await page.evaluate(()=>document.documentElement.scrollWidth-document.documentElement.clientWidth);expect(overflow).toBeLessThanOrEqual(1)});
