import {expect,test} from '@playwright/test';import {lessonOneHundredFortyNinePractice,lessonOneHundredFortyNineResponseCount} from '../src/data/lessonOneHundredFortyNinePractice';import {extendedPracticeByLesson} from '../src/data/extendedPracticeData';import {extendedPracticeSetResponseCount} from '../src/data/extendedPracticeTypes';import {isExtendedPracticeAnswerCorrect,normalizeExactDecimalPracticeAnswer} from '../src/extendedPracticeEngine';
test('lesson 149 source and workload contract',()=>{expect(lessonOneHundredFortyNinePractice).toHaveLength(20);expect(lessonOneHundredFortyNineResponseCount).toBe(50);expect(lessonOneHundredFortyNinePractice.slice(0,3).map(task=>task.source)).toEqual(['Мерзляк · §37 · №1079','Мерзляк · §37 · №1082','Мерзляк · §37 · №1084']);expect(lessonOneHundredFortyNinePractice.slice(0,3).every(task=>task.sourceExact)).toBe(true);expect(lessonOneHundredFortyNinePractice[0].fields.map(field=>field.answers[0])).toEqual(['94710']);expect(lessonOneHundredFortyNinePractice[1].fields.map(field=>field.answers[0])).toEqual(['188','2160']);expect(lessonOneHundredFortyNinePractice[2].fields.map(field=>field.answers[0])).toEqual(['2040','1734']);});
test('lesson 149 is registered in mandatory completion practice',()=>{const practice=extendedPracticeByLesson[149];expect(practice).toBeDefined();expect(practice.tasks).toHaveLength(20);expect(extendedPracticeSetResponseCount(practice)).toBe(50);expect(practice.tasks.every(task=>task.type==='multi-input')).toBe(true);for(const task of practice.tasks){if(task.type!=='multi-input')continue;expect(task.fields.every(field=>field.validation==='exact-decimal')).toBe(true)}});
test('lesson 149 mandatory practice keeps exact decimal semantics',()=>{expect(normalizeExactDecimalPracticeAnswer('094710,000')).toBe('94710');const practice=extendedPracticeByLesson[149];const task=practice.tasks[0];expect(task.type).toBe('multi-input');if(task.type!=='multi-input')return;expect(isExtendedPracticeAnswerCorrect(task,{a:'094710,000'})).toBe(true);expect(isExtendedPracticeAnswerCorrect(task,{a:'94710,0000001'})).toBe(false);expect(isExtendedPracticeAnswerCorrect(task,{a:'94709,9999999'})).toBe(false);});
