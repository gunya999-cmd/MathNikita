import {expect,test} from '@playwright/test';async function openLesson122(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 122:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 122 из 175')).toBeVisible();await expect(page.getByText('Урок 122 · глава 5 · § 33 · 4 из 6')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click())}async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:122,stageIndex}})),index)}async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').evaluate((node:HTMLButtonElement)=>node.click());await expect(stage.locator('.instant-feedback.good')).toBeVisible()}test('lesson 122 opens, solves exact §33 sources and reaches summary',async({page})=>{await openLesson122(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Составная задача начинается с карты зависимостей');await jump(page,8);await expect(stage).toContainText('№ 884');await solve(stage,['9,6','16,4','38,4']);await jump(page,9);await expect(stage).toContainText('№ 886');await solve(stage,['1744,10','177,76']);await jump(page,10);await expect(stage).toContainText('№ 894');await solve(stage,['26,394','37']);await jump(page,28);await expect(stage).toContainText('Составные задачи и удобные вычисления освоены');await expect(stage).toContainText('Урок 122 завершён')});
