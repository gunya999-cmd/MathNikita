import {expect,test,type Page} from '@playwright/test';
async function open154(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 154:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 154 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('[aria-label="Урок 154: повторение среднего арифметического и процентов"]')).toBeVisible()}
async function jump(page:Page,stageIndex:number){await page.evaluate(index=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:154,stageIndex:index}})),stageIndex)}
test('lesson 154 opens diagnostic task and persists progress',async({page})=>{await open154(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l154-practice-01"]');await expect(stage).toContainText('8, 12, 16 и 20');await expect(stage.locator('input')).toHaveCount(3);await stage.locator('input').nth(0).fill('056,0');await stage.locator('input').nth(1).fill('014,00');await stage.locator('input').nth(2).fill('012');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-154-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(saved?.responses?.['l154-practice-01:a']).toBe('056,0');await page.reload();await page.locator('button[aria-label^="Открыть урок 154:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l154-practice-01"]')).toBeVisible()});
test('lesson 154 rejects close but incorrect decimal',async({page})=>{await open154(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l154-practice-01"]');await stage.locator('input').nth(0).fill('56,0000001');await stage.locator('input').nth(1).fill('14');await stage.locator('input').nth(2).fill('12');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()});
test('lesson 154 reviews mean direct percent inverse percent and summary',async({page})=>{await open154(page);await jump(page,1);await expect(page.locator('.interactive-stage[data-stage-id="l154-mean"]')).toContainText('сумма = среднее × количество');await jump(page,2);await expect(page.locator('.interactive-stage[data-stage-id="l154-direct"]')).toContainText('240·0,35=84');await jump(page,3);await expect(page.locator('.interactive-stage[data-stage-id="l154-inverse"]')).toContainText('49:0,14=350');await jump(page,27);await expect(page.locator('.interactive-stage[data-stage-id="l154-summary"]')).toContainText('20 задач · 50 ответов · повторение § 36–38')});
