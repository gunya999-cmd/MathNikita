import {expect,test} from '@playwright/test';import fs from 'node:fs';
test('lesson 147 player keeps strict decimal semantics and 28-stage contract',()=>{const source=fs.readFileSync('src/PercentConversionApplicationsPlayer.tsx','utf8');expect(source).toContain("const KEY='mathnikita-lesson-147-progress-v1'");expect(source).toContain('canonicalDecimal');expect(source).not.toContain('Math.abs');expect(source).not.toContain('toFixed');expect(source).toContain('lessonNumber!==147');expect(source).toContain('20 задач · 50 ответов · § 37 · 2 из 4');expect(source).toContain('lessonOneHundredFortySevenPracticeResponseCount=lessonOneHundredFortySevenResponseCount');});