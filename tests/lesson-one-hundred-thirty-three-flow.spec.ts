import {expect,test,type Page} from '@playwright/test';async function open133(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 133:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 133 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByLabel('Урок 133: деление десятичной дроби на натуральное число')).toBeVisible()}async function jump(page:Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:133,stageIndex}})),index)}test('lesson 133 opens exact source practice and persists progress',async({page})=>{await open133(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l133-practice-01"]');await expect(stage).toBeVisible();await expect(stage.locator('input')).toHaveCount(6);for(const[index,value]of ['25,60','3.750','0,030','0,7020','0,000960','0,125700'].entries())await stage.locator('input').nth(index).fill(value);await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-133-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(Object.keys(saved?.responses??{})).toHaveLength(6);await page.reload();await page.locator('button[aria-label^="Открыть урок 133:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l133-practice-01"]')).toBeVisible();});test('lesson 133 rejects a close but incorrect decimal',async({page})=>{await open133(page);await jump(page,8);const stage=page.locator('.interactive-stage[data-stage-id="l133-practice-02"]');await expect(stage).toBeVisible();for(const[index,value]of ['14,2','3,45','2,3','0,27','0,123','20,599999999999'].entries())await stage.locator('input').nth(index).fill(value);await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible();});