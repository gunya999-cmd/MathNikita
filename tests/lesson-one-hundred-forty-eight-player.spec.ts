import {expect,test} from '@playwright/test';import fs from 'node:fs';
test('lesson 148 player keeps strict decimal semantics and 28-stage contract',()=>{const source=fs.readFileSync('src/PercentCompositeApplicationsPlayer.tsx','utf8');expect(source).toContain("const KEY='mathnikita-lesson-148-progress-v1'");expect(source).toContain('canonicalDecimal');expect(source).not.toContain('Math.abs');expect(source).not.toContain('toFixed');expect(source).toContain('lessonNumber!==148');expect(source).toContain('20 задач · 50 ответов · § 37 · 3 из 4');expect(source).toContain('lessonOneHundredFortyEightPracticeResponseCount=lessonOneHundredFortyEightResponseCount');});
