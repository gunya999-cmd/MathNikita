import {expect,test} from '@playwright/test';

test('lesson 45 polygon builder exposes five selectable vertices and blocks invalid self-crossing construction',async({page})=>{await page.addInitScript(()=>localStorage.setItem('mathnikita-mentor-auto-guide','false'));await page.goto('/');const chapterTwo=page.locator('.course-chapter-group').nth(1);if(!(await chapterTwo.evaluate(element=>(element as HTMLDetailsElement).open)))await chapterTwo.locator('summary').click();await page.getByRole('button',{name:/Открыть урок 45:/}).click();await page.locator('.lesson-opening-start').click();await page.evaluate(()=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:45,stageIndex:3}}));const stage=page.locator('[data-stage-id="l45-builder"]');await expect(stage).toBeVisible();await expect(stage.locator('[data-builder-point]')).toHaveCount(5);for(const vertex of ['C','B','D','E'])await stage.getByRole('button',{name:`Вершина ${vertex}`,exact:true}).click();await expect(stage.locator('.polygon-builder')).toHaveAttribute('data-builder-crossing','true');await expect(stage.locator('.lesson-controls .primary')).toBeDisabled();await stage.getByRole('button',{name:'Сбросить',exact:true}).click();for(const vertex of ['B','C','D','E'])await stage.getByRole('button',{name:`Вершина ${vertex}`,exact:true}).click();await expect(stage.locator('.polygon-builder')).toHaveAttribute('data-builder-valid','true');await expect(stage.locator('.lesson-controls .primary')).toBeEnabled()});
