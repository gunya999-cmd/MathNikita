import {expect,test} from '@playwright/test';
async function openLesson111(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 111:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 111 из 175')).toBeVisible();await expect(page.getByText('Урок 111 · глава 5 · § 30 · 3 из 4')).toBeVisible();await page.locator('.lesson-opening-start').click()}
async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:111,stageIndex}})),index)}
async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 111 opens, solves exact §30 sources and reaches summary',async({page})=>{await openLesson111(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Единицы измерения превращаются в десятичные дроби');await jump(page,8);await expect(stage).toContainText('№ 808');await solve(stage,['1,25','0,18','0,44','5,86','0,02','4,65']);await jump(page,9);await expect(stage).toContainText('№ 810(1–3)');await solve(stage,['4,2','0,35','24,84']);await jump(page,10);await expect(stage).toContainText('№ 816');await solve(stage,['10']);await jump(page,28);await expect(stage).toContainText('Урок 111 завершён')});
