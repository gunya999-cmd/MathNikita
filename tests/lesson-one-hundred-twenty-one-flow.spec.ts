import {expect,test} from '@playwright/test';
async function openLesson121(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 121:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 121 из 175')).toBeVisible();await expect(page.getByText('Урок 121 · глава 5 · § 33 · 3 из 6')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click())}
async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:121,stageIndex}})),index)}
async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').evaluate((node:HTMLButtonElement)=>node.click());await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 121 opens, solves exact §33 sources and reaches summary',async({page})=>{await openLesson121(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Сложение и вычитание работают как обратные действия');await jump(page,8);await expect(stage).toContainText('№ 869');await solve(stage,['4,38','2,272','56','7,86']);await jump(page,9);await expect(stage).toContainText('№ 882');await solve(stage,['1,54','70','445']);await jump(page,10);await expect(stage).toContainText('№ 892');await solve(stage,['91,35','11,987']);await jump(page,28);await expect(stage).toContainText('Уравнения и составные задачи § 33 освоены');await expect(stage).toContainText('Урок 121 завершён')});
