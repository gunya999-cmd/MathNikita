import {expect,test,type Page} from '@playwright/test';
async function openLesson82(page:Page){await page.goto('/');const chapterThree=page.locator('.course-chapter-group').nth(2);if(!(await chapterThree.evaluate(element=>(element as HTMLDetailsElement).open)))await chapterThree.locator('summary').click();const lessons=page.locator('.course-lesson-grid > button');await expect(lessons.nth(81)).toContainText('Объём прямоугольного параллелепипеда: формулы и задачи');await expect(lessons.nth(81)).toBeEnabled();await expect(lessons.nth(82)).toContainText('Объём прямоугольного параллелепипеда');await expect(lessons.nth(82)).toBeDisabled();await page.getByRole('button',{name:/Открыть урок 82:/}).click();await page.locator('.lesson-opening-start').click()}
async function jump(page:Page,stageIndex:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:82,stageIndex}})),stageIndex)}
async function noHorizontalOverflow(page:Page){return page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth+2)}
test('lesson 82 is usable on iPad, preserves numeric magnitude and edited-answer state, and reaches 50-response practice',async({page})=>{test.setTimeout(115_000);await page.addInitScript(()=>localStorage.setItem('mathnikita-mentor-auto-guide','false'));await openLesson82(page);const stage=page.locator('.lesson-runtime:not([hidden]) .interactive-stage[data-stage-id]');await expect(stage).toHaveAttribute('data-stage-id','l82-mission');expect(await noHorizontalOverflow(page)).toBeTruthy();await jump(page,4);await expect(stage).toHaveAttribute('data-stage-id','l82-layer-count');const layerInput=stage.locator('.inline-answer input');await layerInput.fill('30');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();await layerInput.fill('31');await expect(stage.getByRole('button',{name:'Дальше →',exact:true})).toBeDisabled();const edited=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-82-progress-v1')??'null'));expect(edited?.results?.['l82-p1']).toBe(false);await layerInput.fill('30');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();await jump(page,11);await expect(stage).toHaveAttribute('data-stage-id','l82-619-base');const input=stage.locator('.inline-answer input');for(const wrong of ['18.0','170']){await input.fill(wrong);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()}await expect(stage.getByRole('button',{name:'Дальше →',exact:true})).toBeEnabled();await input.fill('180');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const beforeReload=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-82-progress-v1')??'null'));expect(beforeReload?.results?.['l82-p6']).toBe(true);await page.reload();await page.getByRole('button',{name:/Открыть урок 82:/}).click();await page.locator('.lesson-opening-start').click();await expect(page.locator('.lesson-runtime:not([hidden]) .interactive-stage')).toHaveAttribute('data-stage-id','l82-619-base');await jump(page,35);await expect(page.locator('.lesson-runtime:not([hidden]) .interactive-stage')).toHaveAttribute('data-stage-id','l82-summary');await expect(page.locator('.extended-practice')).toHaveAttribute('data-practice-response-count','50');await expect(page.locator('.practice-pythagoras-actions button')).toHaveCount(4);expect(await noHorizontalOverflow(page)).toBeTruthy()});
