import {expect,test} from '@playwright/test';import {lessonOneHundredFiftyOnePractice,lessonOneHundredFiftyOneResponseCount} from '../src/data/lessonOneHundredFiftyOnePractice';import {extendedPracticeByLesson} from '../src/data/extendedPracticeData';import {extendedPracticeSetResponseCount} from '../src/data/extendedPracticeTypes';import {isExtendedPracticeAnswerCorrect,normalizeExactDecimalPracticeAnswer} from '../src/extendedPracticeEngine';
test('lesson 151 source and workload contract',()=>{expect(lessonOneHundredFiftyOnePractice).toHaveLength(20);expect(lessonOneHundredFiftyOneResponseCount).toBe(50);expect(lessonOneHundredFiftyOnePractice.slice(0,3).map(task=>task.source)).toEqual(['Мерзляк · §38 · №1098','Мерзляк · §38 · №1100','Мерзляк · §38 · №1102']);expect(lessonOneHundredFiftyOnePractice.slice(0,3).every(task=>task.sourceExact)).toBe(true);expect(lessonOneHundredFiftyOnePractice[0].fields.map(field=>field.answers[0])).toEqual(['350']);expect(lessonOneHundredFiftyOnePractice[1].fields.map(field=>field.answers[0])).toEqual(['240']);expect(lessonOneHundredFiftyOnePractice[2].fields.map(field=>field.answers[0])).toEqual(['24']);});
test('lesson 151 is registered in mandatory completion practice',()=>{const practice=extendedPracticeByLesson[151];expect(practice).toBeDefined();expect(practice.tasks).toHaveLength(20);expect(extendedPracticeSetResponseCount(practice)).toBe(50);expect(practice.tasks.every(task=>task.type==='multi-input')).toBe(true);for(const task of practice.tasks){if(task.type!=='multi-input')continue;expect(task.fields.every(field=>field.validation==='exact-decimal')).toBe(true)}});
test('lesson 151 mandatory practice keeps exact decimal semantics',()=>{expect(normalizeExactDecimalPracticeAnswer('0350,000')).toBe('350');const practice=extendedPracticeByLesson[151];const task=practice.tasks[0];expect(task.type).toBe('multi-input');if(task.type!=='multi-input')return;expect(isExtendedPracticeAnswerCorrect(task,{a:'0350,000'})).toBe(true);expect(isExtendedPracticeAnswerCorrect(task,{a:'350,0000001'})).toBe(false);});
