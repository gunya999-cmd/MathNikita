import {expect,test} from '@playwright/test';
async function openLesson119(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 119:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 119 из 175')).toBeVisible();await expect(page.getByText('Урок 119 · глава 5 · § 33 · 1 из 6')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click())}
async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:119,stageIndex}})),index)}
async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').evaluate((node:HTMLButtonElement)=>node.click());await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 119 opens, solves exact §33 sources and reaches summary',async({page})=>{await openLesson119(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Складываем одинаковые разряды');await jump(page,8);await expect(stage).toContainText('Вопрос 1');await solve(stage,['дописать нули','запятая под запятой','сложить как натуральные числа','поставить запятую']);await jump(page,9);await expect(stage).toContainText('№ 865');await solve(stage,['10,5','10,35','20,2','1,552','19,091','94,5']);await jump(page,10);await expect(stage).toContainText('№ 871');await solve(stage,['23,5','41,1']);await jump(page,28);await expect(stage).toContainText('Сложение десятичных дробей освоено');await expect(stage).toContainText('Урок 119 завершён')});
