import {expect,test,type Page} from '@playwright/test';
async function open152(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 152:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 152 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('[aria-label="Урок 152: составные задачи на нахождение числа по процентам"]')).toBeVisible()}
async function jump(page:Page,stageIndex:number){await page.evaluate(index=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:152,stageIndex:index}})),stageIndex)}
test('lesson 152 opens exact textbook task and persists progress',async({page})=>{await open152(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l152-practice-01"]');await expect(stage).toContainText('теряет 24%');await expect(stage.locator('input')).toHaveCount(1);await stage.locator('input').fill('025,000');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-152-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(saved?.responses?.['l152-practice-01:a']).toBe('025,000');await page.reload();await page.locator('button[aria-label^="Открыть урок 152:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l152-practice-01"]')).toBeVisible()});
test('lesson 152 rejects close but incorrect result',async({page})=>{await open152(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l152-practice-01"]');await stage.locator('input').fill('25,0000001');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()});
test('lesson 152 teaches complement, mixed chain and integer constraints',async({page})=>{await open152(page);await jump(page,0);await expect(page.locator('.interactive-stage[data-stage-id="l152-complement"]')).toContainText('38%');await jump(page,3);await expect(page.locator('.interactive-stage[data-stage-id="l152-chain"]')).toContainText('10,8:0,9=12');await jump(page,5);await expect(page.locator('.interactive-stage[data-stage-id="l152-integer"]')).toContainText('200 зрителей');await jump(page,9);const sourceChain=page.locator('.interactive-stage[data-stage-id="l152-practice-03"]');await expect(sourceChain.locator('input')).toHaveCount(3);await sourceChain.locator('input').nth(0).fill('10,8');await sourceChain.locator('input').nth(1).fill('12');await sourceChain.locator('input').nth(2).fill('30');await sourceChain.getByRole('button',{name:'Проверить'}).click();await expect(sourceChain.locator('.instant-feedback.good')).toBeVisible();await jump(page,27);await expect(page.locator('.interactive-stage[data-stage-id="l152-summary"]')).toContainText('20 задач · 50 ответов · § 38 · 3 из 4')});
