import {expect,test,type Page} from '@playwright/test';async function open138(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 138:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 138 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByLabel('Урок 138: скобки, уравнения и встречное движение')).toBeVisible()}async function jump(page:Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:138,stageIndex}})),index)}test('lesson 138 opens exact textbook practice and persists progress',async({page})=>{await open138(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l138-practice-01"]');await expect(stage).toBeVisible();await expect(stage.locator('input')).toHaveCount(1);await stage.locator('input').fill('02,8400');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-138-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(Object.keys(saved?.responses??{})).toHaveLength(1);await page.reload();await page.locator('button[aria-label^="Открыть урок 138:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l138-practice-01"]')).toBeVisible();});test('lesson 138 rejects a close but incorrect equation root',async({page})=>{await open138(page);await jump(page,8);const stage=page.locator('.interactive-stage[data-stage-id="l138-practice-02"]');await expect(stage.locator('input')).toHaveCount(2);await stage.locator('input').nth(0).fill('1,47');await stage.locator('input').nth(1).fill('4,379999999999');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible();});test('lesson 138 teaches bracket order and speed of approach',async({page})=>{await open138(page);await jump(page,0);await expect(page.locator('.interactive-stage[data-stage-id="l138-order"]')).toContainText('34−30,9248=3,0752');await jump(page,3);await expect(page.locator('.interactive-stage[data-stage-id="l138-motion"]')).toContainText('136,8:12=11,4');});
