import {expect,test} from '@playwright/test';
async function openLesson116(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 116:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 116 из 175')).toBeVisible();await expect(page.getByText('Урок 116 · глава 5 · § 32 · 1 из 3')).toBeVisible();await page.locator('.lesson-opening-start').click()}
async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:116,stageIndex}})),index)}
async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 116 opens, solves exact §32 sources and reaches summary',async({page})=>{await openLesson116(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Округление даёт близкое удобное число');await jump(page,8);await expect(stage).toContainText('№ 845(1)');await solve(stage,['16,9','4,7','1,3','48,2','37']);await jump(page,9);await expect(stage).toContainText('№ 845(2)');await solve(stage,['8,64','2,78','1','104,94']);await jump(page,10);await expect(stage).toContainText('№ 847(1)');await solve(stage,['530','18 360','4 783 390']);await jump(page,11);await expect(stage).toContainText('№ 847(2)');await solve(stage,['2200','1400']);await jump(page,12);await expect(stage).toContainText('№ 847(3)');await solve(stage,['313000','67000']);await jump(page,13);await expect(stage).toContainText('№ 860(1)');await solve(stage,['138']);await jump(page,28);await expect(stage).toContainText('Первое правило § 32 освоено');await expect(stage).toContainText('Урок 116 завершён')});
