import {expect,test,type Page} from '@playwright/test';async function open137(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 137:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 137 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByLabel('Урок 137: часть, целое и длинные выражения')).toBeVisible()}async function jump(page:Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:137,stageIndex}})),index)}test('lesson 137 opens exact textbook practice and persists progress',async({page})=>{await open137(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l137-practice-01"]');await expect(stage).toBeVisible();await expect(stage.locator('input')).toHaveCount(1);await stage.locator('input').fill('03,200');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-137-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(Object.keys(saved?.responses??{})).toHaveLength(1);await page.reload();await page.locator('button[aria-label^="Открыть урок 137:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l137-practice-01"]')).toBeVisible();});test('lesson 137 rejects a close but incorrect restored whole',async({page})=>{await open137(page);await jump(page,8);const stage=page.locator('.interactive-stage[data-stage-id="l137-practice-02"]');await stage.locator('input').fill('1199,999999999');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible();});test('lesson 137 distinguishes part from whole before choosing an operation',async({page})=>{await open137(page);await jump(page,0);await expect(page.locator('.interactive-stage[data-stage-id="l137-part"]')).toContainText('320×0,01=3,2');await jump(page,1);await expect(page.locator('.interactive-stage[data-stage-id="l137-whole"]')).toContainText('960:0,8=1200');});