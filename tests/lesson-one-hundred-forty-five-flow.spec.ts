import {expect,test,type Page} from '@playwright/test';
async function open145(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 145:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 145 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('[aria-label="Урок 145: итог среднего арифметического и средних величин"]')).toBeVisible()}
async function jump(page:Page,stageIndex:number){await page.evaluate(index=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:145,stageIndex:index}})),stageIndex)}
test('lesson 145 opens exact textbook mean-price task and persists progress',async({page})=>{await open145(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l145-practice-01"]');await expect(stage).toContainText('2 кг');await expect(stage).toContainText('88 р.');const input=stage.locator('input');await expect(input).toHaveCount(1);await input.fill('00112,000');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-145-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(saved?.responses?.['l145-practice-01:a']).toBe('00112,000');await page.reload();await page.locator('button[aria-label^="Открыть урок 145:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l145-practice-01"]')).toBeVisible()});
test('lesson 145 rejects close but incorrect exact decimal',async({page})=>{await open145(page);await jump(page,8);const stage=page.locator('.interactive-stage[data-stage-id="l145-practice-02"]');await stage.locator('input').fill('2,400000001');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()});
test('lesson 145 teaches weighted groups, rounding and fish scheduling',async({page})=>{await open145(page);await jump(page,1);await expect(page.locator('.interactive-stage[data-stage-id="l145-weighted"]')).toContainText('нельзя просто');await jump(page,4);await expect(page.locator('.interactive-stage[data-stage-id="l145-rounding"]')).toContainText('1,28≈1,3');await jump(page,5);await expect(page.locator('.interactive-stage[data-stage-id="l145-scheduling"]')).toContainText('6');await expect(page.locator('.interactive-stage[data-stage-id="l145-scheduling"]')).toContainText('3 минут');await jump(page,27);await expect(page.locator('.interactive-stage[data-stage-id="l145-summary"]')).toContainText('20 задач · 50 ответов · § 36 · 3 из 3')});
