import {expect,test,type Page} from '@playwright/test';async function open128(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 128:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 128 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByLabel('Урок 128: свойства умножения десятичных дробей')).toBeVisible()}async function jump(page:Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:128,stageIndex}})),index)}test('lesson 128 opens exact source practice and persists progress',async({page})=>{await open128(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l128-practice-01"]');await expect(stage).toBeVisible();await expect(stage.locator('input')).toHaveCount(4);for(const[index,value]of ['32,8','2,43','475','14,6'].entries())await stage.locator('input').nth(index).fill(value);await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-128-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(Object.keys(saved?.responses??{})).toHaveLength(4);await page.reload();await page.locator('button[aria-label^="Открыть урок 128:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l128-practice-01"]')).toBeVisible();});