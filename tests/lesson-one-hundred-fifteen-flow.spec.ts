import {expect,test} from '@playwright/test';
async function openLesson115(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 115:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 115 из 175')).toBeVisible();await expect(page.getByText('Урок 115 · глава 5 · § 31 · 3 из 3')).toBeVisible();await page.locator('.lesson-opening-start').click()}
async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:115,stageIndex}})),index)}
async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 115 opens, solves exact §31 sources and reaches summary',async({page})=>{await openLesson115(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Фиксированная точность задаёт шаг');await jump(page,8);await expect(stage).toContainText('№ 834');await solve(stage,['1,1','1,01','4,001','10,0001']);await jump(page,9);await expect(stage).toContainText('№ 836');await solve(stage,['10,537','10,542','10,549']);await jump(page,10);await expect(stage).toContainText('№ 842');await solve(stage,['123','132','213','231','312','321']);await jump(page,11);await expect(stage).toContainText('№ 843');await solve(stage,['90 с']);await jump(page,28);await expect(stage).toContainText('§ 31 полностью завершён');await expect(stage).toContainText('Урок 115 завершён')});

test('lesson 115 accepts alternative valid answers for source № 836',async({page})=>{await openLesson115(page);await jump(page,9);const stage=page.locator('.interactive-stage[data-stage-id="l115-practice-02"]');await solve(stage,['10,531','10,54','10,5499'])});
