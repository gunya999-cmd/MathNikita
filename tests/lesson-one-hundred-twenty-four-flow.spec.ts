import {expect,test} from '@playwright/test';async function openLesson124(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 124:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 124 из 175')).toBeVisible();await expect(page.getByText('Урок 124 · глава 5 · § 33 · 6 из 6')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click())}async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:124,stageIndex}})),index)}async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').evaluate((node:HTMLButtonElement)=>node.click());await expect(stage.locator('.instant-feedback.good')).toBeVisible()}test('lesson 124 opens, solves exact §33 anchors and reaches summary',async({page})=>{await openLesson124(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Перед контрольной собираем карту § 33');await jump(page,8);await expect(stage).toContainText('№ 887');await solve(stage,['5,7','14,1','29']);await jump(page,9);await expect(stage).toContainText('№ 889');await solve(stage,['191,1','3,0659','370,894']);await jump(page,10);await expect(stage).toContainText('№ 895');await solve(stage,['4x+5,7807','9c+241,845']);await jump(page,11);await expect(stage).toContainText('№ 898');await solve(stage,['105,23','30,041','32,69']);await jump(page,28);await expect(stage).toContainText('Сложение и вычитание десятичных дробей систематизированы');await expect(stage).toContainText('Урок 124 завершён')});