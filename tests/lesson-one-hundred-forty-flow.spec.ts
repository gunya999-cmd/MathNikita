import {expect,test,type Page} from '@playwright/test';async function open140(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 140:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 140 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByLabel('Урок 140: подобные слагаемые и движение по реке')).toBeVisible()}async function jump(page:Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:140,stageIndex}})),index)}test('lesson 140 opens exact textbook expression and persists progress',async({page})=>{await open140(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l140-practice-01"]');await expect(stage).toBeVisible();await expect(stage.locator('input')).toHaveCount(1);await stage.locator('input').fill('054,4200');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-140-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(Object.keys(saved?.responses??{})).toHaveLength(1);await page.reload();await page.locator('button[aria-label^="Открыть урок 140:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l140-practice-01"]')).toBeVisible();});test('lesson 140 rejects a close but incorrect equation answer',async({page})=>{await open140(page);await jump(page,8);const stage=page.locator('.interactive-stage[data-stage-id="l140-practice-02"]');await stage.locator('input').nth(0).fill('0,159999999');await stage.locator('input').nth(1).fill('0,14');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible();});test('lesson 140 explains river speeds and minute conversion',async({page})=>{await open140(page);await jump(page,3);await expect(page.locator('.interactive-stage[data-stage-id="l140-river-speed"]')).toContainText('28,4+2,1=30,5');await jump(page,4);await expect(page.locator('.interactive-stage[data-stage-id="l140-river-time"]')).toContainText('60,49:26,3=2,3');await jump(page,5);await expect(page.locator('.interactive-stage[data-stage-id="l140-minutes"]')).toContainText('0,5·60=30 минут');});