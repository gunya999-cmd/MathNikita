import {expect,test,type Page} from '@playwright/test';
async function openLesson81(page:Page){await page.goto('/');const chapterThree=page.locator('.course-chapter-group').nth(2);if(!(await chapterThree.evaluate(element=>(element as HTMLDetailsElement).open)))await chapterThree.locator('summary').click();const lessons=page.locator('.course-lesson-grid > button');await expect(lessons.nth(80)).toContainText('Объём фигуры: единичный куб и кубические единицы');await expect(lessons.nth(80)).toBeEnabled();await expect(lessons.nth(81)).toContainText('Объём прямоугольного параллелепипеда');await expect(lessons.nth(81)).toBeDisabled();await page.getByRole('button',{name:/Открыть урок 81:/}).click();await page.locator('.lesson-opening-start').click()}
async function jump(page:Page,stageIndex:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:81,stageIndex}})),stageIndex)}
async function noHorizontalOverflow(page:Page){return page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth+2)}
test('lesson 81 is usable on iPad, rejects decimal magnitude tricks, resets edited correctness and reaches 50-response practice',async({page})=>{test.setTimeout(110_000);await page.addInitScript(()=>localStorage.setItem('mathnikita-mentor-auto-guide','false'));await openLesson81(page);const stage=page.locator('.lesson-runtime:not([hidden]) .interactive-stage[data-stage-id]');await expect(stage).toHaveAttribute('data-stage-id','l81-mission');expect(await noHorizontalOverflow(page)).toBeTruthy();await jump(page,2);await expect(stage).toHaveAttribute('data-stage-id','l81-oral2-count');const input=stage.locator('.inline-answer input');for(const wrong of ['6','10']){await input.fill(wrong);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()}await expect(stage.getByRole('button',{name:'Дальше →',exact:true})).toBeEnabled();await input.fill('8');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();await input.fill('9');await expect(stage.getByRole('button',{name:'Дальше →',exact:true})).toBeDisabled();const edited=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-81-progress-v1')??'null'));expect(edited?.results?.['l81-p1']).toBe(false);await input.fill('8');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();await jump(page,18);await expect(stage).toHaveAttribute('data-stage-id','l81-617-length');const lengthInput=stage.locator('.inline-answer input');await lengthInput.fill('1,0');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible();await lengthInput.fill('10');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();await jump(page,20);await expect(stage).toHaveAttribute('data-stage-id','l81-617-volume');const volumeInput=stage.locator('.inline-answer input');await volumeInput.fill('10.00');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible();const beforeReload=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-81-progress-v1')??'null'));expect(beforeReload?.results?.['l81-p1']).toBe(true);await page.reload();await page.getByRole('button',{name:/Открыть урок 81:/}).click();await page.locator('.lesson-opening-start').click();await expect(page.locator('.lesson-runtime:not([hidden]) .interactive-stage')).toHaveAttribute('data-stage-id','l81-617-volume');await jump(page,35);await expect(page.locator('.lesson-runtime:not([hidden]) .interactive-stage')).toHaveAttribute('data-stage-id','l81-summary');await expect(page.locator('.extended-practice')).toHaveAttribute('data-practice-response-count','50');await expect(page.locator('.practice-pythagoras-actions button')).toHaveCount(4);expect(await noHorizontalOverflow(page)).toBeTruthy()});
