import {expect,test,type Page} from '@playwright/test';
async function open149(page:Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 149:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 149 из 175')).toBeVisible();await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('[aria-label="Урок 149: итоговые задачи на проценты"]')).toBeVisible()}
async function jump(page:Page,stageIndex:number){await page.evaluate(index=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:149,stageIndex:index}})),stageIndex)}
test('lesson 149 opens exact textbook task and persists progress',async({page})=>{await open149(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l149-practice-01"]');await expect(stage).toContainText('246 000');await expect(stage).toContainText('110%');const input=stage.locator('input');await expect(input).toHaveCount(1);await input.fill('094710,000');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-149-progress-v1')??'null'));expect(saved?.stageIndex).toBe(7);expect(saved?.responses?.['l149-practice-01:a']).toBe('094710,000');await page.reload();await page.locator('button[aria-label^="Открыть урок 149:"]').evaluate((node:HTMLButtonElement)=>node.click());await page.locator('.lesson-opening-start').evaluate((node:HTMLButtonElement)=>node.click());await expect(page.locator('.interactive-stage[data-stage-id="l149-practice-01"]')).toBeVisible()});
test('lesson 149 rejects close but incorrect result',async({page})=>{await open149(page);await jump(page,7);const stage=page.locator('.interactive-stage[data-stage-id="l149-practice-01"]');await stage.locator('input').fill('94710,0000001');await stage.getByRole('button',{name:'Проверить'}).click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()});
test('lesson 149 teaches changing percentage bases and closes section 37',async({page})=>{await open149(page);await jump(page,0);await expect(page.locator('.interactive-stage[data-stage-id="l149-base"]')).toContainText('100%');await jump(page,2);await expect(page.locator('.interactive-stage[data-stage-id="l149-remainder-base"]')).toContainText('2040');await jump(page,4);await expect(page.locator('.interactive-stage[data-stage-id="l149-repeated-change"]')).toContainText('1,10');await jump(page,27);await expect(page.locator('.interactive-stage[data-stage-id="l149-summary"]')).toContainText('20 задач · 50 ответов · § 37 · 4 из 4')});
