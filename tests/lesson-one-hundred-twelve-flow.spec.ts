import {expect,test} from '@playwright/test';
async function openLesson112(page:import('@playwright/test').Page){await page.goto('/');const button=page.locator('button[aria-label^="Открыть урок 112:"]');await expect(button).toBeEnabled();await button.evaluate((node:HTMLButtonElement)=>node.click());await expect(page.getByText('Урок 112 из 175')).toBeVisible();await expect(page.getByText('Урок 112 · глава 5 · § 30 · 4 из 4')).toBeVisible();await page.locator('.lesson-opening-start').click()}
async function jump(page:import('@playwright/test').Page,index:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:112,stageIndex}})),index)}
async function solve(stage:import('@playwright/test').Locator,values:string[]){const inputs=stage.locator('input');for(const [index,value] of values.entries())await inputs.nth(index).fill(value);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible()}
test('lesson 112 opens, solves exact §30 sources and reaches summary',async({page})=>{await openLesson112(page);const stage=page.locator('.interactive-stage[data-stage-id]').first();await expect(stage).toContainText('Деление на тысячу и десять тысяч');await jump(page,8);await expect(stage).toContainText('№ 810(4–6)');await solve(stage,['0,5876','2,6435','0,058']);await jump(page,9);await expect(stage).toContainText('№ 813');await solve(stage,['1','6','8','14','19','22']);await jump(page,10);await expect(stage).toContainText('№ 818');await solve(stage,['>','<']);await jump(page,28);await expect(stage).toContainText('Урок 112 завершён')});
