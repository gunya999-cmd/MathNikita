import {expect,test,type Page} from '@playwright/test';
async function openLesson89(page:Page){await page.goto('/');const chapter=page.locator('.course-chapter-group').nth(2);if(!(await chapter.evaluate(el=>(el as HTMLDetailsElement).open)))await chapter.locator('summary').click();const lessons=page.locator('.course-lesson-grid > button');await expect(lessons.nth(88)).toContainText('Повторение главы 3');await expect(lessons.nth(88)).toBeEnabled();await page.getByRole('button',{name:/Открыть урок 89:/}).click();await page.locator('.lesson-opening-start').click()}
async function jump(page:Page,stageIndex:number){await page.evaluate(stageIndex=>window.dispatchEvent(new CustomEvent('mathnikita-go-to-stage',{detail:{lessonNumber:89,stageIndex}})),stageIndex)}
test('lesson 89 full flow preserves correction logic, retry persistence and exact practice contract',async({page})=>{test.setTimeout(120_000);await page.addInitScript(()=>localStorage.setItem('mathnikita-mentor-auto-guide','false'));await openLesson89(page);const stage=page.locator('.lesson-runtime:not([hidden]) .interactive-stage[data-stage-id]');await expect(stage).toHaveAttribute('data-stage-id','l89-mission');await jump(page,4);await expect(stage).toHaveAttribute('data-stage-id','l89-remainder-q');const input=stage.locator('.inline-answer input');for(const wrong of ['32','34']){await input.fill(wrong);await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.bad')).toBeVisible()}await expect(stage.getByRole('button',{name:'Дальше →',exact:true})).toBeEnabled();await input.fill('33');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();const saved=await page.evaluate(()=>JSON.parse(localStorage.getItem('mathnikita-lesson-89-progress-v1')??'null'));expect(saved?.results?.['l89-p2']).toBe(true);await jump(page,25);await expect(stage).toHaveAttribute('data-stage-id','l89-digits-list');await stage.getByRole('button',{name:'307, 370, 703, 730',exact:true}).click();await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();await jump(page,28);await expect(stage).toHaveAttribute('data-stage-id','l89-edges-third');await stage.locator('.inline-answer input').fill('9');await stage.locator('.check-button').click();await expect(stage.locator('.instant-feedback.good')).toBeVisible();await jump(page,35);await expect(stage).toHaveAttribute('data-stage-id','l89-summary');await expect(page.locator('.lesson-reflection .extended-practice')).toHaveAttribute('data-practice-response-count','50');await expect(page.locator('.lesson-reflection .extended-practice')).toHaveAttribute('data-practice-task','l89-extra-01')});
