name: Lesson 103 data check

on:
  workflow_dispatch:
  push:
    branches: [lesson-103-build]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lesson-103-data:
    name: Lesson 103 · source data, player and 20/50 contract
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 24}
      - run: npm install
      - run: npx playwright test tests/lesson-one-hundred-three-data.spec.ts tests/lesson-one-hundred-three-player.spec.ts --config=playwright.desktop.config.ts --workers=1
