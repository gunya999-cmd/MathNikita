name: Lesson 37 hard retest

on:
  workflow_dispatch:
  push:
    branches: [agent/lesson-37-angle-notation]
  pull_request:
    branches: [main]
    paths:
      - 'src/AngleNotationPlayer.tsx'
      - 'src/angleLesson.css'
      - 'src/LessonThirtySevenOpening.ts'
      - 'src/data/extendedPracticeLesson37.ts'
      - 'src/data/extendedPracticeData.ts'
      - 'src/data/yearPlan.ts'
      - 'src/LessonCourseShell.tsx'
      - 'src/CourseCatalog.tsx'
      - 'tests/lesson-thirty-seven-ipad.spec.ts'
      - 'tests/lesson-thirty-seven-voice.spec.ts'
      - 'tests/course-plan.spec.ts'
      - 'tests/pedagogical-practice-quality.spec.ts'

permissions:
  contents: read

concurrency:
  group: lesson-37-hard-retest-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build · lesson 37
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24 }
      - run: npm install
      - run: npm run build
      - run: npx playwright test tests/pedagogical-practice-quality.spec.ts tests/extended-practice-count.spec.ts --config=playwright.desktop.config.ts --workers=1

  chromium:
    name: Chromium · lesson 37 flow, voice and 20-task practice
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PRACTICE_START: '37'
      PRACTICE_END: '37'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24 }
      - run: npm install
      - run: npx playwright install --with-deps chromium
      - run: npx playwright test tests/lesson-thirty-seven-ipad.spec.ts tests/lesson-thirty-seven-voice.spec.ts tests/extended-practice-all-lessons.spec.ts tests/course-plan.spec.ts --config=playwright.desktop.config.ts --workers=1

  webkit:
    name: iPad WebKit · lesson 37 flow, voice and 20-task practice
    runs-on: ubuntu-latest
    timeout-minutes: 35
    env:
      PRACTICE_START: '37'
      PRACTICE_END: '37'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 24 }
      - run: npm install
      - run: npx playwright install --with-deps webkit
      - run: npx playwright test tests/lesson-thirty-seven-ipad.spec.ts tests/lesson-thirty-seven-voice.spec.ts tests/extended-practice-all-lessons.spec.ts tests/course-plan.spec.ts --config=playwright.config.ts --workers=1
