name: Lesson 106 data check

on:
  workflow_dispatch:
  push:
    branches: [lesson-106-build]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  lesson-106-data:
    name: Lesson 106 · source data, player and 20/50 contract
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 24}
      - run: npm install
      - run: npx playwright test tests/lesson-one-hundred-six-data.spec.ts tests/lesson-one-hundred-six-player.spec.ts --config=playwright.desktop.config.ts --workers=1
